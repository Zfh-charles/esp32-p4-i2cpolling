---
description: P4 战略哲学 — 世界观与方法论（引用时读，不含当前状态）
alwaysApply: false
---

# 小智 P4 · 战略哲学

> 只放**方法论**，不放操作规则（在 `xiaozhi-p4-core.mdc`）、不放当前状态（在 `p4-current-state.mdc`）。
> 装备合一，不互相替代。冲突时以「可实践」为最高。

## 思想装备（经典 → 工程职责）

| 经典 / 方法 | 工程职责 |
|------|----------|
| **实践论** | 感性（串口现象）→ 理性（可否证假设）→ 实践（冷启+marker+关键字）→ 再实践（过门升阶/误判撤回）。直接经验（本板日志+本 marker ELF）> 类比猜测；间接经验（IDF 源码）只用于**设计探针**。 |
| **矛盾论** | 抓主矛盾与主要方面；特殊矛盾特殊解；可统一则调度（TTS↔脸），主矛盾转化必须**换武器**并重跑归因序。 |
| **中国革命战争的战略问题** | 研究**本板特殊规律**（账本即汇编）；积极防御（先保可观测/可烧录）；慎重初战；集中兵力打歼灭（单变量打透）；有根据地才进攻；反拼命主义。 |
| **论持久战** | 三阶段：防御→相持→反攻；**反速胜**（一次播满/一次推完）、**反投降**（砍表情/关 AFE/降频保命）；战役速决、战争持久；主动灵活有计划。 |
| **孙子兵法** | 庙算（先读作战卡）；知彼知己（彼=PANIC/mtval/ra，己=栈深/预算/通道/烧录门）；先为不可胜（SAFE_MODE/冷启门/ELF/watchpoint）；先胜而后战；避实击虚；知止（过门则止升阶，证伪则止假设）。 |
| **科学实验法** | 假设可证伪；预见 M+/M−；单变量**判决**；确定性证据（可重复+可定位+可对照+单变量）；失败回滚。排除一层≠证实下一层。 |
| **软件迭代演进** | 绞杀旧路径；版本即实验号（`FW_MARKER`/`s1xx`）；门禁进化（未过门禁升阶）；可观测性先行；误判债必还；DoD=日志闭环（编译成功≠交付）；批次即回滚单元；新功能默认关。 |

## 第一性原理（不可再分解）

1. 云端听见用户 → **UDP 上行 Opus**（+ wake opus）。
2. 板上有表情 → **`SD_MOUNT_OK` + `seed_stills`**（USB 复位≠冷启）。
3. 脸的正确抽象 → **seed + 状态增量 + 局部刷新**；「局部」由**素材能力**定义，不由固定行数定义。
   - 刷新成本量子 ≈ **全宽×50 行**（`buffer_size=width*50`，`area_px` 峰值 24000）；横向缩框不省钱。真正降本＝减行数或换独立小图。
   - 降本分维：`decode`（JPEG 尺寸）≠ `present/flush`（行带面积）≠ `canvas 写入`；从全帧抠小块只降后两项，`decode_ms` 一分不省。
4. 崩溃可治 → **可归因现场**；否则只加探针。
5. 可迭代 → 风暴下仍能烧录（`FLASH_WINDOW`/`SAFE_MODE`）。
6. 状态提交优于推帧；通道分离（MQTT 控制 / UDP 媒体）。

## 实验循环（强制）

```
四问（收益/损失/回滚/验证）→ 假设 H（可证伪）→ 预见 M+/M−
→ 组正交批次（每项可关断+独立标记+独立 M+/M−）→ 冷启+脚本裁决
→ 过门则记账 → 崩则整批回 known-good，再开关二分 → 账本一行
```

**确定性证据**最低标准（少一条判决无效）：可重复（同 marker/场景）· 可定位（PANIC_LAST/addr2line 本 ELF/明确排除链）· 可对照（改前后或正反例）· 单变量（一个因果结论只由一个变量支撑）。

## 反面倾向（论持久战两种错误）

- **反速胜**：一次 PR 播满 SD 帧 / 一次全屏 MJPEG「搞定动画」 / 一次推完 P0–P3。
- **反投降**：关表情、关 AFE、永久砍功能「只要不死」＝以砍目标制造假稳定。
- 持久战的「武器」是正确抽象（seed+增量+局部）与硬件诚实，不是一时帧数多寡。
