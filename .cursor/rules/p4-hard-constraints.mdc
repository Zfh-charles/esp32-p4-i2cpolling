---
description: P4 硬约束 — 踩坑后冻结的板级特殊规律（命令式，无隐喻）
globs: xiaozhi-p4-epdainaozhong/main/**/*.{c,cc,h},xiaozhi-p4-epdainaozhong/main/**/*.cpp,xiaozhi-p4-epdainaozhong/sdkconfig*
alwaysApply: false
---

# P4 硬约束（冻结事实）

> 均由重复真机证据钉死。改动前若与本表冲突，先证伪本表再动。新硬约束经真机确认后追加，单次实验/阶段状态写 `p4-current-state.mdc`。

## 1. 硅片 / PSRAM（主矛盾）

- 芯片 **P4 v1.0**，命中 errata **MSPI-750 / MSPI-751**（v3.1 才修）：DMA/CACHE 随机访问 PSRAM「先写后读」在频率比越界时返回旧数据。
- 保持 **`SPIRAM_SPEED_200M`** 满足 `N×freq_core ≥ freq_axi`（freq_core=MSPI 钟，要**高**）。**禁**降 20M「规避」（`s1cf-ratio` 已证方向反、`s1ci` 已撤回）；**禁** ECC（对时序病无效，吃 1/8 容量）。
- **禁**用业务刀（脸/ROI/AFE/降 PSRAM 容量）去修硅片。IDF 5.4.1 无官方 MSPI-751 软规避开关。
- 降本只降**每 tick 总线突发**，不删 seed/底图缓存（容量 ~11.3MB/32MB 非瓶颈）。

## 2. SD 卡

- 表情资源在 `/sdcard/...`。无 `SD_MOUNT_OK` + `seed_stills done ok=6/6` **不验脸**。
- 已挂载成功 → 直接复用；**禁** deinit 后 remount；**禁** `InitEmotionSystem` 破坏性循环重挂。
- USB/esptool hard_reset 后 SD 常 TIMEOUT；涉 SD/电源/时序验收**必须物理断电冷启**。`init_fail` 只催冷启，不改 SD 读逻辑。

## 3. 通道（控制 / 媒体分离）

- MQTT 承载 `hello`/`listen`/`stt`/`tts`/`llm` 等控制 JSON；UDP 承载加密 Opus（上行麦 + 下行 TTS）。
- 云端要识别语音 → 必须 `MAIN_EVENT_SEND_AUDIO → protocol_->SendAudio()` **UDP 上行 Opus**。
- `CONFIG_SEND_WAKE_WORD_DATA=y` → 唤醒后先发 wake opus 再 `SendWakeWordDetected`。
- **禁**为省 CPU/带宽关上行 Opus 或 `SEND_WAKE_WORD_DATA` 却期望云端 stt。

## 4. AFE / 任务栈 / 共享对象

- `audio_detection=12288`、`audio_communication=8192`，开 `FREERTOS_WATCHPOINT_END_OF_STACK`。**禁**恢复小栈；ESP-SR `fetch()`/WakeNet 在**调用者栈**上跑 FFT+卷积。
- **禁**用阻塞调用返回后的 `uxTaskGetStackHighWaterMark` 判栈；须用**深处采样**的 `stack_free`。
- AFE：`reset_buffer` 仅 fetch 任务；停 AFE 先清 Feed 门再 Stop；**Feed 与 reset_buffer 同 `afe_ops_mutex_`**（s1bu）。
- 共享总线对象（AT/MQTT/HTTP/Modem）：**谁 Connect 谁销毁**；禁跨任务 `reset`/`delete` 仍被持有的对象（s1aw/s1br UAF）。
- 深活（JPEG decode/present）**禁**放全局 timer 小栈（`esp_timer` 3584）；`face_worker`/承载任务须 ≥8192 且深处采样判栈（s1bh/s1bi）。

## 5. 显示所有权 / 听态序（LVGL）

- 对话走 LVGL canvas；**禁**旁路 DirectPanelBlit 抢面板（易「日志 OK 肉眼无变化」或 WDT）。
- idle/唤醒武装后**尽早 PauseMjpeg**；对话 listen/speak 保持 decode pause。
- 听态序：**pause MJPEG → 关 AFE → 开 voice → present**（禁先开 voice 再关 AFE）。
- 全屏事务**单例 + 安全窗**：同时刻最多一个全屏底图/transition，避开 AFE fetch 与 TTS 起始边沿。
- **视觉可丢、音频不可丢**：优先级 音频采集·播放 > 会话·网络控制 > 必显 UI 状态 > 情绪静态切换 > 动画帧。视觉走 latest-value 覆盖式，过期即丢，**禁补播**。

## 6. 烧录 / 后勤（可烧录=根据地）

- 一次 esptool **`Write timeout`** / 读 0 字节 → **立即停**一切 esptool/`flash_when_alive`；禁软重枚举、禁连打抢窗。只催用户**物理断电**或**按 BOOT 上电**进 ROM download；确认后才单次抢窗。
- **未烧进 ≠ 已上板**：`Write timeout`/`FLASH_GAVE_UP`/未见新 `FW_MARKER` → 仍跑旧固件。
- 双 OTA：`idf.py flash` 只写 ota_0，板可从 ota_1 起 → **双槽写 + 复位 otadata**；冷启见 `FW_MARKER` 才算上板。
- 重启风暴：`FLASH_WINDOW`（streak 拉长静默）+ `SAFE_MODE`（streak≥5 停在 `Application::Start` 前，USB 可写）。
- 占用 COM7 前停 monitor；构建后立刻存 `elf_snapshots/xiaozhi_<marker>.elf`（A8，无 ELF=不可复判）。

## 7. 故意保留、永不「还债」

禁 EXTEND/loop（s1ah → WDT）· 禁 compose 全屏 blit（s1bk → WDT）· speak 700ms settle + `FACE_BACKOFF` · 禁 speaking inject / 全屏 short_anim burst · 禁 ROI `prime` 再 seed full=480²。

## 8. 韧性 / 冗余落地约束（硅片受限，设计原则见 core H）

> 依据：两周 356 次 REBOOT 审计，~90% panic 为 MSPI-751 PSRAM 总线腐败（合法 PSRAM 读失败 / nibble 互换 / 移位 / null）。软件不可根治 → 按下列约束**廉价存活 + 冗余**。

- **控制态放内部 SRAM，不放 PSRAM**：指针 / 数组索引 / 长度 / 状态机 / 会话·情绪·提醒队列等**控制数据**必须在片内 SRAM（`MALLOC_CAP_INTERNAL`）；PSRAM 只放**可重建**的像素/解码缓存。控制态被腐败＝野指针硬崩，像素被腐败＝一帧花屏（可容忍）。
- **PSRAM 读出的指针/索引 deref 前必校验**：范围/对齐/幻数检查，越界走冗余副本或安全默认，**禁直接 deref**（LoadAccessFault 直接触发点）。关键结构建议**异区双副本 + 校验和**。
- **PSRAM DMA/拷贝 4B 对齐 + burst 安全**（MSPI-750 非 4B 对齐 burst1/2 读回旧值）；present memcpy/flush 路径核对起址与长度对齐。
- **暖启动恢复**：崩溃复位（`crash_streak>0`，区别 power-on）走快恢复——`RTC_NOINIT`/NVS 存最小关键态（当前情绪 / 会话 / 提醒队列 / last-good seed 指纹），复位后**秒级贴 last-good seed** 而非慢 init 黑屏。禁把暖恢复写坏冷启动路径（改启动顺序＝独占批次 + 先 known-good）。
- **分层看门狗软恢复**：子系统卡死优先软重启该子系统（face_worker/modem），非整片复位；SAFE_MODE（streak≥5）保底不变。
- **崩因分流自动化**：`mtval≈sp`（栈类，**可修**：加栈/watchpoint）与 nibble-swap/移位 mtval（硅片类，**只降暴露**）必须分开归因，禁混判（见 core C / face-emotion 归因序）。
