---
description: P4 会话预检 — 每回合唯一必读（四问/批次/预算/分层索引）
alwaysApply: true
---

# P4 作战卡（每回合唯一必读）

> 这是唯一强制注入的规则。其余**按需**读（省算力）。先庙算，后代码。

## 一、分层索引（触发 → 读哪份）

| 触发 | 读 |
|------|----|
| 改代码前动了 `main/**`（SD/AFE/Opus/显示/烧录） | `p4-hard-constraints.mdc`（冻结事实） |
| 崩溃 / WDT / panic 归因 | `face-emotion-sd.mdc`（归因序 + 战史账本） |
| 表情 / 显示 / 音画调度出刀 | `face-emotion-sd.mdc` |
| 换阶段 / 定原则 / 改战略 | `xiaozhi-p4-core.mdc`（原则 A–G） |
| 想引用世界观/方法论 | `p4-strategy-philosophy.mdc` |
| 「我在哪、known-good 是谁、下一刀」 | `p4-current-state.mdc`（**唯一常改**） |
| 产品地图 / 长期契约 | `.cursorrules` · `AGENTS.md` |

**你在这（摘要，权威以 `boot_trace.cc` 的 `FW_MARKER` + current-state **§0** 为准）**：硅片地板仍在（P4 v1.0 MSPI-751）+ 相持打法（预算化视觉）。真源 marker=`s1fp_smell_lock_names`（=s1fo 路径）；**KG-Life**=`s1cm`；**KG-Function**=`s1fk`/`s1fm`/`s1fo`；**KG-Archive**=`s1eo`；滚动 V3 占用=`s1fp`。可发布核=会话闭环+分层嘴+预算消费；enter/blink 仍冻结。下一刀见 current-state §0。

## 二、出刀四问（必答，≤4 行写进回复；答不出 → 只侦察）

1. **收益**：`S3` 解锁阶梯/根因 · `S2` 寿命量级 · `S1` 体验/可观测 · `S0` 仅观感
2. **损失**：`D3` 伤根据地（不可烧录/不可归因/SD 门/音频链路/分区/素材）· `D2` 功能回退 · `D1` 观感 · `D0` 无感
3. **回滚**：`R0` 运行期开关 · `R1` 切 known-good 槽 · `R2` git revert+重烧 · `R3` 不可逆
4. **验证**：`V0` 编译 · `V1` USB 启动可见 · `V2` 必须冷启 · `V3` 长泡（完成寿命样本或右删失暴露）

**裁决**：`收益≥S1` 且 `回滚≤R2` 且（`损失≤D2` 或 已有 known-good+用户确认）才出刀。禁 `S0×V3`、禁 `D3×R3`。把握 <90% → 降级为探针（探针=`S1/D0/R0/V1`，最划算）。

## 三、批次准入（一次烧录 = 一个批次）

单变量纪律约束的是**判决**，不是烧录次数。批次内多改动合法的充要条件：① 运行期可关断（旧路径保留）② 独立日志标记 `s1xx-a/b/c` ③ 独立 M+/M−。满足即正交批次，崩了用**开关二分**（重启换配置，不重编译）。

- `R0/R1` 且不伤根据地 → **批量出刀**（推荐）。
- 伤根据地 / `R2` → 独占批次 + 先确认 known-good。
- **永远独占**（不可关断类）：`sdkconfig`（PSRAM 频率/栈深/FreeRTOS）、分区表、启动顺序、bootloader。
- **无 known-good（可切槽/可重烧的已验证 marker+ELF）→ 禁止出刀**，先建根据地。

## 四、弹药预算（算力=弹药）

- 回合开始声明预算：读几份文件 / 跑几个脚本 / 是否烧录。超预算先问。
- 验收走**脚本裁决**：`reboot_audit.py`（per-marker 平均寿命）· `stability_evidence_collector.py` · `stability_soak_status.py`。**禁**把原始 `serial_monitor.log` 大段读进上下文。
- 完成失败轮 `n<4`：不宣告精确 MTBF，也不反复分析；但稳定固件不能因“迟迟不失败”被反向阻塞。`V2` 全过且为 `R0/D0~D1` 的单变量，可用限时灰度继续：单 marker 连续 6–12h 零自然复位记阶段正证据，48–72h 累计零自然复位可记强通过；若有失败则回到 per-marker 完成寿命样本比较。用户明确接受风险且 known-good/ELF/回滚齐全时，可提前推进下一项 R0 单变量。
- **滚动门**：同时最多一项未完成 V3 的视觉热路径变量；任一可重复 panic/WDT、音频失效、正常退出或二次唤醒回归，立即退上一 marker，禁止在失败候选上继续叠刀。
- 一批次**同回合**记一次账（写回 `face-emotion-sd.mdc` 账本 + 更新 `p4-current-state.mdc`）。

## 五、避实击虚（一行否证清单）

USB≠冷启 · MQTT≠UDP Opus · 邻接日志≠因果 · 返回后 HWM≠栈安全 · 写错槽≠已上板 · RTS/hard_reset≠进程重启 · 广谱崩≠脸有罪 · `rst:0x7`≠根因（先补 PANIC）· 降频≠降容量 · `experimental` 规格默认嫌疑 · ≥400 行条带≠局部刷新 · 全帧抠嘴≠省 decode。

## 冲突优先级

**可实践（能烧录/能归因）> 禁投降砍目标 > 无证据不开火 > 弹药经济 > 临时灵感。** 改战略先改 rules。
