---
description: P4 当前战役状态 — 唯一常改文件（阶段/known-good/marker/主矛盾方面/下一刀）
alwaysApply: false
---

# P4 当前状态（唯一常改）

> 只放**会过期的**内容：阶段、known-good、当前 marker、主矛盾方面、下一刀、冻结项。
> 长期原则不写这里（写 core / hard-constraints）。每批次**同回合**更新本文件 + 账本。
> **§0 是唯一权威态势**；其下「一、三…」长流水保留作历史，**不得覆盖删除**（并行会话可能在写）；新批次优先改 §0。
> 更新日期：2026-08-20。

## §0 一页态势（权威；出刀前必读）

| 项 | 值 |
|----|----|
| **真源 marker** | `boot_trace_v10_s1fp_smell_lock_names`（行为守恒：LVGL 锁超时命名常量，数值=s1fo；以 `boot_trace.cc` 为准） |
| **阶段标签** | 硅片地板未除（防御）+ **相持打法**（预算化视觉）；非纯防御、非反攻 |
| **KG-Life** | `s1cm`（mean≈2054s / n=14）— 寿命统计锚；滚动正证据不可擅自替换本槽 |
| **KG-Function** | `s1fk`/`s1fm`/`s1fo` — 体验回退优先指这里（s1fp 仅命名抽取） |
| **KG-Archive** | GitHub `s1eo` + `s1db_clean_extract` — 结构/存档回退 |
| **滚动 V3 占用** | **s1fp**（=s1fo 路径 + 锁名抽取）；同时最多一项视觉热路径 |
| **face 能力位** | M2a–c V2✓ · M2d idle life canary 逻辑仍在（s1fo→s1fp 未改语义）；enter/exit/blink **冻结** |
| **可发布核** | 会话闭环 + 分层嘴 + VisualBudget 消费；润色能力位可选、各自 R0 |
| **SD 素材** | 板上 `mjpeg_ai_dialogue_v5p3_mouth_focus`；R1 回退基线=v5 |
| **唯一下一刀** | 冷启验 `s1fp` exact marker；确认口型/idle life/二次唤醒不回退；滚动 V3 继续；M−退 `s1fo`/`s1fm` |
| **韧性欠账（可见）** | R0-triage / R1-guard / R2-sram / R3-warm / R4-bisect — 鼓励与视觉交错 |
| **明确冻结** | 全屏 enter、眨眼、对话期≥400 行 MID、整包混合提交、再调 Frozen 时长、200M→20M |

## 一、你在这（历史表；与 §0 冲突时信 §0）

| 项 | 值 |
|----|----|
| **战争阶段** | 战略**防御地板** + **相持打法**（P4 v1.0 MSPI-751；软件只降触发窗口 + 预算化视觉） |
| **主矛盾方面** | PSRAM 总线突发 ∩ 硅片 errata；保 200M 合比值，禁降 20M，禁业务刀修硅片 |
| **KG-Life** | `s1cm`（`reboot_audit.py` mean≈2054s / min≈324s / n=14）；`elf_snapshots/xiaozhi_s1cm.elf` + `fw_archive/xiaozhi_s1cm.bin` |
| **KG-Archive** | `s1db_clean_extract` / GitHub `s1eo`（行为守恒抽取与存档点） |
| **KG-Function** | `s1fk` / `s1fm`（体验回退点；详见 §0） |
| **当前上板/源码 marker** | **以 §0 真源为准**（`s1fo`）。下记 s1fl 行为描述保留作历史，不再当作「当前」。 |
| **综合进度** | 可发布核约具备；M2a/b/c V2 过；M2d=`s1fo` 占滚动门。百分比口号易漂，以能力位与 §0 为准。 |
| **PC life 素材** | 当前 SD 已换 `mjpeg_ai_dialogue_v5p3_mouth_focus`；v5 仍是 R1 素材回退基线。 |
| **face 阶段** | **M2d idle life canary 进行中**（其上 M2a–c 已 V2）。冻结 enter/exit/眨眼。 |

## 二、face 演进（存活门阶梯，取代旧「视觉门」L1m/L3b）

> **组织原则不是 React 隐喻，而是「每 tick PSRAM 突发预算账」**：每个渲染器声明自己每秒的 PSRAM 写后读字节数（量子＝全宽×50 行≈24KB/条带），阶段对**总突发**设上限，实测卡门。
> **每一级的门都是「per-marker 平均寿命不回退」**（视觉指标次要）；**每个视觉特性挂 R0 flag**（回归=一次重启，不是项目停摆）。

| 级 | 内容 | 门（存活优先） |
|----|------|----------------|
| **M0** | 静态优先·零稳态突发：对话 **StaticHold**；idle 低频微动；全屏底图仅安全窗单例 | mean ≥ KG-Life 不回退 |
| **M1** | **仅 Speaking** 分层嘴；每 tick ≤1 行带；latest-value | 嘴延迟/闭嘴门 + mean 不回退 |
| **M2a** | 五档 VisualBudget **影子** `apply=0` | 分类正确 + 会话不回退 |
| **M2b** | MouthReduced/Only **消费嘴节拍** | V2 体验 + 滚动 V3 |
| **M2c** | MouthOnly 才允许 speak-life；薄队列 defer | 同左 |
| **M2d** | idle life **金丝雀**（稀疏令牌，可丢） | 寿命不回退；失败 R0 关 |
| **M2e/f（冻结）** | enter/exit 小带、眨眼 | 另批；属反攻愿景，不是可发布核 |
| **M3 反攻** | manifest v2 工具链；绞杀对话期 ≥400 行兼容路径 | 需 v3.1 **或** v1.0 已证大余量 |

**诚实结论**：v1.0 上「静态优先」是正确稳态；**可发布核=会话+嘴+预算消费**，不是「M2 大包全点亮」。丝滑润色（enter/blink）随时可被寿命门否决。

## 三、下一临界路径

> **2026-08-20 s1fl M2a影子预算**：用户授权以“先灰度、有问题回退”代替等待自然崩溃凑n=4，但不把灰度记成稳定性已通过。五档分类器只读取`device_state + dq/pq/sq + output_age + playback_hold`，输出状态变化/60样本心跳，`apply=0`且Face Worker完全不消费。S2/D0/R0/V1→V2/V3。V2联合门PASS：generation 1..13、mouth_arm=24、mouth_blit=69、四次`channel_closed→wake_enable`为205–207ms、第二轮`wake_word_detected→Listening`为224ms；idle=`TransitionCandidate`、Listening=`StaticOnly`、Speaking在`MouthReduced/MouthOnly`间随pq切换，零panic/reboot。M2a收口；下一刀可独立进入M2b嘴部节拍消费，长期稳定性仍未宣判。

> **2026-08-19 s1fk 可归因性刀**：H=冷启USB枚举晚于早期`FW_MARKER`，使长泡无法严格归属；不创建新任务/定时器，复用每秒主循环，在uptime≥60s仅输出一次`FW_MARKER ... echo=delayed_once`。S1/D0/R2/V1；M+=延迟exact marker恰好一次且无日志洪泛，M-=60s后仍缺marker、重复输出或任何功能回退。完整构建、归档、双OTA烧录通过；uptime 61.499s exact marker命中且到73s无重复，V1 PASS。当前验收器因本次是RTS热启，SD/seed、多轮TTS、退出/二次唤醒保持PENDING；须物理冷启与交互补门，再进入per-marker长泡。该刀不提高视觉完成度，只补齐M1→M2的寿命裁决基础设施。

> **2026-08-18 manifest-v2契约硬化离线批次**：不改`main/**`、不换SD、不编译烧录。新增`dialogue_pack_contract.py`，按固件真实加载边界检查六表情入口、安全相对路径、480×480/RGB565、MJPEG索引与SHA、嘴/眼/life文件尺寸、canonical base hash、每tick≤48行且单层、pose/transition上限，并把检查接入builder输出`contract_report.json`；4个自动反例覆盖合法包、路径逃逸、ROI越界、patch尺寸错误。旧v5p4硬错误为0，但暴露neutral/sad/angry/loving共8条`auto_unreviewed` life轨。新候选`mjpeg_ai_dialogue_v5p5_contract_hardened`只保留已人工批准的standby/happy track1，其余life轨显式空批准；contract=`errors=0/warnings=0`。相对v5p4，共有234个媒体hash差异0、六个MJPEG差异0，只删除四个未批准表情各36个life RGB565/A8文件。当前只增加体验防错与集成度，不冒充V2真机收益；s1ez寿命门通过前不部署。

> **2026-08-18 s1ez 稳定性刀**：08-17 三起标为 `MQTT_END` 的崩溃实际发生在该breadcrumb之后的idle life；10:58 INT_WDT匹配s1es ELF，CPU0=`AfeFetchGateEnter→xQueueSemaphoreTake→spinlock_acquire`，AFE从life frame2后等待约0.87s，face仍提交frame3..5；当时DMA计数已指向旧帧，坐实small-life路径缺最终DMA fence且audio/face同优先级。s1ez令visual只在门空闲时提交、持门等final-DMA、face prio2<audio prio3、timeout帧丢弃不补播、日志移到解锁后。R0=`S1EZ_P_LIFE_AFE_FENCE=0`；M+=`p=1/prio=2/gate=released`、AFE fetch连续、无fence timeout；M-=life消失、音频欠载或WDT/panic。已完整构建、归档并写入双OTA；BIN SHA256=`77C76D57CF4613987602C98924C35250A390CC4CA8CD0122098803F2F7723C2A`，ELF SHA256=`DBA5AC1499D64514D6739BB447BF0539B9B54F35B2BF38E3D05B1D3285BB0770`。当前由夜间长泡采集；补exact marker后按per-marker n≥4裁决。

> **2026-08-19 s1fa 稳定性候选**：s1ez匹配ELF把22:31 panic解析到FreeRTOS `xTaskIncrementTickOtherCores`，非法地址来自被破坏的TCB优先级/ready-list索引；`mtval`不近`sp`、cache_on且flashop_depth=0，不能归为MQTT业务空指针。现场同时证明idle-life在AFE繁忙时仍每120ms唤醒（约28k次/小时），且panic前约1.1s刚完成一次life blit；第二起纯WDT也落在MQTT连接收尾。s1fa只验证“连接窗口与低频显示重叠是放大器”：提醒MQTT AT事务阻挡idle-life，视觉零等待丢帧并900ms退避，Speaking mouth不进此门。R0=`S1FA_Q_MQTT_IDLE_QUIET=0`；M+=`q=1/retry_ms=900`，MQTT窗口见`q=mqtt_skip`且无life blit、窗口外life恢复、音频/二次唤醒正常；M-=窗口内仍blit、life永久消失、会话回退或任何panic/WDT。完整构建通过并归档；BIN SHA256=`CD22D87B035A93D7489E483EAA4D2A4B192B30F7B9FC898637781923A458109C`，ELF SHA256=`4EEDD0FB85BE3204D7B481B1FEE55FDF94D0780CCC94152F64D4D8C8EE19D333`。已完整写入双OTA及assets，exact marker/冷启门待补；稳定性仍须per-marker `n≥4`且mean不低于s1cm。

> **2026-08-19 s1fb AFE错相候选**：s1fa冷启短窗证明q门在`MQTT_CONN_BEG..END`内正确drop并在END后释放；但`t=66.258s`成功life frame2后，约47s只有`skip_afe n=33→74`且无新blit。AFE fetch约30ms，固定900ms=`30×30ms`，失败重试锁在同一忙相位，根因不是素材或q死锁。s1fb将AFE失败重试独立为`731/887/1019ms`错相序列，平均879ms，不提高当前约4k次/小时预算；MQTT仍900ms，成功帧仍260ms、周期rest仍4.2s。R0=`S1FB_R_IDLE_AFE_DITHER=0`；M+=8–15s内blit继续推进且q窗口仍无blit；M-=frame长期不前进、重试激增、音频/唤醒退化或panic/WDT。增量构建和二次复核通过；BIN SHA256=`490B953132DE62525F0F0068E37E8980ECC5E2D2AA48F800260B21C29B7C4A6C`，ELF SHA256=`FDC1F478741854748210C3ED2DE5665179F9A6722457854DC7C1553048652D17`。已完整写入双OTA及assets，exact marker/冷启门待补；综合进度暂不增加。

> **2026-08-19 s1fb 冷启裁决**：新retry序列与q门均生效，MQTT窗口严格drop并在END后回到AFE准入；但监控段约5分钟只有5次life blit，frame约0→4，92条限频`skip_afe`对应数百次失败尝试。错相只消除固定900ms共振，不能解决wake-word AFE近连续持门；视觉M−，不再继续调单一retry常数。下一刀应是AFE fetch返回后的低频令牌化安全窗：只在idle、音频健康、非MQTT时每若干秒最多授予一个48行帧，超时丢弃；不得恢复120ms竞抢或放开AFE↔display并发。

> **2026-08-18 v5p4离线体验/集成批次**：不改板上s1ez/v5p3。候选`mjpeg_ai_dialogue_v5p4_mouth_focus_soft_seam`将v5p3的standby/happy仅批准track1，与P-seam四个非强情绪medium/large软过渡合并；验证6/6。对v5p3媒体文件数均为378，missing/extra=0，hash差异严格32个嘴层文件，sad/angry、MJPEG、life均零差异。`s1ev_acceptance.py`默认从`boot_trace.cc`读取marker，并在s1ez下检查p=1、face prio2<audio3、life gate released、AFE连续及fence timeout比例。当前日志门=PENDING，仅缺exact marker与实际第二轮wake；其余功能/栅栏短门通过。夜间不换SD、不烧录；s1ez寿命结果通过后才V2部署v5p4。

> **2026-08-18 P-life-semantic 离线候选**：用户确认天气嘴幅可接受但 happy 右侧头发持续重复运动抢注意力。根因是 life 工具以运动分数直接发布，happy auto track0=`x128,y40,w288,h48`覆盖上方头发/饰品。工具新增 profile/CLI 语义批准门；v5p2只批准auto track1（下方身体/衣饰），嘴部、固件和刷新节拍不变。R0=继续使用当前v5；若决定换包，V2只验天气长TTS头发不再重复、仍有低频生命感、嘴部/字幕/音频/第二轮不回退。

> **2026-08-18 s1ew 当前验收**：日志已坐实唯一漏嘴的 `speech_generation=4` 在整个 TTS 段没有收到 `type=llm emotion`，而其他 generation 均可正常 commit/arm/blit；因此不能再把嘴部武装硬依赖云端 emotion。s1ew 在每个新 Speaking generation 无 pending emotion 时先展示 standby seed并 provisional `mouth_arm`，后到emotion再同代重绑。重绑与worker patch读取用同一递归互斥，避免FreePatches/read UAF。R0=`S1EW_M_PROVISIONAL_MOUTH=0`；M+=第一轮/任一无emotion TTS仍见 `provisional_mouth=arm gen=N` 与 `mouth_blit`，后到emotion可重新commit且音频连续；M-=假arm、锁等待影响音频/字幕、接缝回归、panic/WDT。已完整双槽烧录；下一步物理断电冷启验 exact marker、SD mount/seed 6/6，再做多轮TTS。继续用v5 SD包，无需换卡；寿命仍须per-marker n≥4。

> **2026-08-16 s1ej 当前刀**：先烧录并冷启，确认 `life_track_bind track=0/1`；长 TTS 中应看到两轨逐轮交替而非并发。验收第二轮唤醒、字幕、音频连续性与接缝；若复位或体验回退，R0 直接烧回已归档 s1eh，不改会话/AFE。

> **2026-08-16 s1ek 下一验收**：先把 `mjpeg_ai_dialogue_v4_temporal_feather` 整包替换到 SD `/sdcard/dialogue_v2/`，再烧录并物理断电冷启。日志应显示 `compose=canonical`；观察嘴层水平线、life结束残影和双轨衔接，同时复验第二轮唤醒/字幕/音频。任何功能回退直接回烧 s1ej 并恢复v3包。

> **2026-08-16 s1el 下一验收**：天气/happy 长TTS应出现 `s1el life_blit ... overlap_mouth=0`，且同一worker tick不与mouth blit并发；neutral相交轨迹保持闭嘴门。angry不新增life，只观察原有强情绪底图+嘴层是否仍感到资源紧张。若音频或会话回退，直接回烧s1ek。

> **2026-08-16 s1em 当前验收**：先构建并归档匹配ELF，再写bootloader/partition/otadata/双OTA/assets。冷启须见 `FW_MARKER ...s1em...`、`SD_MOUNT_OK`、`seed_stills done ok=6/6`。长TTS中正常态应以 `budget=MouthOK` 为主；只在真实短欠载边沿出现有限 `budget=Frozen→MouthOK`，期间不能停在张嘴末帧。复验字幕、音频连续性、退出及3秒内第二轮唤醒；任何功能回退R0关 `S1EM_J_VISUAL_BUDGET2` 或R1回烧已归档s1el。寿命n<4只记样本，不判稳。

> **2026-08-16 s1em 裁决/下一刀**：s1em 已触发 M−并回烧s1el，用户确认回退版整体体感可接受。下一版禁止继续调 Frozen 时长：先把源码重锚到s1el行为，再将预算改为“节拍器限速 + generation/session重置 + 不补跳”；嘴部空间接缝另由PC素材同generation扩大软过渡区处理。时间问题与空间问题分批验收。

> **2026-08-16 s1en 收口**：源码已重锚s1el并编译归档，不烧录、不改变当前已验收设备。综合进度记60%，今天停止出刀。下一工作日先做PC侧最大嘴形边界差报告/软过渡素材（不改固件），再单独设计非追赶式刷新节拍；两者禁止同批上板。
> **2026-08-16 s1eo 提醒灰度批次**：远端 `agent/com4-proactive-reminder-stability` 是431990增/1308删的混合提交，禁止整包合入。仅吸收：(1) MQTT唤醒忙时合并为一个pending，空闲后约1秒重试；(2)播放路由已ready时`PrepareSpeakerPlayback()`幂等返回。R0=`CONFIG_REMINDER_DEFER_BUSY_WAKE`；M+=`poll_deferred_set -> poll_deferred_begin`、`speaker_prepare_noop`；M-=普通对话状态机/表情/UDP路径变化。TTS预缓冲、远程重复响铃、UDP重排、vendor ML307与EEZ UI暂缓，分别独立marker验证。
> **2026-08-16 s1eo 上板记录**：完整镜像与 assets 已写入并通过 esptool hash 校验；启动捕获到 `FW_MARKER boot_trace_v10_s1eo_reminder_defer_spk_noop`、`PANIC_LAST none`。本次是 USB reset，不能替代 SD 冷启门；下一步物理断电冷启后验 `SD_MOUNT_OK`、`seed_stills done ok=6/6`，再验普通对话、退出及 3 秒内第二轮唤醒，最后验忙时提醒延后闭环。n<4 不判稳定。
> **2026-08-17 s1eo 三轮验收**：基础三轮对话可走通，但暴露三个独立 M−：happy `112x56` 嘴层方形边界；“你休息吧”仅依赖云端 goodbye、延迟不确定；云端 `embarrassed` 被旧别名映射为 angry，语义不合。随后出现首个自然失败样本：WDT + StoreAccessFault，`prev_t=422.272s`、`prev_phase=FACE_PRESENT`、`mepc=0x4fc19f8c`、`ra=CopyRgb565BandRows`、`mtval=0x482edb00`，发生于 idle standby breathe 400行事务完成后，DMA inactive。s1eo n=1 不判稳，但低于s1cm均值，记预警；四问题必须按“素材空间 / 退出语义 / emotion canonical / 韧性”四批分刀。

> **2026-08-17 s1ep 当前刀（用户指定1/2/4同批）**：PC v5以活动岛抑制ROI内颈饰/衣领帧差，多圈层评分替代“只看最外沿1px”的假安全；固件canonical收口到`emotion_video_player_canonicalize_emotion()`，adapter/MJPEG/layered共用；idle若standby life契约可用，9帧一轨、260ms帧间、4.2s轮间、每tick≤48行，失败才回legacy400。R0=`j_canonical/k_standby_life`；M+=`FACE_S1EP canonical embarrassed>neutral`、`idle_life=1`且无idle 400行；M-=口型被过度抑制、SD bind阻塞唤醒、仍出现400行热路径或寿命均值回退。未包含本地“你休息吧”退出。

> **2026-08-17 s1eq 当前刀**：首次唤醒WDT邻接`happy FACE_PRIME`的480x480全屏enter首帧；六个`/mjpeg`与格式化前备份SHA一致，不能归罪素材内容。唯一行为变量=`g_enter 1→0`，StaticHold单次seed与layered嘴/life保留。M+=首次情绪无`FACE_ARC/FACE_PRIME`且出现`static_hold_done→mouth_arm→mouth_blit`；M-=仍在TTS边沿出现全屏动画burst、音频/第二轮回退或复位。短验收M+已见，长泡仍须per-marker n≥4。

1. 不主动打开会触发 USB reset 的旧串口监控。直接验 GitHub exact `s1db_clean_extract`：说“你休息吧”应退出；另测云端自然结束；两者都须在 3 秒内可开启第二轮。
2. 先验字幕：中文必须正常，不得出现白色块；再验首轮唤醒→STT→TTS。
3. 连续完成至少三轮对话；每轮退出后 3 秒内重新唤醒，不能停最后一帧，不能持续 FEED full。
4. 基线门通过后，按顺序灰度合回：MQTT UDP熔断 → 主动提醒speaker ready/ACK → TTS预缓冲；每项独立 marker/判据。动画体验只在基线音频门通过后继续。

## 四、冻结项（未获用户再裁定禁出刀）

`A` cap 400→450/480 · `B` 回退固定 240×300 · `F` MID 18→24/30 · `G` MID 160→120ms · `H` breathe 更密 · `I/P0` 200M→20M（已证方向错）。

## 五、提醒

- 不得因再次复位就关 idle 动态或整个表情系统（投降式假稳定，违 B3/F6）。
- 无有效 layered pack 不开 VisualBudget；不得从 480² 全帧实时抠嘴冒充分层。
- 采集健康先于判稳（`serial_monitor.log` 在长、`pos` 在动，才算在收）。

## 六、两周稳定性审计（2026-08-16，`reboot_audit.py`，`prev_t` 单位 ms→下表已换算秒）

> **与三/四节的「功能基线重锚定」是并行矛盾，分刀处理**（B4）。本节只管硅片稳定性，不碰第二轮唤醒/字幕功能。

**per-marker 寿命（近两周主线，mean 秒 / n）**：s1cm 2054(n14,基线) → s1ct 2816(8) → s1cu 2334(5) → **s1dc 16878(4,min2861)** → **s1dd 39465(10,min606)** → **s1de 29088(4,min3157)** → ⚠**s1di 118(4)** / ⚠**s1dq 63(1)** 明显回归；s1ed 无样本（太新）。

**规模坐实（356 次 REBOOT）**：
- **崩点 66%=`FACE_PRESENT`**（234/356）＝**最大 PSRAM 消费者=暴露面**，非脸有罪。
- **mtval 直方图 ~90% MSPI-751 指纹**：65 合法 PSRAM 读失败 + ~40 nibble 互换/移位（`0x84../0x04../0x8e..`）+ 18 null + 8 `0xF...`。硅片病，软件不可根治。
- **少数可修类**：26 内部 SRAM mtval，含 `mtval≈sp`（如 s1dc `mtval=0x4ff55624 sp=0x4ff55610` Δ20B）＝**栈/ISR 溢出**，与硅片分流单独修。

**结论 → 战略转向韧性/冗余存活**（架构见 `xiaozhi-p4-core.mdc` 原则 **H**、落地约束见 `p4-hard-constraints.mdc` §8）：预防已触硅片地板，s1dc/s1dd/s1de 已把量级推到 4–11 小时；下一量级靠**廉价存活 + 冗余**，不靠再降暴露。

## 七、韧性下一刀（R 系列，独立于功能重锚定；均 R0/独占按注）

| 序 | 刀 | 内容 | 四问速记 |
|----|----|------|---------|
| **R0-triage** | 崩因自动分流 | audit/BootTrace 自动标 `mtval≈sp`(栈,可修) vs nibble-swap(硅片,只降暴露)，先修栈类 | S1/D0/R0/V1 |
| **R1-guard** | PSRAM 读前校验 | 热路径 PSRAM 读出的指针/索引 deref 前范围+对齐校验，越界走安全默认不 deref | S2/D1/R0/V2 |
| **R2-sram** | 关键控制态迁 SRAM | face/情绪/会话/提醒队列的**控制结构**放 `MALLOC_CAP_INTERNAL`，PSRAM 只留像素 | S3/D2/**独占+先 known-good**/V3 |
| **R3-warm** | 暖启动恢复 | 崩溃复位区分 power-on，快贴 last-good seed（RTC/NVS 最小态），把黑屏冷启降为秒级闪回 | S2/D2/**独占**/V2 |
| **R4-bisect** | s1di/s1dq 回归二分 | 定位 reminder_gray_v1 / face_window_local_end 为何从 8h 掉到 ~100s | S2/D0/R0/V3 |

- 先出 **R0-triage + R1-guard + R4-bisect**（R0/低损失，可与功能重锚定并行批次，独立标记）；R2/R3 改内存布局/启动，须独占 + 先建 known-good。

## 八、存档点 + 分支可吸收 backlog（2026-08-16 s1eo）

> GitHub 存档：`Zfh-charles/esp32-p4-i2cpolling`，marker=`boot_trace_v10_s1eo_reminder_defer_spk_noop`。
> 远端混合提交 `agent/com4-proactive-reminder-stability`（+431990/−1308）**禁止整包合入**。下列按坏味道拆成独立可关断项，过门再吸。

### 已吸收（本存档包含）

- route-v2 防御批：worker / emotion3 / fs_gate / StaticHold / full_still / enter_arc
- M1 体验 60%：独立嘴层 + latest-value + 单 tick 单条带 + canonical 预合成 + 双轨 life（每 tick 一轨）+ ROI 仲裁
- s1eo：忙时 MQTT 合并为一个 pending、空闲约 1s 重试；`PrepareSpeakerPlayback()` 已 ready 时幂等返回。R0=`CONFIG_REMINDER_DEFER_BUSY_WAKE`

### 下一工作日优先（空间/时间分批，禁同板上板）

| 序 | 项 | 来源/教训 | 做法 | 禁 |
|----|----|-----------|------|-----|
| **P-seam** | 最大嘴形空间接缝 | s1en 收口 | **只改 PC 素材**：同 generation 扩大软过渡区；出边界差报告。不改固件 | 禁为接缝重编码原始 MJPEG；禁固件再做 alpha 累计 |
| **F-metro** | 节拍器限速 | s1em Frozen 时长 M− | generation/session 重置 + **不补跳**；禁再调 Frozen 时长 | 禁与 P-seam 同批上板 |
| **R4** | s1di/s1dq 寿命回归 | 审计 8h→~100s | 开关二分 reminder_gray / face_window | 禁用砍脸修回归 |

### 灰度可吸（各独立 marker + R0 + 独立 M+/M−）

| 序 | 项 | 说明 |
|----|----|------|
| **G-udp-fuse** | MQTT UDP 熔断 | s1fe已实现输入边界；s1ff叠加prime/3包重排，源码V0通过，待V2/V3 |
| **G-ack** | 提醒 speaker-ready / TTS-ACK | s1ff改为真正播放排空后才进入feedback/ACK，源码V0通过，待主动提醒真机门 |
| **G-tts-prebuf** | TTS 预缓冲 | s1ff仅主动提醒启用4帧/420ms有界预缓冲，普通对话不改；待V2 |
| **G-repeat-ring** | 远程重复响铃 | 混合提交里的，未验证 |
| **G-udp-reorder** | UDP 重排 | 混合提交里的；动媒体路径＝独占 + 先 known-good |

### 明确不吸 / 已证伪

- **整包**合入 `com4-proactive-reminder-stability` 或 vendor ML307 / EEZ UI 大 diff
- 继续加长/缩短 **Frozen 时长**（s1em M−）
- posebank 眨眼作为正式包（已回退；仅 `tools/experimental/`）
- 对话期 ≥400 行 MID、全帧抠嘴、200M→20M、关 AFE/表情保命

### 韧性系列（与体验分刀，见第七节）

`R0-triage` → `R1-guard` → `R4-bisect` 可与灰度并行；`R2-sram` / `R3-warm` 独占。

### 代码味道债（存档后整理，不在本批改热路径）

- `eezui_display_adapter.cc` 仍是上帝类（Present/Tick/字幕/life）；只允许**行为守恒抽取**，禁顺手改时序
- `dialogue_emotion_builder.py` 单文件兼索引/ROI/life/校验：PC 侧可按「扫描 / 生成 / 校验」拆模块
- 根目录 `tools/emotion_roi_builder` 仅剩 `__pycache__`＝死代码，以 `emotion_tool_dev` 为准
- 串口脚本三份（`read-com` / `read_serial` / `serial_boot_capture`）职责重叠，归档后收敛到 `tools/stability/`

## 九、2026-08-17 s1er 当前批次

- 真源 marker：`boot_trace_v10_s1er_netdown_session_teardown`；基于 s1eq，仅修改网络掉线收尾，不改表情、AFE、Opus 编解码或 SD 路径。
- s1eq 现场并非复位：本次冷启后仅有一条 `POWERON boot_count=1`，无后续 `rst/FW_MARKER/PANIC_LAST`；设备持续运行。用户感知的“复位/卡死”实际邻接 `Ml307Board: Network is down`。
- 根因：网络掉线回调只执行 `SetDeviceState(idle)`，绕过统一会话收尾，形成 `state=idle session=None ch=1`，随后连续 400s+ `Protocol: Channel timeout`，字幕停在天气末句且无法继续对话。
- 修复：`HandleNetworkDown()` 投递到主事件循环并调用统一 `EndSessionAndRestoreIdle()`；idle 防抖仅在音频通道已关闭时允许跳过，禁止残留 channel 被 debounce 掩盖。
- M+：掉线后日志出现 `NET_DOWN_RECOVERY begin ... ch=1`，随后 `network_down_recovery ... ch=0 wake=1`，字幕回唤醒提示；网络恢复后可重新唤醒。M-：继续出现 `state=idle ch=1` 或 120s 后 `Channel timeout`。
- 构建已通过，BIN SHA256=`d0b76c1f0772e7b5d2ee7e52738582ed9542e4ea3dbb43d67ae513934c05ba33`，ELF SHA256=`685dfdd617767ac429192049d9f958b139da47c139449e7170576834d5f7f2c8`；已归档。bootloader/partition/otadata/OTA_0/OTA_1/assets 已写入且逐段 hash verified。日志采集恢复时设备 uptime 已约25s，错过启动 marker，但见 `seed_stills 6/6`、pack 与双 life 轨 hash 正常；仍需下一次物理冷启补 exact marker，且网络掉线恢复 M+ 尚未真机触发。

## 十、2026-08-17 s1es 当前批次

- 现场：同一 session 连续天气查询均收到云端 `llm emotion=happy`；首段有 `commit→static_hold_done→mouth_arm→mouth/life blit`，后续只见 `requested→pending committed=happy` 后返回。退出并新建 session 后首段又恢复。
- 根因：Presenter 仅按情绪字符串去重，未把新的 Listening→Speaking/TTS 段视为新渲染 generation；而每段 listening/speaking 边界已恢复 standby bookend，导致 committed 仍为 happy、画布/嘴层却已回 standby/未武装。
- 修复：每个新的 Speaking 边沿递增 `speech_generation`；pending/committed 同时带 generation。同情绪同代仍去重，同情绪跨代重新走一次 StaticHold 与 layered mouth/life。保留 `g_enter=0`，不恢复全屏 enter arc、不补播旧帧。
- M+：连续三次天气依次出现 `speech_generation=N`、`commit emo=happy gen=N`、`mouth_arm` 与 blit；同代重复消息只见 `dedupe_same_generation`。M-：第二/三次仍只有 pending，或引入重复全屏 enter、音频/唤醒回退。
- 构建通过并归档：BIN SHA256=`2fb0fa3bdba2727393649ee73367c13ccf67ea957ba9ac8bfb12408954134991`，ELF SHA256=`e43dc7283212c48b1e0b3a626e41f30a5d7479c4370879c248ece06d6a5fe56b`。2026-08-17 已完整烧录 bootloader/partition/otadata/双 OTA/assets，逐段 hash verified；日志捕获 exact marker、`SD_MOUNT_OK`、`seed_stills 6/6`、pack 与双 life 轨 hash 正常。待同一 session 连续三次天气与第二轮唤醒裁决 M+。

## 十一、2026-08-17 s1et 强情绪 release hub

- 素材证据：相对 standby，angry/sad hold 的平均像素差为26.07/23.32；各自原生 exit 末段可降至14.20/13.77，收益约46%/41%。neutral/happy/loving收益较低，本批不扩。
- 唯一行为变量：Speaking→Listening/Idle 时，若当前绑定包存在匹配 canonical hash 的 `release_layer`，串行提交5个预合成小带后回 standby；90ms/帧、每tick最多1带、每带≤48行。预算/锁失败或旧包无契约立即走原 s1es bookend，不播放全屏 exit。
- PC包：`mjpeg_ai_dialogue_v6_release_hub` 以已验收 v5 的六个 `frames.mjpeg` 为真源重建；六个 MJPEG 与 hold_base hash 均逐一一致，验证6/6通过。误用当前裸素材生成的候选已标记 `REJECTED_raw_mismatch`，禁止部署。
- R0=`S1ET_L_RELEASE_HUB`/`FaceRouteV2_SetReleaseHub(false)`；M+=angry/sad 结束出现 `release_arm→release_blit 0..4→standby` 且主观跳变减弱；M-=字幕/聆听延后明显、两带并发、音频/第二轮唤醒回退或复位。BIN SHA256=`88e3bf6df9b35a87ce89a177b90fa724b0b9853b0953d41eef2a0da2a438f703`，ELF SHA256=`dd8bf7c4a5cf6d50ef94865ffc2db94815e1d44a7fc662636ee5de420f3c94c6`；归档与构建哈希一致，完整烧录逐段verified。待换SD、物理冷启和真机裁决。
# 2026-08-17 s1eu 待上板验收

- 当前源码 marker=`boot_trace_v10_s1eu_standby_common_hub`；已增量编译通过并归档 `fw_archive/xiaozhi_s1eu.bin` / `elf_snapshots/xiaozhi_s1eu.elf`。BIN SHA256=`9AD03A7C5E74FE992B625CAFFE1DF2A3B8A922FE34DFED2D43AE524D745919DA`，ELF SHA256=`2572097EBE4CE7D1F0350D93E621DE75B49B0E284541DC5B0597BD6CDF272E0D`。
- 配套 SD 包=`mjpeg_ai_dialogue_v7_common_hub`（validation 6/6）；必须与 s1eu 一起换上，否则 `enter_layer` 缺失会走兼容旧路径，不能验双向共底轨。
- 下一次验收只看 angry/sad：入口是否约0.6s渐入且无原生全帧猛跳；退出日志必须 `exact_standby=1` 且没有随后 `close_seed`；复验播报、Listening、退出、3秒内第二轮唤醒。尚未烧录/冷启，不得写已上板或已解决。
# 2026-08-17 s1ev 当前回退批次

- 当前源码 marker=`boot_trace_v10_s1ev_s1es_visual_reanchor`，默认 `l_release=0`：s1et/s1eu enter/release 轨不参与运行；行为回到 s1es 的 StaticHold + v5 layered mouth/life + TTS turn generation。
- 回退配套包=`C:\bake\mjpeg_ai_dialogue_v5_activity_feather`，validation 6/6、460文件；v6/v7 禁止继续放在 SD `/dialogue_v2/` 验收。
- 已编译归档 `fw_archive/xiaozhi_s1ev.bin` / `elf_snapshots/xiaozhi_s1ev.elf`，并于 2026-08-17 完整写入双 OTA；应用双槽独立 verify 均通过。下一验收：物理冷启 exact marker、`l_release=0`、v5 bind；连续两次天气都应有 `mouth_arm/blit`；angry 回原 s1es 瞬切行为但不得花糊/花屏；退出和3秒内第二轮唤醒不回归。

## 十二、2026-08-17 稳定性统计口径校正

- `serial_monitor.log` 与主采集器持续活动；停滞的摘要监控已恢复，`stability_status.txt` 会继续更新。
- 根目录与 `tools/stability/` 两份 `reboot_audit.py` 的自然失败集合补入 `INT_WDT`，避免漏算中断看门狗。
- 校正后 s1es 为 `n=2 / mean=5933.2s / min=1890.5s / max=9975.9s`，高于 s1cm mean≈2054s，但未达到 `n≥4`，**不得宣告寿命门通过**。
- 当前 s1et 只有右删失存活样本，且板上与SD release契约不匹配；下一刀不是新增动画，而是烧录 s1ev、恢复 v5、冷启核对 exact marker 后完成四场景验收。

## 十三、2026-08-17 FACE_SD 修复完成

- `0x80070570` 根因不是 v5 源包：源包 460 文件全量可读；原 `FACE_SD (D:)` 的 FAT32 目录项与簇链损坏，CHKDSK 命中损坏目录并发现约 93.7MB 丢失链，修复后首次续传又立刻复发。
- 对已确认的 2GB `FACE_SD` 执行完整 FAT32 格式化，并从本地完整包 `mjpeg_ai_sd_ready_v5` 恢复 `/mjpeg` + `/dialogue_v2`。
- 恢复结果：466/466 文件、逐文件 SHA-256 `Mismatch=0`；写后 CHKDSK `no problems`。已装回设备冷启，日志见 `SD_MOUNT_OK`、`seed_stills done ok=6/6`，并完成三代 TTS 嘴部更新；但启动 marker 漏采，不能确认 s1ev 上板。

## 十四、2026-08-17 s1ev 联合验收器

- 新增 `tools/stability/s1ev_acceptance.py`，只读取日志尾部，不全扫 1.25GB；联合裁决 marker、SD、seed、跨代嘴部、退出、第二轮唤醒与 panic/reboot。
- 验收器已修正跨监控段借证问题：最新 `MONITOR START` 晚于旧 `SD_MOUNT_OK` 时，不再把烧录前 SD 证据拼入烧录后会话。
- 当前输出 `PENDING(marker_exact)`；同一烧录后日志段已通过 SD、seed 6/6、generation 1..11、repeat mouth、normal exit、second wake 209ms≤3s、no reboot。
- 验收器曾错误用“退出 idle→下一 listening”计算响应，把用户等待多久再说唤醒词混入设备延迟；现改为第二轮 `wake_word_detected→STATE listening`，日志实测209ms。
- 下一动作只有一个：保持采集开启，物理断电冷启补 exact marker。通过前不新增表情热路径。

# 2026-08-18 s1ex 当前候选 — 嘴部 medium 轻微增益

- 板上仍是已完整烧录的 s1ew + 当前 v5 SD 包；用户短体验确认天气嘴部可接受但幅度略小。P-life-semantic 候选尚未部署，头发语义门不得与本刀混判。
- 当前源码 marker=`boot_trace_v10_s1ex_mouth_gain_soft`。唯一行为变量：`medium` 门槛 `1100→1000`（约9%）；`small=400`、`large=2200`、release迟滞、80ms节拍、ROI、素材和总线提交量全部不变。
- R0=`S1EX_N_MOUTH_GAIN_SOFT=0`；M+=中等嘴形略更常出现、幅度主观只小幅增加；M-=重新出现过密跳动、方块/接缝更醒目或音频/会话退化。功能/稳定性仍按 s1ew 原门验收。
- 构建与归档完成：应用大小`0x45db80`，分区余量13%；BIN SHA256=`44EBD08F5EA817FE720A74F2F30857445DBB9D12F68A2914700D58E0FF5246E4`，ELF SHA256=`9364F02D563E8E31F73740D7A4126B27C4D0CA0040D900B28B5F7FFB9CDF92FB`；归档为`fw_archive/xiaozhi_s1ex.bin`、`elf_snapshots/xiaozhi_s1ex.elf`。
- 2026-08-18 已对COM7以460800 baud单次完整写入bootloader、partition、otadata、OTA_0、OTA_1和assets，六段均`Hash of data verified`，无timeout、无重试，RTS hard reset完成。下一步物理断电冷启捕获exact s1ex marker、SD/seed门并做V2嘴幅观感；不需换SD。

# 2026-08-18 s1ey 当前候选 — 嘴部视觉权重重平衡

- s1ex 真机反馈：嘴部仍偏弱，头发动画感强于嘴部。mean门槛只改变档位命中率，不改变嘴形素材几何；日志与manifest共同确认 standby/happy 的上半部 `life track0` 位于头发区域并连续播放9帧。
- 固件唯一变量 O：保留s1ex `medium=1000`，将 `large` 门槛 `2200→2000`（约9%）；ROI、80ms节拍、pose素材和每tick带数不变。R0=`S1EY_O_MOUTH_LARGE_GAIN_SOFT=0`。
- 配套素材候选=`mjpeg_ai_dialogue_v5p3_mouth_focus`：仅批准 standby/happy 的下半部 `track1`，拒绝头发 `track0`；validation 6/6。相对v5，282个共有媒体hash差异0，唯一媒体差异是两条track0的36个RGB565/A8文件缺失，原MJPEG、嘴层及其他表情不变。
- 当前源码 marker=`boot_trace_v10_s1ey_mouth_large_semantic_life`，已编译归档。应用`0x45dbe0`、余量13%；BIN SHA256=`8A6549595F0B627EFCD7D760072D268DE4FBE3BB2C95C356361C4736F7E9E0E7`，ELF SHA256=`AF824FA8D36780C78DDC5844E2F69718480421E9FFC5A6474845E9EE71631EDA`。
- 2026-08-18 已单次完整写入COM7：bootloader、partition、otadata、双OTA、assets六段均`Hash of data verified`，无timeout、无重试，RTS hard reset完成。候选SD包尚未确认替换；固件嘴部O刀已写入，但头发语义刀只有换包后才生效。
- 下一验收必须确认素材已部署并物理冷启。M+=嘴部视觉权重略增、头发不再循环、下方仍有低频生命感；M-=large过密/接缝更醒目、整体生命感消失、音频/会话回归或复位。失败时O可R0关闭，素材R1换回v5。

# 2026-08-19 s1fe 当前候选 — MQTT/UDP输入防御

- 唯一行为边界：合法16字节nonce与合法Opus UDP包保持原发送/接收时序；只拒绝空`AudioStreamPacket`、超过16位长度的payload、畸形server hello、错误key/nonce、短包/长度不一致包及UDP创建/连接失败。字段改用`memcpy`读写，去掉未对齐指针强转和`sizeof(std::string)`误判。
- 明确未吸收远端混合提交中的UDP零载荷prime、3包重排、TTS预缓冲、远程重复响铃、vendor ML307和EEZ UI；不改变普通对话、主动提醒ACK、LVGL、AFE、SD或PSRAM预算。
- 四问：`S1/D0/R2/V0→V2`。M+=正常MQTT hello后UDP建立、上行STT与下行TTS连续，异常输入只打印明确guard日志且不崩；M-=合法服务端包被`payload size`拒绝、UDP通道无法打开、无STT/TTS或二次唤醒回退。
- V0现状：独立`build_s1fe`配置生成成功，`mqtt_protocol.cc`与`boot_trace.cc`均由P4交叉编译器单独编译通过。完整固件链接因构建环境把历史目录判成全量2259目标重建而主动停止；尚无新BIN/ELF、未归档、未烧录，禁止声称可上板或已验证。

# 2026-08-19 s1ff 当前候选 — 分支高性价比防御整合

- 来源为已核过的 `agent/com4-proactive-reminder-stability` 混合分支，只移植可独立解释的机制，不整包覆盖当前已验收动画/会话主线。
- 已实现：assets分区头/表/文件offset边界校验与按实际长度mmap；BQ27220后台缓存快照和LVGL电量图标；MJPEG文件经16KB internal staging分段搬到PSRAM且解码任务栈12KB；主动提醒TTS以4帧或420ms有界预缓冲启动，队列真正排空才反馈/ACK；UDP连接后发送transport-only prime，接收侧3包小窗重排并在TTS stop/通道关闭时flush；保留s1fe畸形输入拒绝。
- 明确未合：vendor ML307 UART fork/12秒冷启/强制重启、managed EEZ UI整包、FATFS LFN heap迁移、硬编码endpoint、远程重复响铃。原因分别是缺失当前板级故障证据、违反第三方边界/单变量门、属于产品语义而非防御修复。
- 四问：`S2/D2/R0~R2/V0→V2/V3`。M+=冷启显示正确电量且不阻塞；SD seed 6/6；普通对话/STT/TTS/二次唤醒不退化；主动提醒首包平稳且ACK晚于真实播放排空；UDP reorder只在乱序时生效。M-=启动误关机、电量UI遮挡字幕/脸、SD加载显著变慢或失败、TTS首音过慢/尾音截断、UDP无音或会话卡死、任何panic/WDT。
- 完整构建通过：应用大小`0x461220`，分区余量12%；BIN SHA256=`BFEE4CAD65349EA68ABFEABC96562C1F177A444870EE0885D9197254DA2421C0`，ELF SHA256=`BEA5D7CF4DD0A927D0A1255EA96C581E0876CE56113DD37D7A36F2F1180BF4CF`。2026-08-19已完整写入双OTA及assets，六段hash verified；当前到V1烧录事实，尚未物理冷启/V2。
- 下一步不得继续加功能：先完整烧录并物理冷启，捕获exact marker、`SD_MOUNT_OK`、seed 6/6、电量快照；再做普通三轮对话/3秒二次唤醒、一次主动提醒和夜间per-marker n≥4。任一媒体/提醒M−先关对应机制，不以整包回退掩盖归因。

# 2026-08-20 s1fm — M2b 嘴部预算消费（V0）

- 当前真源 marker=`boot_trace_v10_s1fm_m2b_visual_budget_mouth`。唯一行为变量：Face Worker锁外读取应用发布的五档预算；`MouthOnly`保持原80ms节拍，`MouthReduced`首个上升步立即执行、后续上升隔tick，latest-value覆盖旧目标且不补播；其他档不开启新口型。闭嘴/回落始终优先，预算推迟嘴部时同tick不提交life带。
- Speaking初沿按`MouthReduced/provisional`处理，避免音频队列尚未填充时首嘴被误冻约1秒。没有新增任务/队列/锁/PSRAM缓存/全屏事务，不改嘴幅、情绪、素材、enter/exit或眨眼。R0=`S1FM_V_VISUAL_BUDGET_MOUTH_APPLY=0`；s1fl为功能回退点，s1fk为较长寿命根据地。
- 四问：`S1/D1/R0/V0→V2→V3`。M+=健康队列保持原口型，偏薄时只降上升频率、不补旧口型，首嘴、收口、退出和二次唤醒不回退；M-=首嘴冻结、闭嘴延迟、音频断续、补播、会话回归或panic/WDT。
- 完整构建通过：应用`0x461e40`、5MB分区余`0x9e1c0`（12%）。归档BIN SHA256=`E7045ABFFF91448EAA14532E390ADEB76D802773F331D4CCE0699365C1A753E4`，ELF SHA256=`006382AE57268535DE7607767D03D57EA2E70F893B469830FDB6EF7961117A36`。
- 烧录/V1：精确停止COM7采集父子PID 6404/16784，仅写otadata/OTA_0/OTA_1，三段均hash verified并RTS复位；新采集PID 18284。uptime 61.498s捕获exact marker，64.498s见`TransitionCandidate ... apply=1 s1fm`，AFE连续、内存稳定、零panic/WDT。联合门为PENDING：marker与no-reboot已PASS，等待长TTS口型、正常退出和二次唤醒；体验与寿命均不宣告过门，M2 enter/exit和眨眼继续冻结。
- V2裁决：用户体感“还可以”，`face_integration_gate=PASS/s1ev_gate=PASS`。同一marker段覆盖generation 1..9、mouth_arm=16、mouth_blit=47；`MouthReduced/MouthOnly`均被Face Worker实际消费，provisional首嘴正常。4次`channel_closed→wake_enable`为209/207/207/203ms，第二轮`wake_word_detected→Listening`为225ms，正常退出且零panic/reboot。M2b功能/体验/集成V2通过；V3寿命继续夜泡，enter/exit与眨眼不得借此解冻。
- 2026-08-20 用户裁决调整升阶成本：`n≥4 + mean≥s1cm`保留为有失败样本时的最终统计口径，不再硬性等待自然失败；V2全过、known-good/ELF/R0齐全的D0~D1视觉刀允许滚动灰度，同时最多一项未完成V3。6–12h零复位记阶段正证据，48–72h累计零复位可强通过；任何可重复panic/WDT、音频/退出/二次唤醒M−立即退上一marker。

# 2026-08-20 s1fn — M2c life 余量门（V0）

- 唯一行为变量：Speaking仍保留已验收嘴层；`MouthOnly`（下行深度≥3）允许当前低频life小带，`MouthReduced`丢弃未完成life轨并只保最新嘴形，恢复余量后从轨起点继续，禁止补旧life帧。idle继续使用PSRAM-free背光呼吸；不改素材、嘴幅、情绪、enter/exit或眨眼。
- R0=`S1FN_W_VISUAL_BUDGET_LIFE_APPLY=0`，失败整批退s1fm。M+=健康队列观感接近s1fm，薄队列出现`life_budget defer`且嘴/音频连续，恢复见`resume latest_only=1`；M-=正常天气长期无life、嘴部冻结、音频/退出/二次唤醒回归或panic/WDT。四问=`S1/D1/R0/V0→V2→滚动V3`。
- 完整构建通过：应用`0x461fb0`、分区余12%。归档BIN SHA256=`3C1410DE9217AC49497CE08EC6D5987AD84EF382383F66CF44E597BF3E3371E8`，ELF SHA256=`41E57DFFEB3EC99F9F26269D7A48480B2919794D2641EA7EAE2ED3B527C44AA4`。尚未烧录/V2。
- 烧录/V1：精确停止COM7采集父子PID 18284/13704，仅写otadata/OTA_0/OTA_1，三段hash verified并RTS复位；新采集PID 19212。61.646s捕获exact marker，70.646s见`TransitionCandidate apply=1 s1fn`，AFE/heap健康、零panic/WDT。热启PSRAM仍约32.5MB且本段无SD/seed边界，联合门诚实PENDING；必须物理断电冷启后再验M2c，不改SD包。
- V2裁决：用户体感“还可以”，联合门全PASS。冷启SD/seed 6/6，generation 1..9、mouth_arm=16、mouth_blit=43；薄队列多次`defer drop_partial=1`，回到`MouthOnly`时`resume latest_only=1`，嘴与life按预算分流成立。4次退出重武装205/203/205/204ms，第二轮wake→Listening 225ms，零panic/reboot。少量`resume level=MouthReduced`是source变为不可用时清除deferred的日志措辞假阳性，没有life提交；不为探针文字重烧，后续工具只把`resume level=MouthOnly`计为恢复证据。M2c功能/体验/集成V2通过，进入滚动V3，不叠下一视觉热刀。
