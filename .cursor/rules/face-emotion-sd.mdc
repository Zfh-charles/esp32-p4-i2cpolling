---
description: 表情战役战术手册 + 踩坑战史（服从总战略指挥）
globs: main/boards/ep-chat-p4-ml307/**/*.{c,cc,h},main/application.cc,main/protocols/mqtt_protocol.cc,main/audio/**/*.{c,cc,h}
alwaysApply: false
---

## 2026-08-17 s1ep 烧录补记

- canonical 审计发现恢复/Presenter 可绕过 `SetEmotion` 直达播放叶子；已在 `PlayMjpegEmotion()` 增加幂等 canonicalize，正常输入行为不变。
- 构建与归档：BIN SHA256=`f68bc5e8b2aefc39da1bfd4b388f436e084efa1969e48fabf23bd1f301eec3bd`；ELF SHA256=`8bdd48149ad46b0d5bee0cc0baf679425820dccd62218aba01a78907051f04d4`。
- otadata、OTA_0、OTA_1 均写入并通过 esptool hash；启动已见 exact s1ep marker 与 `PANIC_LAST none`。USB reset 后出现 `SD_POWER_CYCLE_REQUIRED / ESP_ERR_TIMEOUT`，故必须物理断电冷启后再验素材、idle life 与三轮业务；当前不记真机通过。

# 表情、SD 与语音通道（战术手册）

> **服从** `xiaozhi-p4-core.mdc` 总战略指挥。  
> 本文件 = 战役细则 + **战史账本**（特殊规律）+ 归因详解。  
> 阶段：防御(L0)→相持(L1/L1s)→反攻(L2–L4)；开火纪律见原则 **A–F**。

## 目标形态：时间片表情（Time Slice）

每个 LLM/STT 情绪对应一次**有限时间片**，不是 SD MJPEG 全屏循环重刷。

```
[OPEN] standby 书签帧  →  [MID] 该情绪增量变化  →  [CLOSE] standby 书签帧
```

### standby 的两个身份（**术语必须分开，混用会推出错结论**）

| 身份 | 含义 | 板上证据 |
|------|------|----------|
| **契约帧**（bookmark frame） | **SD 素材设计**：每个表情 clip 的**首帧与末帧是同一张 standby 静止帧**。所以时间片 OPEN/CLOSE 用 standby 帧是**素材自带的**，不是我们额外加的静止约束 | `emotion_video_player.c` 注释：`frame0 is often near-white/idle pose (all emos looked identical)` —— 正因 6 个表情 frame0 全同，seed 才改取 `fc/3` 中段帧；seed 指纹 hash 各异（standby `15d24223`/happy `c1933d98`/sad `42c9dc4e`/angry `cc3371a0`/loving `cdc11ee8`/neutral `d3ecd7a4`）= 取的是中段而非 frame0 |
| **表情块**（clip） | `standby.mjpeg` **78 帧本身是动画**（呼吸/微动），不是静止图 | breathe `decode_at idx=0/13/26/39/52/65` 每帧上屏均 `FACE_ROWS dirty=0..479` → 帧间确实在变 |

**禁止的推论**：「对话 OPEN/CLOSE 是静帧」⇒「standby 表情是静止的」。前者是**契约帧**，后者是**表情块**，两回事。 
**可用的推论（已作废）**：曾推「A→B 可插 standby 内部帧做桥」——**s1ce-gate 板上证伪**（`contract=0`，各表情 f0≠last≠standby.f0）。C 原设计冻结。  
**仍成立**：OPEN/CLOSE 书签用 **seed(fc/3) 开眼静帧**；standby **表情块**内部有动画（breathe 已验）。

| 段 | 要求 |
|----|------|
| OPEN / CLOSE | 时间片**第一帧与最后一帧**同为 standby **契约帧**（素材首尾帧），且与**待机 rest 姿**一致，进出不僵 |
| MID | 中间帧只表达该情绪内涵变化；参考 SD 时长/帧节奏**靠拢不强求**；整体丝滑连贯 |
| 待机 | idle 也要有生命感：standby **ROI 微动（breathe）**，周期末回到同一 seed 书签；禁全屏 MJPEG 循环 |
| 刷新 | **增量（固定中心 ROI / 后续可硬件 ROI）**；禁对话期全屏 short_anim burst |
| 驱动 | `type=llm` 优先；无 llm 时 STT hint；发什么情绪就刷什么 MID |
| 音频共存 | TTS/Opus 期间预算与间隔必须 TTS-safe；目标：音画自然；若见 `rst:0x7` 必须补抓 panic 证据后再归因 |

### 有限逼近思想（泰勒类比 → 工程策略，非像素上算级数）

数学上泰勒展开用**有限阶局部多项式**逼近任意光滑曲线，余项可控、展开点明确。  
落到本项目：**不要用 SD 全长曲线（近百帧全屏）当唯一真值**；在 standby 书签邻域用**有限次 ROI Commit** 逼近情绪弧。

| 类比 | 表情工程含义 |
|------|----------------|
| 展开点 \(x_0\) | OPEN/CLOSE = standby seed（rest 姿）；MID 可从 `fc/3` 邻域起步（TTS-safe 时） |
| 0 阶 | 仅 seed 静帧（L0） |
| 1～N 阶 | 有限 MID ROI 帧（快攻/慢持）；N = 预算（现 speak≈12，可升但须多表情过门） |
| 余项 / 截断误差 | 观感「不够满」可接受；**禁止**用无上限 loop 凑「任意逼近」→ 已证实 WDT |
| 局部有效 | ROI 只改脸心区域；全幅重刷 = 换坐标系硬算整段曲线，贵且脆 |
| 升阶条件 | 稳门过了再加帧/开 breathe；同泰勒：先保证邻域收敛，再提高阶数 |

**可借鉴的推进原则**

1. **先定展开点再升阶**：书签一致（首末=standby）优先于中间多花样  
2. **阶数=预算**：自然流畅靠「对的增量序列 + 对的间隔」，不靠逼近满 SD 帧数  
3. **TTS 邻域收缩阶数**：说话期靠**拉大间隔**收缩（s1am 280/450 + 700ms settle），**不靠砍 stride 或帧数**（s1av/s1aj 那套已被 s1ba 证伪并于 s1bc 撤回）  
4. **idle breathe = 在 \(x_0\) 邻域的微扰**：小 N、慢间隔、周期回 seed；s1bc 已解禁，守卫仍是 `seed_stills_ready`（s1at）+ 对话即停  
5. **禁止字面实现**：不对 RGB 做多项式拟合/逐像素泰勒；借鉴的是**截断逼近 + 误差预算**，载体仍是 seed + ROI Commit  

### 节奏参考（模拟 SD，不拷贝负载）

- SD 卡 6 表情（standby/happy/sad/angry/loving/neutral）的**帧数、文件大小、约 30fps 时长、seed=帧数/3**
  是新 LVGL 时间片的**设计参考**，用来估 MID 快攻/慢持的观感量级
- **向其靠拢，但不强求**一一对齐帧数或整段时长；以 TTS 共存、观感自然、且崩溃可归因为优先
- 产品路径：**少量 ROI 帧 + 快攻/慢持**；MID 结束后 CLOSE→standby；**禁止**说话期无上限 EXTEND / loop_continue（已证实 WDT）
- 丢掉：对话期 MJPEG 全屏 flush、panel DirectPanelBlit 抢主路径、speaking inject
- 例：angry SD≈104 帧/~3.5s@30fps → LVGL 用有限 ROI 覆盖开场，不必播满；加长须多表情连测过门
- **SD 实测帧数（节奏参考）**：standby78 / happy90 / sad91 / angry104 / loving74 / neutral107；seed=`fc/3`
- **现状**：MID/breathe 均 `decode_at`；speak 默认 260ms 起步；行带统一 400（breathe 特供 200 已于 s1cc 撤回）；TTS/AFE 争用留余量；冻结抬 cap/降 PSRAM；禁 compose/砍脸。**route-v2 后**：以上均属**兼容路径**参数，对话主线转 L1m
- **三维分刀（第一性）**：时间（丝滑/fps）≠ 空间（接缝/ROI）≠ 成本（decode+present）；LVGL 刷屏粒度≈**全宽×50 行**（`buffer_size=width*50`，`FACE_FLUSH area_px` 峰值 24000），横向缩框基本不省钱 → 接缝用**行带**（s1bp→s1bs 行差→s1bv 锚定→s1bw 高度）而非居中方块/逐帧 bbox；**弧跨度用索引 seek（s1by），禁 `decode_next_n(step)` 连解**
- **实测余量（s1bn 场）**：mid `decode≈3.4ms`/`present≈13ms`；`flush_ms≈0`；speak 旧间隔 280/450→~2–3fps 才是幻灯片感主因
- **PSRAM 稳态（s1bs 场实践）**：seed 后 ≈11.4MB；多轮对话钉在 **11.32MB（delta=0）** → 对话无 PSRAM 泄漏；勿把「崩」归因成表情吃光 PSRAM
- **烧录坑（s1bo-ota）**：双 OTA 时 `idf.py flash` 只写 ota_0；板可从 ota_1 起 → 须双槽写或确认 otadata；**RTS/hard_reset ≠ 进程重启**；验收须冷启见 `FW_MARKER`

### 素材分类与渲染契约（route-v2，2026-08 路线修订）

**触发**：新素材 `mjpeg_ai` 是**全身/全画幅生成动画**（背景、镜头、人物整体都在变）。此前「PC 自动裁统一 ROI → 固件播小 ROI」的前提是**背景稳定 + 人物局部运动**，在该类素材上不成立，硬套只会得到「全高 dirty → 行带封顶 → 上下拼接」。

**A 类：全画幅动作动画（`full_frame_clip`）**

| 项 | 约定 |
|----|------|
| 用途 | 开机、闹钟、非对话庆祝、idle 偶发动作、情绪 enter/exit 的**一次性**短动作 |
| 禁止 | 在 Listening（AFE 高负载）持续播；在 TTS 期间循环整段；用 400 行顶/底交替「近似」全屏 |
| 并发 | **同一时刻最多一个全屏视觉事务**（D8）；须避开 AFE fetch 与 TTS 起始边沿 |
| 丢帧 | 允许直接丢，不排队、不补播（D9） |

**B 类：对话实时层（`layered`）**

静态情绪底图 + 嘴部 3～4 档 + 眼睛 open/half/closed（+ 可选脸红/泪等小区域）。这是**音量驱动嘴部唯一合法的资源来源**——**不是**从完整 AI 视频里实时找嘴。

> **成本硬事实（决定 B 类可行性）**  
> 1. flush 量子＝**全宽×50 行**（`area_px` 峰值 24000）→ 嘴部 ROI 的**成本下限**是 1～2 条行带，不是 `w×h`；横向缩框不省钱。  
> 2. **嘴/眼必须是独立小图资源**。若仍解 480×480 全帧再抠小块，只降 present/canvas，**`decode_ms` 一分不省**（对照 s1by：降的是解码次数，不是解码尺寸）。  
> 3. 底图切换＝一次全屏事务（~450KB），**低频且须走安全窗**；不得每轮对话反复整屏重铺。

**每 tick PSRAM 突发预算账（组织原则；取代 React 隐喻当第一性）**

在 v1.0 硅片（MSPI-751）上，可行性由**单位时间的 PSRAM 写后读突发**决定，不由动画丰富度决定。故：

1. 每个渲染器**声明**自己每秒的 PSRAM 突发字节数（成本量子＝全宽×50 行 ≈ 24KB/条带；底图事务 ≈ 450KB）。
2. 阶段对**总突发**设上限；升阶前先问「这条突发是否落在 AFE fetch / TTS 起始 / idle WakeNet 的高危窗口」。
3. **每一级门＝per-marker 平均寿命不回退**（视觉指标次要）；**每个视觉特性挂 R0 flag**，回归即秒级关回退，不是项目停摆。

→ 落地阶梯 **M0–M3** 与当前阶段见 `p4-current-state.mdc`（M0=静态优先零稳态突发；M1=仅 speak 嘴部、每 tick 硬上限 1 行带、2 态预算；M2 才上 5 档 VisualBudget；M3 才上 manifest v2）。下节 L0–L4 是**实现手法**，其**过门判据统一改为存活门**。

**状态机（素材无关，统一层）**

```
Standby → ListeningStill → EmotionEntering → SpeakingHold
        → EmotionHolding → EmotionExiting → Standby
```

- **Listening**：neutral/listening 静态底图；嘴闭合；只允许极小状态指示；**禁完整 MJPEG**（D3/D4 不变）。
- **Speaking**：保持情绪底图；嘴部随 PCM 包络；可偶发眨眼；**不循环全屏情绪视频**；播放队列变浅立即冻结嘴与眨眼。
- **TTS 结束**：等真实 PCM/I2S 播空 → 闭嘴 → 情绪保持 300～600ms → 预算允许才播一次 exit，否则静态回 standby。

### 对话低频生命层（Life Layer，M2 探针）

目标不是复制待机全屏 MJPEG，而是在静态情绪底图与高频嘴层之间增加“整体仍在呼吸”的低频数据流：

```
emotion hold base
  + life pose（头发/头部/肩部微漂，低频、可丢）
  + mouth pose（音量驱动，高频、latest-value）
```

- 素材：每个 eligible 情绪提供 `pose_0/pose_1` 同尺寸底图及对应嘴形；两姿态必须同构图、同光照、无镜头位移。无 pose bank 自动退 StaticHold+Mouth，不阻塞对话。
- 节奏：生命姿态约 3～5 秒切一次，以 3 个低频 alpha 阶段渐变，禁止单帧硬跳；TTS 边沿、嘴大开、显示忙或音频预算不足时推迟，过期不补播。
- 空间：首版只更新全宽上半身带，边缘羽化；严禁为生命层启动全屏 MJPEG loop。PC 工具终态应生成带软遮罩的 `life_band_0..N`，固件不实时解 480² 再裁。
- 合成：生命姿态提交后立即重贴当前嘴形，嘴与身体使用同一 pose generation，禁止身体换姿态而嘴仍引用旧姿态。
- 预算：生命层优先级低于嘴；独立 R0 flag，per-marker mean 回退立即关闭。生命层属于 M2，不得借它放宽全屏 singleton、AFE gate 或音频优先级。
- 验收：neutral/happy 长播报不再“定格照片”；无明显水平带、无嘴漂移；字幕/TTS/第二轮唤醒不回退；稳定性仍以 per-marker mean 判门。

**2026-08-16 `s1ef_life_layer_probe`**：实现首个 R0 探针。只对白名单中性/愉快情绪启用；Speaking 且嘴等级≤1 时，pose_0/1 的 50..389 行以 85/170/255 三阶段、360ms 间隔软混合，完成后保持约 4.2s；上下 12 行羽化，每阶段重合成嘴层。Listening、强情绪、无第二 pose、锁竞争均不执行或延后，不补播。编译通过；真机寿命与观感待验收。

**2026-08-16 `s1eg_generation_guard`**：`s1ef` 真机否决——用户确认强情绪结束后嘴与下半身分裂，且三阶段宽带仍不流畅。根因是动画 tail、canonical layered base、pose bank 不属于同一可证明 generation；50..389 宽带又人为制造底部时间边界。立即 R0 关闭宽带生命层；canonical base 提交失败时，强情绪拒绝嘴层合成，中性/愉快保留 mouth-on-hold。生命层下一次只接受 PC 侧从同一底图生成的软遮罩微动资源及 base hash，不再混合两张完整 pose。

**2026-08-16 PC Life Layer v1**：`emotion_tool_dev/dialogue_emotion_builder.py` 已生成 `mjpeg_ai_dialogue_v3_life_v1`，校验 6/6。每情绪两条≤48行轨道，预裁 RGB565+A8 mask，连续 9 帧往返，绑定 `generation_id`+canonical `base_rgb565_sha256`；运行时契约 `max_tracks_per_tick=1`、latest/drop-old。`standby/neutral/happy/loving` 默认候选，`sad/angry` 默认关闭。此举能降低实时 decode/混合与 PSRAM 突发，但固件尚未消费该 life manifest，不能把“已生成”写成“已上板”。

**2026-08-16 `s1eh_pc_life_track0`**：用户确认 s1eg 基础功能正常，换 v3 包后对话/表情/退出/唤醒也正常。首个固件消费者只对白名单 neutral/happy 加载 track_0；绑定时校验实际 hold_base SHA-256，最多9帧、单帧≤48行。Speaking 每约4.2s播放一次、160ms/帧，每tick一条带；嘴等级>1或LVGL锁失败则延后/丢帧。track_1及强情绪未启用。已编译归档并双OTA烧录，待观感与per-marker寿命验收。

**2026-08-16 `s1ei_exact_rest_exit`**：用户确认 s1eh 中说“你休息吧”会继续 Listening 到云端超时。代码核查发现本地无退出意图判断，历史成功依赖云端偶发 `goodbye`，不是生命层语义回归。新增严格完整句匹配（仅“你休息吧”及句末。/！/!），命中后调用现有 `CloseAudioChannel()`，复用 channel_closed→idle→WakeNet 链路；不做模糊关键词匹配。已编译归档并双OTA烧录。

**2026-08-16 s1ei 回退**：用户明确否决本地退出判断，源码与上板固件均恢复 `s1eh_pc_life_track0`，不保留该语义改动。下一视觉刀不是同时播放双轨，而是在 s1eh 寿命门通过后让 track_0/track_1 按完整生命周期交替，以保持每tick一条带且不提高平均刷新频次。

**音频→视觉的连接（禁止音频任务碰 LVGL）**

```
AudioOutput: 每 20~40ms 算 PCM 包络 → 原子写 mouth_level(0..3) → 立即回 I2S
FaceWorker : 8~12Hz 读最新 mouth_level → 查 VisualBudget → 提交嘴部小图
```

迟滞：attack 30～50ms / release 80～150ms / silence hold ~100ms / 最小姿态保持 ~60ms。队列**覆盖式、长度 1、带 generation**，过期即丢。

> **任务栈红线（s1bh / s1bi 教训）**：`face_worker` 承担 decode/present ⇒ 栈**不得**沿用 `esp_timer`(3584) 或旧 4096 档，须 ≥8192 并用**深处采样**的 `stack_free` 判决（禁返回后 HWM）。**禁止**把包络/视觉逻辑塞进 `audio_detection`/`audio_communication`（ESP-SR `fetch()` 就在这两条栈上跑 FFT+卷积）。

**VisualBudget（准入控制，取代固定「700ms 后开动画」）**

```
Frozen < StaticOnly < MouthOnly < SmallAnimation < FullTransition
```

输入：AFE 是否在 fetch（即 s1cm mutex 是否被持有）/ 播放·解码队列深度 / 上行编码队列深度 / 近期 I2S 欠载 / LVGL 是否在刷 / 上次 present_ms（复用 `FACE_BACKOFF` 判据）/ 会话状态 / 是否已有全屏事务。

| 场景 | 预算 |
|------|------|
| 唤醒边沿 | Frozen |
| Listening | StaticOnly |
| Speaking + 播放队列不足 | Frozen |
| Speaking + 队列健康 | MouthOnly |
| Speaking + 余量充足 | SmallAnimation |
| Idle + WakeNet | **SmallAnimation 封顶**（idle∩AFE 是复位主战场之一，s1ck-fail/s1cm；**不给 FullTransition**） |
| 无音频且系统稳定 | FullTransition |

**先查预算再解码**，禁止解完才决定不显示。

**素材能力声明（manifest v2；无 manifest → legacy 六表情兼容，禁阻塞启动）**

```json
{ "render_mode": "layered",
  "capabilities": { "full_transition": true, "static_hold": true,
                    "mouth_levels": true, "eye_blink": true } }
```

固件**按能力降级**，不得假设所有素材都有嘴部 ROI（二次元半身 / 全身 / 机器人 / 动物 / 无嘴角色都要能跑）。PC 侧工具由「自动找统一 ROI」改为**能力分析 + 资源生成器**：帧索引、enter/hold/exit 分段、静态关键帧、首尾差异、全屏变化比例、是否允许对话期播放、风险等级；自动结果只是建议，必须可由 profile 覆盖并**重新校验**；**禁为取索引重编码原始 MJPEG**。

**通用的是上层状态机，不是像素算法**：`enter/hold/speaking/exit` + `VisualBudget` + `mouth_level` + `generation` + `deadline/drop` 统一；`full_frame_clip` / `fixed_roi_clip` / `layered` / `static` 因素材而异。

### 总策略

> 战役条令见 `xiaozhi-p4-core.mdc`（原则 A–G + 阶段定义）；四问/批次/预算见作战卡；当前阶段/主矛盾方面/known-good 见 `p4-current-state.mdc`。本文件专注**归因序 + 战史账本 + 演进阶梯**。

### 踩坑账本（战史：特殊规律 → 特殊解 → 可迁移认识）

| 标记 | 错 / 假象 | 钉死证据 | 改法 | 借鉴 |
|------|-----------|----------|------|------|
| 烧录门 | USB 复位后当冷启验脸；`init_fail` 去改 SD 读逻辑 | `SD_MOUNT fail` / `ESP_ERR_TIMEOUT` | 断电冷启；先 `SD_MOUNT_OK`+`seed_stills 6/6` | **现象归因错误会毁掉正确代码** |
| s1ah | speaking 无上限 `loop_continue` | `HP_SYS_HP_WDT` | 禁 EXTEND/loop；MID 有限预算 | 目标≠无限播；预算化 |
| s1aj | MID18@280 多表情加压 | 第三段 MID 中途 WDT | 曾回 MID12；**s1bc 已恢复 18**（原因果被 s1ba 证伪） | 加帧仍要多表情+二次对话过门，但**过门失败≠帧数有罪** |
| s1al | 二次对话 speaking 边沿 ROI | 第 2 帧后无续帧→WDT | Presenter 统一入口保留；时序另改 | L2 统一≠负载已安全 |
| s1am | TTS/I2S 边沿与 OPEN/快 ROI 叠峰 | settle 前易死 | speak：`open_skip`+700ms settle+ROI@280/450 | **错峰调度 > 蛮力降帧** |
| s1an | CLOSE/`present_listen` 误全幅 seed；字幕与脸互斥 | `close_seed full=480`；NotifyDialogue 未接线 | 书签 ROI；`SetChatMessage`→Presenter；`caption_live` | 条件写错=`speak=0` 时掉进全幅 |
| 诊断缺口 | 对话期未武装 CONTEND，flush 日志静默 | 死亡无 `FACE_FLUSH` | s1ao：`WdtContendArmFaceSpeak` | **没有窗口的探针=假阴性** |
| s1ao | 假说「卡在单次 flush」不够准 | `rst:0x7`+`prev_phase=FACE_UNLOCK`；`taskLVGL core=0`；`area_px=480²` 风暴 | 只钉相位，不改策略直到定论 | USB 掉线会丢 `rst:`→靠 **BootTrace RTC prev_phase**；ROI invalidate≠局部 flush |
| s1ap | unlock 后沿 patch + 同核 + close_seed | 上表 s1ao | `close_roi`/`present_roi`；LVGL `affinity=1`；patch 移 unlock 前 | **核隔离 + 热路径顺序 + 禁对话全幅书签** |
| s1aq | 对话 OK 后回 idle WDT | `prev_phase=IDLE_REST_END`；`RestoreIdleReady cost_ms≈2495`；卡在 `SetAudioRoute(force)`/`EnterCaptureMode` 前于 WK | 已 armed 则 `RestoreIdleReady(false)`+`IDLE_WK_SKIP`；分段 Mark；`FACE_FLUSH` 用真实 dirty area | **回 idle 勿重复拆 I2S；force≠必须 wake 重启** |
| s1ar | MID 从帧0 起观感偏「起势」 | seed 用 `fc/3`，prime 用 skip=0 | prime `mid_stride_skip=fc/3`；预算仍 12；`FACE_STRIDE` | **节奏跟 seed 同源，不加帧** |
| s1as | idle 静帧无生命感 | 产品要待机也动 + 首末=standby | idle ROI breathe：6 帧/@900ms + rest 回 seed；对话即停 | **有预算微动≠说话期无上限 loop** |
| s1at | breathe 抢在 P2 preload 前 | 无 `seed_stills done`；`free_psram` 仍 ~25MB | arm 须 `seed_stills_ready`；preload 前 stop；seed 后 arm | **新动效禁止插队 SD 加载** |
| s1au | seed 后 breathe「死循环」 | `FACE_BREATHE tick`→`prev_phase=FACE_PRESENT`；WDT/`Load access fault` | 曾禁用 ArmIdleBreathe；**s1bc 已解禁**——同型崩溃在 breathe 已关的纯 idle 照样发生，因果不成立 | **崩溃紧邻某日志≠该日志有罪**；`prev_phase` 只说明「崩时正好在这」，要用反例（关掉它还崩）证伪 |
| s1av | 对话 MID（二次 happy）中途 WDT | `FACE_STRIDE skip=30 decode_ms=94`→mid left=8→`prev_phase=FACE_TICK` | 曾在 speak 期禁 `fc/3` 连解；**s1bc 已恢复**，两条路径同走 stride，`decode_ms` 继续在 `FACE_STRIDE` 可观测 | decode_ms≈94 是**成本**不是**故障**；成本高要靠错峰/间隔治，不是直接砍掉功能 |
| s1aw | 长 idle Reminder MQTT 重连崩溃 | Core1 `Load access fault`；`xQueueSemaphoreTake`←`pthread_mutex`←`AtUart::Send`←`Ml307Mqtt::Connect/Subscribe`←`ConnectOnce`；A0=`0xffff0000`；`Stop()` 与任务并发 `mqtt_.reset()` | `Stop` 只置 flag+等任务退出再销毁；ConnectOnce 每步查 `running_`；destroy 前 50ms drain；失败退避 30s；`REMINDER_MQTT_*` BootTrace | **跨任务销毁 AT/MQTT 对象=UAF；生命周期归持有任务** |
| s1ax | s1aw 后长 idle 静默 HP WDT | `FW_MARKER=s1aw`；`prev_phase=REMINDER_MQTT_WAIT` `prev_t=3388142ms`（~56min）；无 Guru；崩前 AFE_DIAG/HEAP「看起来正常」；末条仅心跳 WAIT | **仅记账**；曾推 modem UART 争用。**s1bh 后须降权**：`AFE_DIAG hwm` 不能排除 `audio_detection` 栈溢出；`WAIT` 心跳≠崩点。modem 路径改由 s1ay 相位判决，禁把 s1ax 当已钉死 | **静默 rst +「AFE 正常」≠已排除音频栈**；心跳相位禁止当根因；并行嫌疑要分刀，勿与脸负载混判 |
| s1ay | idle WDT 无确定性相位 | s1ax：`WAIT` 无法判决；`IsConnected` 每 5s 发 `AT+MQTTSTATE`（看似只读、实占 UART）；与 `HTTP_POLL_*` 可时间重叠 | **只加诊断**：`HTTP_POLL_*` / `MQTT_STATE_BEG|OK` / `MQTT_CONN_*` / `HTTP_ACK_*`；BEG/OK 括号刀口；不改策略。下崩：`STATE_BEG`=卡 AT；`HTTP_POLL_*`=卡 poll；仍落 `MQTT_WAIT`/`STATE_OK`=须回 C 序（栈/PANIC） | **探针须括号危险调用，勿只打 alive；只读 API 可有总线副作用** |
| s1az | 把 `rst:0x7` 直接当“脸负载过高” | Saved PC 反解落在 `_panic_handler` / `panic_print_str` / `panic_enable_cache`，非业务函数 | 规则修订：`rst:0x7` 仅视为复位信号；必须补 `PANIC_LAST`/`Saved PC`/`addr2line` 后再改 ROI/MJPEG/调度 | **复位码不是根因；先拿到真实崩溃点再优化负载** |
| **s1ba** | **崩溃根因长期错记在表情侧**；s1au 关 breathe 当“修好”，此后每轮都在改 ROI/帧数/调度 | 两次 `Load access fault` 指纹同型：一次紧跟 `FACE_BREATHE`，一次纯 idle 无 `FACE_*`；`MEPC` 落在 `libdl_lib` 卷积核 | 当时改 `MORE_PSRAM`→`INTERNAL_PSRAM_BALANCE`（可留作卫生配置）。**事后被 s1bh 证伪为根因**：同址崩溃实为 `audio_detection` **栈溢出**后指针错乱，碰巧落在 WakeNet 核上 | **同型指纹跨场景 → 排除表情业务，正确；但「落在 libdl = PSRAM 分配模式有罪」仍跳步**。须先做 `mtval≈sp` / 栈检查（见 s1bh）再动 alloc_mode |
| **s1bb** | **`Saved PC` 被当成崩溃点**；s1az 的 `PANIC_LAST` 钩子挂在 `esp_panic_handler`，实测三次复位全是 `none`，等于白装 | s1ba 三次 `rst:0x7`：两核 `Saved PC` 同为 `0x4ff12afc`=`s_get_cache_state`（**纯内存读、无循环**，不可能死循环）；另一核 `0x4ff00c9c`=`panic_enable_cache`。链路 `_panic_handler`(vectors.S)→`panicHandler`→**`panic_enable_cache` 卡死**→走不到 `esp_panic_handler`。`cache_utils.c` 有 `while(!s_flash_op_complete)` 自旋，双核同时 trap 会互等 | 钩子前移到 `--wrap=panicHandler` + `--wrap=xt_unhandled_exception`（IRAM、`panic_enable_cache` 之前），直接存 `RvExcFrame`；新增 `entries`/`core2` 判定 `DUAL_CORE_TRAP` | **所有落在 panic 路径内的 `Saved PC`（`_panic_handler`/`panic_print_*`/`panic_enable_cache`/`esp_restart_noos`/`reconfigure_wdts`）一律只说明「现场已丢」，不是根因**；探针必须挂在卡点之前，否则等于没装 |
| **s1bc** | **误判连累的降级一直没撤**：s1ba 已证伪「表情负载致 WDT」，但 s1au/s1av/s1aj 三处按该错误因果做的削减仍留在代码里，等于长期扣着观感还债 | s1ba 指纹钉死根因在 WakeNet PSRAM 内核；s1bb 钩子上线后冷启+对话压测**未再复现** `rst:0x7`，`PANIC_LAST` 兜底在线 | 恢复三处：①`ArmIdleBreathe` 解禁（s1at 的 `seed_stills_ready` 守卫保留）②MID prime stride 说话期也走 `fc/3`（原 s1av 强制 skip=0）③MID 预算 12→18，收敛为常量 `kFaceMidFrames` | **撤降级要按「原始因果是否被证伪」逐条判，不能整批回滚**：s1ah 禁无上限 loop、s1am 说话期 280/450 + 700ms settle 属设计原则（错峰/收缩阶数），与错误因果无关，**保留不动** |
| **s1bd** | **真崩溃点终于抓到：不在业务侧，在 PSRAM 总线**。此前 s1au/s1av/s1aj/s1ba 全部在业务侧找因，方向根本错了 | s1bb 钩子首次输出真实帧：`core=0 via=2 mcause=5(LoadAccessFault) mepc=0x48088d88 ra=0x48086032 mtval=0x4990b030`，`addr2line`→`esp_sr_lib/.../p4/esp32p4_bf_s16_hp.S`（AFE FFT 汇编）；另一次 `core=1 mcause=1(InstrAccessFault) mepc=0xfffffffe ra=0xffffffff`。**关键反证：s1ba 固件（breathe 已禁用）冷启同样连续 `rst:0x7`**，故与 s1bc 恢复的动效无关 | 关掉 `SPIRAM_XIP_FROM_PSRAM`+`FETCH_INSTRUCTIONS`+`RODATA`+`FLASH_LOAD_TO_PSRAM`（原先四项全开＝全部 .text/.rodata 在 PSRAM 执行，与 14MB 表情缓存 + LVGL FB + AFE 缓冲同挤一条 HEX@200M MSPI；IDF 文档明示该选项「will actually increase the load on the PSRAM MSPI bus」）。**注意：此刀事后被 s1be 证伪，不是修复**；保留的唯一理由是**把「代码在哪执行」这个变量固定成 flash**，使 `mepc` 不再随机漂在库里、能直指业务行号（s1be 因此才定位到 `decode_task`）。当作诊断脚手架看待，勿再当成已修 | **`mepc` 落在第三方库 ≠ 该库有 bug**：当代码本身就在 PSRAM 执行时，`mepc` 只标记「总线失手时 CPU 正好在跑谁」，所以指纹会随机漂在 libdl/esp_sr/任意常驻代码。**判据：`mtval` 是合法 PSRAM 地址却 Load 失败 → 查总线/时序/配置，不要查业务** |
| **s1be** | **关 XIP 没解决**：s1bd 假设「取指挤 PSRAM 总线」，改完冷启仍无限重启。说明根因不是代码放哪，而是 **PSRAM 数据访问本身不可靠** | s1bd 冷启 `DUAL_CORE_TRAP`：**两核同一瞬间双双 LoadAccessFault**——Core0 `mepc=0x400a434c`→`dl_esp32p4_..._atrous_conv1d_qacc...S:127`（WakeNet 卷积）读 `mtval=0x494e2f00`；Core1 `mepc2=0x40030f92`→**`emotion_video_player.c:961 decode_task`** 读 `mtval2=0x48093230`。代码已在 flash（`0x40xxxxxx`）执行，两个**互不相关**的任务同时读合法 PSRAM 地址却失败；崩前 `FACE_BREATHE tick decode_ms=89` + AFE 并发 | 本轮只加诊断：`SPIRAM_MEMTEST` 恢复为 IDF 默认 `y`（本项目曾为省启动时间关掉，等于关掉唯一的 PSRAM 完整性自检，失败会带地址 abort）。曾试 `BOOTLOADER_LOG_LEVEL=INFO` 暴露 PSRAM/MSPI 初始化日志，但 bootloader 涨到 `0x6480` 超分区上限 `0x6000`，**已撤回保持 NONE**；PSRAM 自检靠 memtest abort，不靠 bootloader 串口。**「降到 20M / 开 ECC / 8LINE」当时只是猜测，把握不足，未做** | **两个无关任务同时在同一资源上 fault = 该资源整体失效，业务侧全部排除**。这是比「指纹同型跨场景」（s1ba）更强的判据：同型指纹还可能是同一段库代码，双核异地同刻则只能是共享硬件。~~推论：200M PSRAM 是 experimental 配置，先怀疑它~~ —— **该推论先被 s1bf 修正（「共享硬件失效」与「共享硬件被软件临时关掉」现象一样），最终被 s1bg 彻底作废：真根因是 heap 损坏，`mtval` 只是被踩坏的指针值。禁止再据此降频** |
| **s1bf** | **把「双核同刻 fault」直接读成「PSRAM 硬件不可靠」，差点去降频/开 ECC**——那是纯猜测（把握远不足九成）。真正被漏掉的是 s1bb 早就摆在眼前的线索：两核 `Saved PC` 落在 `s_get_cache_state` / `panic_enable_cache`，当时只判「现场丢失」就放过，**没问为什么偏偏落在 cache 函数上** | 读 IDF 源码钉死机制：`spi_flash_disable_cache()` → `cache_hal_suspend(CACHE_LL_LEVEL_EXT_MEM, CACHE_TYPE_ALL)`，**`EXT_MEM` 是 flash 与 PSRAM 共用的那一级**，所以任何 flash 写（NVS commit 等）期间，PSRAM load 必然 `LoadAccessFault`、flash 取指必然 `InstrAccessFault`（正解释 s1bd 的 `mepc=0xfffffffe`）。`cache_utils.c` 原注释写明其前提是「两核此时都不碰 external RAM」，靠 `esp_intr_noniram_disable()` + IPI 保证；`CONFIG_SPI_FLASH_AUTO_SUSPEND` 未开 | **只加诊断，不改策略**：`--wrap` 四个 `spi_flash_{disable,enable}_interrupts_caches*` 记 in-flight 深度/发起核/序号（DRAM_ATTR，cache 挂起时仍可读），panic hook 里连同 `spi_flash_cache_enabled()` 一起存 RTC，新增 `PANIC_LAST \| CACHE` 行。判据二分：`cache_on=0` 或 `flashop_depth>0` → **软件时序问题，可修**（查 IRAM-safe ISR 是否碰 PSRAM／framebuffer）；两者皆 0 → 才轮到 PSRAM 时序/频率 | **「共享资源失效」必须再分一层：是它坏了，还是它被人合法地关了。** 前者只能降规格，后者是可修的软件 bug——**先证伪后者**。更一般地：探针解析出的函数名不只用来判「现场是否丢失」，**函数名本身就是线索**（落在 cache 函数上＝该问 cache 状态），别只做二值判断就丢掉它 |
| **s1bg** | **「PSRAM 200M 硬件不可靠」是误判**（正确）。但接着把根因写成 **heap 损坏**——**事后被 s1bh 证伪**：`HEAP_INTEGRITY` 全 stage `ok=1`，野 `mtval`/落在 `tlsf`/`rtos_int_enter` 实为**栈溢出踩坏栈帧与指针后的二次表象** | `CACHE` 行证伪 cache-suspend（全 `cache_on=1 flashop_depth=0`）。诊断脚手架有价值：①`HEAP_POISONING_LIGHT` ②`HEAP_INTEGRITY` stage 探针 ③拉长 `FLASH_WINDOW` | 探针保留；**勿再把「mtval 越界 / 落在 tlsf」直接当堆根因**——须先做第 0 步 `mtval`↔`sp`（见统一归因序）。堆完整性 `ok=1` 即可排除堆损坏假设 | **探针分层有价值，但「排除 PSRAM」≠「钉死 heap」。** 每排除一层后必须重新跑完整归因序，禁止在半截结论上改策略 |
| **s1bh** | **根因终于钉死：`audio_detection` 任务栈 4096 溢出**。此前一路误判：s1au/s1av/s1aj 怪表情负载 → s1ba 怪 AFE PSRAM 分配 → s1bd 怪 XIP → s1be/s1bf 怪 PSRAM 200M 硬件 → s1bg 怪 heap 损坏。**全错，且每次都因为「没把探针指向栈」** | s1bg 双向定死：①`HEAP_INTEGRITY` 在 `app_start`/`p2_preload_end`/`enter_idle`/`tick_10s` **全部 ok=1** → 堆完好，排除 heap 损坏 ②`cache_on=1 flashop_depth=0`（15+ 次全一致）→ 排除 cache-suspend ③决定性样本：`mcause=7(StoreAccessFault) mepc=0x4ff0026a`→**`_interrupt_handler` at `vectors.S:297`**（中断压栈保存上下文），`mtval=0x4ff68f0c` 距 `sp=0x4ff68ef0` 仅 28 字节；`ra=dl_nn_pointwise_conv1d`。另两样本 `ra=test_real_fft_hp`→`esp32p4_bf_s16_hp.S`/`direct_radix2_bit_reverse`。**ESP-SR 的 `fetch()` 在调用者栈上跑 FFT + WakeNet 卷积**，4KB 装不下 | ①`audio_detection` 4096→**12288**、`audio_communication` 4096→8192 ②开 `FREERTOS_WATCHPOINT_END_OF_STACK`（硬件 watchpoint，越界即精确报任务名）③`main.cc` 加 **SAFE_MODE**：`crash_streak>=5` 时在 `Application::Start()` **之前**停住并循环打印，USB 永久可写；`FLASH_WINDOW` 改 `>=2→30s`、`>=3→90s`（覆盖 ~80s 全量烧录） | **`uxTaskGetStackHighWaterMark` 会骗人**：本例 `hwm=1912` 看着安全，但它在 `fetch()` **返回后**采样，测不到推理执行中的真实峰值——**在阻塞调用返回后测栈等于没测**。**`mepc` 落在 `_interrupt_handler`/`rtos_int_enter` 且 `mtval≈sp` ＝ 栈溢出铁证**，此时 `ra` 才是罪魁任务。教训链：`mtval` 在 PSRAM 区间 → 别急着怪 PSRAM；堆 canary 全 ok → 别怪堆；**先看 `mtval` 与 `sp` 的距离**。另：重启风暴必须有 SAFE_MODE，否则每轮只有 8s 静默，永远烧不进去。**风暴末期 USB CDC 端点会彻底死锁**：`COM7` 仍在（`Get-PnpDevice` 状态 OK、端口可 Open），但读 0 字节 + esptool `Write timeout`；此时 `Disable/Enable-PnpDevice` 软重新枚举**无效**，只能物理断电或按 BOOT 上电进 ROM download mode。配套：`flash_when_alive.ps1` 轮询 COM7，一活立刻抢窗烧录。**烧录后验收**：`FW_MARKER …s1bh…` 已上板；生存时间从 ~25–50s 拉到 **~143s**，崩溃点已离开 AFE FFT/WakeNet。残留：`prev_phase=FACE_PRESENT prev_t≈143s` → `LoadAccessFault` `mepc=heap_caps_free` `ra=free_decoder_data(lv_bin_decoder.c)` `mtval=0xfb020113`（坏指针 free）；另有一次同寿命 WDT 且 `PANIC_LAST none`。**下一刀只查 LVGL bin_decoder UAF/双 free，禁止再砍 AFE/表情预算** |
| **s1bi** | **s1bh 残留被误判成「lv_bin_decoder UAF」**；表象是 `ra=free_decoder_data` / `tlsf_walk_pool`，实质是**堆已被先踩坏**。对话结束回 idle 正常 PASS 后，idle breathe 仍崩 | ①addr2line(s1bh ELF)：`mepc=heap_caps_free` `ra=free_decoder_data:564`；另一次 `PrintHeapStats`→`largest_free_block`→`tlsf_walk_pool` `mtval=0x04d58d04`（`cache_on=1`）②`FACE_BREATHE`/`FacePanelAnimTick` 在 **`ESP_TIMER_TASK`（`CONFIG_ESP_TIMER_TASK_STACK_SIZE=3584`）** 上直接调 `hw_decode_frame_optimized`；同函数在 `emotion_decode` 任务栈为 **5120** ③崩前末标 `prev_phase=FACE_PRESENT`；回 idle `RestoreIdleReady cost_ms=5` 已 PASS → 非对话收尾刀口 | **`FacePanelAnimTimerCb` 只 `Application::Schedule`→`FacePanelAnimTick`**（`main_event_loop` 栈 **8192**）；timer 回调禁止再跑 decode/Present。`FW_MARKER=…s1bi_face_timer_mainstack` | **邻接日志落在 LVGL free ≠ LVGL 有罪**（再验证 s1au/s1az）。**深活（JPEG decode）禁止放在全局 timer 小栈上**——溢出踩堆后，受害者可以是任意 `heap_caps_free`/`tlsf_walk`。验收：冷启+对话+idle breathe 长泡；M+=`FW_MARKER …s1bi…` + 持续 `FACE_BREATHE tick` + 无新 `REBOOT_AFTER reason=PANIC`；M−=再出现 `ra=free_decoder_data` / `tlsf_walk` 于 FACE_PRESENT |
| **s1bi-ops** | **Agent 复犯：把「可烧录根据地」打穿**。规则已写 CDC 死锁只能断电/BOOT，仍在 `Write timeout` 后连跑 `flash_when_alive` 数百次 + 未烧进时继续推进 | `FLASH_GAVE_UP`；COM7 Open 但 Write timeout；s1bi **从未成功写出**（板子仍可能是 s1bh） | 立刻停一切 esptool；E5 升格为「一次 timeout 即停」+ E7「未烧进≠已上板」；preflight 增烧录纪律勾选；**只等用户物理断电/BOOT** 后再单次抢窗 | **可烧录 > 任何未验证的代码刀**。软重试会把 CDC 钉死；空转抢窗 = 主动破坏后勤 |
| **s1bj** | L2 未收口：init/`ForceStop`/少数路径仍直接 `PlayMjpegEmotion`/`CommitFaceStillOnce`，旁路 Presenter | 代码检索：`InitEmotionSystem` 直调 Play；`ForceStopAndSwitchEmotion` 直调 Play；seed/preload/flush 已部分走 Notify | 公共入口一律 `NotifyEmotion`；`CTRL PRESENT commit … via=presenter s1bj`；Play/Commit 仅作 leaf。`FW_MARKER=…s1bj_presenter_strangle` | React→LVGL：状态只经 Presenter Commit；leaf 可解码/上屏，不可再当入口 |
| **s1bj-gate** | — | 串口：`via=presenter` + happy/angry/sad `FACE_SLICE` + breathe≥6min + `HEAP ok=1`；无脸相位新 REBOOT | **L2 过门**；可进 L3 | 稳门与升阶分开 |
| **s1bk** | L3「脸+字幕合并」仅分次加锁；compose 全屏 blit 已证 WDT 不可开 | ROI `PresentFaceFrameToLvgl` 与 `NotifyDialogue` 各抢锁 | MID 期 caption **defer**；ROI 持锁内 `FlushCaptionIfDirty`→`PresenterApplyDialogueAssumingLock`；`FACE_L3 coalesce … s1bk`。**禁** `USE_COMPOSE_BLIT=1`。`FW_MARKER=…s1bk_l3_coalesce`；ELF `xiaozhi_s1bk.elf` | 合并调度≠全屏 compose；同锁局部刷即可 |
| **s1bk-boot** | — | 上板：`FW_MARKER …s1bk…` + `SD_MOUNT_OK` + `seed_stills 6/6` + `via=presenter`；breathe 续跑 | 实现/烧录门过 | USB 启可验 marker |
| **s1bk-gate** | — | 对话：`caption_defer`→`caption_coalesce`→`FACE_L3 coalesce`×5（happy/sad/angry/laughing）；`FACE_SLICE` open→mid→close；回 `FACE_BREATHE`；无新 Guru | **L3 过门**；禁开 compose；下一刀勿扩帧 | 同锁合并≠全屏 compose |
| **s1bk-note** | idle ~60s `prev_phase=FACE_PRESENT` + Guru | addr2line：`dl_esp32p4_sr_atrous_conv1d…` / `dl_nn_pointwise_conv1d`；`cache_on=1`；`mtval≠sp` | **B4 并行 AFE/SR 轨**，非 coalesce 路径；禁用砍脸「修」 | 邻接相位≠因果 |
| **s1bk-reboot** | 用户见多次重启疑「正常」 | 最近：`rst:0x7` `prev_phase=IDLE_ENT_BEG` ~419s `PANIC_LAST none`；另有 AFE Guru | **均异常**（非 SW/烧录预期）；idle/AFE **B4**；本刀不修 | 复位码≠正常关机 |
| **s1bl** | MID18 从 fc/3 **连续**只盖 ~1/5 片长，情绪弧偏短 | happy 90→仅 ~30–47 | `mid_arc_step=max(1,(fc/3)/18)` + `decode_next_n`；`FACE_ARC … s1bl`；帧数仍 18。`FW_MARKER=…s1bl_mid_arc`；ELF `xiaozhi_s1bl.elf` 已烧 | 升观感靠抽样跨度，不靠加帧 |
| **s1bl-boot** | — | `FW_MARKER …s1bl…` + `SD_MOUNT_OK` + `seed_stills 6/6`；breathe 续跑 | 烧录/冷启门过 | |
| **s1bl-gate** | 缺对话 `FACE_ARC` | 同场 `FACE_ARC … s1bm` + `FACE_SLICE`（happy/sad/angry） | **过门** | 稳门与升阶可同场验 |
| **s1bm** | 各情绪共用 fc/3 起点，happy/sad 弧区分弱 | 同一 step 仅平移窗口 | `mid_arc_start`：happy=fc/4、sad=2fc/5、else=fc/3；step 仍 span=fc/3÷18；`FACE_ARC … s1bm`。`FW_MARKER=…s1bm_emo_arc` | 偏置起点≠加帧 |
| **s1bm-gate** | — | `start=` happy22/sad36/angry34/loving24 与 SD fc 表一致；`FACE_COST` mid decode_ms~3–5 无暴涨 | **过门**；主矛盾转 s1bn | 同场多表情即可 |
| **s1bn** | 固定中心 ROI 240²@120 头脸硬水平接缝 | 外周 seed + 中心弧帧矩形复合 | ROI→`w/2 × h*5/8`、`y=(h-rh)/4`（偏上）；`FACE_ROI_GEO … s1bn`。`FW_MARKER=…s1bn_roi_bias` | 消接缝≠全屏 flush |
| **s1bn-gate** | — | `FACE_ROI_GEO x=120 y=45 wxh=240x300` 于 happy/sad/angry/standby；`FACE_ARC` 仍 s1bm 值；breathe present_ms≤23；无脸相位新 `REBOOT_AFTER` | **过门**；顶边已上移，但**左右/下沿仍冻结**（肩部不动） | 单矩形只能挪，不能消 |
| **s1bn-note** | 用户报「肩膀有条线不动」+「待机像静止」 | ROI 只覆盖 x=120..359、y=45..344，肩/下摆恒为 seed；breathe=6 帧@900ms、prime `skip=26` 连续帧（standby fc78），振幅极小 | **先 L1t（s1bo 帧率）再 L1b（s1bp 全宽行带）**；禁与逐帧 bbox 叠刀；breathe 弧化另刀 | 固定矩形边界=接缝；刷屏按全宽条带→横向缩框不省钱 |
| **s1bo** | 误把「ROI 框小」当不丝滑主因；MID 实为 ~2–3fps 幻灯片 | s1bn 场：mid decode≈3.4 present≈13；flush area 峰值 24000=`width*50`；旧 speak 280/450ms | 统一 MID 节奏 speak/idle 160/140；去 fast→slow 掉档；`FACE_BACKOFF` 160→260→450（present>35 或 lock 超时）；`FACE_RATE … s1bo`。`FW_MARKER=…s1bo_frame_rate` | **时间维度单刀**；帧数仍 18；ROI 几何不动 |
| **s1bo-ota** | `idf.py flash` 写 ota_0@0x200000，板子实际从 **ota_1@0x700000** 起 | 读回 0x200000 已是 s1bo，串口仍 `s1bn`；otadata seq 指 ota_1 | 双槽都写 + 复位 otadata；验收须冷启见 marker | 双 OTA：写错槽=假烧录成功 |
| **s1bo-gate** | — | `FACE_RATE interval_ms=160 step=0`；mid 时间戳差≈177ms；`present_ms` 10–14；无 `FACE_BACKOFF`；`FACE_ARC` 仍 s1bm | **L1t 过门**；下一刀先 s1bq 再 s1bp | 节奏单刀已验 |
| **s1bq** | OPEN standby→MID seek 片中，开场猛跳 | s1bm `start=fc/4|2fc/5|fc/3` | `mid_arc_start=0`；`step=max(1,fc/18)` 铺满整段；`FACE_ARC … s1bq`。`FW_MARKER=…s1bq_full_arc` | 匀速抽样≠播满帧；撤回 s1bm 偏置 |
| **s1bq-gate** | — | 冷启 `…s1bq…`+seed；`FACE_ARC start=0 step=5` happy/angry/sad/delicious；`FACE_RATE` 仍 ~160 | **过门**；主矛盾转 s1bp | 开场自然性单刀已验 |
| **s1bp** | s1bn 书签框外冻结 → 肩/左右残影 | 只刷 240×300，框外仍旧 seed | ROI→全宽×行带（prefer h≈350 snap50，≤70% 行；超限回 s1bn）；`FACE_ROI_GEO … band s1bp`。`FW_MARKER=…s1bp_row_band` | **空间维度 L1b**；弧/帧率不动 |
| **s1bq-reboot** | 用户见「对话结束后复位」 | 同场：`RestoreIdleReady cost_ms=5` 已 PASS → idle ~26s → `prev_phase=MQTT_STATE_OK` `PANIC_LAST none`；另有 `FACE_PRESENT`+`dl_nn_pointwise_conv1d` LoadFault（mtval∈PSRAM、`cache_on=1`、`mtval≠sp`） | **B4→s1br**：跨任务 `reset_buffer` 与 fetch 竞态；多轮 `free_psram` 稳态无泄漏；禁砍脸 | 对话收尾无罪；邻接相位≠因果 |
| **s1br** | 对话结束/听态切换后偶发 LoadFault 于 WakeNet | `Stop()` 在 main 任务直接 `afe_iface_->reset_buffer`，与 `audio_detection`/`audio_communication` 中 `fetch_with_delay` 并发 | `Stop` 只置 `afe_reset_pending_`；fetch 任务在 WaitBits 前/后应用 `reset_buffer`；`AFE_RESET defer|apply … s1br` | **B8 共享对象生命周期**；谁 fetch 谁 reset |
| **s1bs** | 固定全宽带仍可能切掉肩下脏行 → 残影 | s1bp `y=0..300` 固定，脏行在带外则留旧像素 | canvas 行差定 dirty 行 → snap50 全宽带；cap 400；`FACE_ROWS`/`band s1bs`。`FW_MARKER=…s1bs_rowdiff_afeown` | 行差≠全图像素 bbox；仍禁 compose |
| **s1bs-gate** | — | 冷启 `SD_MOUNT_OK`+`seed_stills 6/6`；`FACE_ROWS`/`band s1bs`；PSRAM 对话期 11.32MB 钉死 | **L1b 实现上板**；breathe 常 `capped=1`（成本另刀） | 须冷启，USB 复位不够 |
| **s1bs-sd** | 用户报「愤怒变聆听、听不见也不说」 | `SD_POWER_CYCLE_REQUIRED`/`init_fail`/`preload abort`；`free_psram≈32MB`（未装缓存） | **E1 冷启**；先 SD 门再验脸/听说；非 ROI/愤怒逻辑 bug | 无 seed=无表情；听态壳≠会话健康 |
| **s1bt** | s1br 后仍 idle~439s `LoadAccessFault` | `prev_phase=FACE_PRESENT`；`mepc/ra=dl_nn_pointwise_conv1d`；**`mtval=0x38`（NULL+off）**；`cache_on=1`；`HEAP ok=1`；崩前 breathe 正常、无紧邻 `AFE_RESET` | **停喂再 reset**：`EnableWake*` 先清 `AS_EVENT_*` +20ms 再 `Stop`；`Feed` 查 RUNNING；唤醒检出只清 DETECTION 不 pending reset。`FW_MARKER=…s1bt_afe_feed_gate` | s1br 必要但不充分；Feed∩reset 可致潜伏损坏；**邻接 FACE_PRESENT≠脸有罪** |
| **s1bu** | s1bt 门控仍有 TOCTOU；板上崩仍在 **s1bs**（s1bt 冷启未闭环） | 同场：`S0/S1=0` `mtval=0x38`；addr2line→`dl_nn_pointwise_conv1d`←`afe_fetch`←`AudioDetectionTask`；栈 canary/`hwm=10104` 完好；非脸 | **Feed 与 `reset_buffer` 同 `afe_ops_mutex_`**；保留 s1bt 清 AS_EVENT。`AFE_RESET apply … s1bu`；`FW_MARKER=…s1bu_afe_feed_mutex` | flag 检查≠互斥；潜伏损坏可在 idle fetch 爆 |
| **s1bu-gate** | — | 上板后 `AFE_RESET … s1bu`；同会话 idle≈622s breathe 续跑；**无**新 `REBOOT_AFTER`/`Guru`（相对 first s1bu） | **软过门**（稳）；残影转 s1bv；正式长泡仍观察 | 实现上板≠视觉残影已消 |
| **s1bs-note** | 「残影已 s1bs」易误读成视觉过门 | 对话 happy/angry/sad **全** `capped=1`；居中封顶 → band 常 `0..399`，y≥400 肩下永不刷 | **未过 L1b 视觉门**；仅实现上板 | DoD=肩/左右无硬边，不是有 FACE_ROWS |
| **s1bv** | capped 居中切掉肩下脏行 → 残影仍在 | 上表 s1bs-note；dirty 常 0..479 | capped 时 **顶/底锚定交替**（仍 max 400 行）；`FACE_ROWS … anchor=top\|bot s1bv`。`FW_MARKER=…s1bv_cap_anchor_alt` | 交替覆盖两端≠全屏 flush |
| **s1bv-vis** | — | 用户报残影「淡了」 | 视觉改善；**未**当稳门 | |
| **s1bw** | 二次退对话后仍复位 | 退 idle PASS 后 ~12s；`prev_phase=FACE_PRESENT`；Core1 **InstrAccessFault mepc=0 ra=0**；栈→`lv_draw_image`/`lv_image_event`；崩前 `anchor=bot rows=430`（>400） | bot 锚定禁止再 snap 拉高；硬夹 `rows≤400`；`FACE_ROWS … s1bw`；`FW_MARKER=…s1bw_band_height_cap` | 邻接「退对话」≠刀口；超高 invalidate 可踩 LVGL |
| **s1bw-gate** | — | 上板后 `FACE_ROWS … s1bw`；rows 直方图≤400；同会话≥4min breathe 无新 REBOOT | **软过门**；残影锚定保留 | |
| **s1bx** | 待机「像静止」 | breathe 仅 mid-clip 连续 6 帧@900ms（fc78 的 ~8%） | prime `skip=0` + `step=fc/6` + interval 600ms；`FACE_BREATHE … s1bx`。`FW_MARKER=…s1bx_breathe_arc` | 生命感靠弧跨度，不加帧 |
| **s1bx-crash** | s1bx 上板后多次 idle 复位 | `FACE_BREATHE`/`FACE_PRESENT`；`present_ms` 暴涨；InstrFault `mepc=0xfffffffe`；根因：`decode_next_n(step≈13)`=**13×JPEG/tick** | **s1by**：`decode_at` 索引 seek + 单次 JPEG；breathe 用绝对 `idx`；弧跨度保留 | 跨度≠连解；成本维度单刀 |
| **s1by** | s1bx 用 N 次连解实现弧跨度 → 拖死 idle | 上表；`decode_one(skip)` 同型 | `emotion_video_player_decode_at_rgb565`；`FACE_BREATHE … s1by`；`FW_MARKER=…s1by_decode_at` | O(1) seek；MID `decode_next_n(step)` 另刀 |
| **s1bz-audit** | **全局复位统计推翻「s1bx 拖崩」这一说法**；今日 227 次 `REBOOT_AFTER` 横跨 s1bh→s1bx 全部 marker，与脸的十几刀改动无关 | 每 marker 平均寿命：s1bc/s1bd/s1bf/s1bg ≈28–50s（=s1bh 栈溢出）→ s1bh 后 ≈100–1400s。**s1bn（固定 240×300 框）=1441s**；**行带时代 s1bp/s1bs/s1bv/s1bx ≈81/439/106/118s**。s1bx 并未比 s1bv 更差 | 主矛盾方面从「breathe 弧」转为「**present 路径 PSRAM 总线成本**」；s1by(decode_at) + s1bz(breathe 跳过镜像拷贝) 同一假设下合并一刀；`FACE_BUS … s1bz`；`FW_MARKER=…s1bz_face_psram_cost` | **用「每 marker 平均寿命」而不是「最近几次崩」判回归**；观感刀与稳定性刀的相关性必须用统计量证，不能凭印象 |
| **s1bz-attr** | 归因序全层已排除，唯一活着的假设是 **PSRAM 数据访问**（此前 s1be 提过、从未做对照） | 20+ 样本：`mtval` 几乎全落 PSRAM（0x48/0x49…）；`mepc` 分散在 **ROM memcpy 体 0x4fc19f5x**（`ra`=`PresentFaceFrameToLvgl`/`SyncCanvasFromRgb`）、`dl_nn_*conv1d`（AFE）、`tlsf integrity_walker`（堆自检）、`_interrupt_handler vectors.S:297`；两核均有；`cache_on=1 flashop_depth=0`；`HEAP_INTEGRITY` 全 ok=1；`mtval≉sp`。**`flashop_seq` 各次都停在 447–477 → idle 期几乎无 flash op，cache-suspend（s1bf）彻底排除**。另有坏指针形态：`mtval=0x84df9cc4`（合法 PSRAM `0x48df9cc4` 的首两 nibble 互换）、`0xfffffffe`、`0x0` | **共同因子只有「正在访问 PSRAM」**。配置为 `SPIRAM_MODE_HEX` + **`SPEED_200M`（IDF 对 P4 标 `depends on IDF_EXPERIMENTAL_FEATURES`，默认 `SPEED_20M`）**。先做软件侧降载（s1bz）；若仍崩，**200M→20M 单变量对照**是唯一未跑过的实验 | **「不同模块 + 两核 + 跨 15 个 marker 都在同一资源上 fault」= 该资源问题**（强于 s1ba 同型指纹）。但「资源有问题」仍分两层：先降它的负载（软件、可逆），再动它的工作点（规格） |
| **s1bl-afe** | s1bl 上板 ~48s idle 再崩 | `prev_phase=MQTT_STATE_OK`；`PANIC_LAST` `mcause=5` `mepc/ra=dl_nn_pointwise_conv1d` `mtval=0x49507700` `cache_on=1`；`mtval≠sp`；崩前 breathe rest→seed | **B4 并行 AFE/SR**（同 s1bk-note）；非 MID 弧路径；禁砍脸。监控：`stability_watch.py`→`stability_alerts.log`/`stability_triage.log` | 邻接 MQTT_STATE_OK≠AT 卡死（已到 OK） |
| **s1bl-cdc** | 上班监控中途 COM7 无字节；esptool `Write timeout` | 串口 OPEN 成功但读 0；一次 chip_id 即 Write timeout | **E5 CDC 死锁**；已停一切 esptool；只留只读 monitor/watch；**须用户物理断电或 BOOT 上电**后再抢窗 | 禁 flash_when_alive 连打 |
| **s1bl-note** | s1bl 上板后 ~48s 再崩 | `prev_phase=MQTT_STATE_OK`；`mepc/ra`→`dl_nn_pointwise_conv1d`；非 FACE_ARC 路径 | **B4 AFE**；不回滚 s1bl | 与弧抽样分刀 |
| **s1bz-gate** | — | 上板 `FW_MARKER …s1bz…`；`FACE_BUS rows=400 bytes=384000 patch=0 breathe=1`；`FACE_BREATHE decode_at … decode_ms=3~4`（s1bx 为 45） | **成本实现过门**（decode 降 11×、breathe 写流量减半）；**稳定未过门**：3 次 boot 寿命 212/41/78s，仍在行带时代量级 | 降载有效≠根因已除 |
| **s1ca** | 假设「`main_event_loop` 8192 栈不足以承载 s1bi 之后的 present 深活」——所有 `PresentFaceFrameToLvgl`/`SyncCanvasFromRgb`/`tlsf_walk_pool` 崩帧 `sp` 都在 **0x4ff5b6xx–0x4ff5b7xx 同一栈**；野指针形态 `0x84df9cc4`↔合法 `0x48df9cc4`（首两 nibble 互换）像被覆写而非总线错 | **已证伪**：栈 8192→24576 并在 present **深处**（非返回后）采样，`FACE_BUS … stack_free=22828 task=main_event_loop` → 峰值仅用 **1748B**，原 8192 本就余 6.4KB | 加栈保留作余量（无害），**不得当作根因修复**；假设回退到 s1bz-attr（PSRAM 数据访问）。`FW_MARKER=…s1ca_mainloop_stack`；ELF `xiaozhi_s1ca.elf` | **「同一栈上反复崩」只说明该任务干活最多，不等于栈不够**——必须用深处采样的 `stack_free` 判决，别用 sp 聚类替代测量。这是 s1bh 的镜像误判：那次是栈真溢出，这次形似而非 |
| **s1ca-asset** | **把「对话 standby 是静止书签」误说成「standby 表情本身静止」**，进而低估了 standby clip 与情绪过渡的可用性 | ①`emotion_video_player.c:1405` 注释「frame0 … all emos looked identical」＝**首尾契约帧**存在的旁证（当初正因此把 seed 从 frame0 改到 `fc/3`）②standby fc=78 且 breathe `idx=0/13/26/39/52/65` 每帧 `dirty=0..479` → **clip 内部有动画** | 术语拆成**契约帧** vs **表情块**（见上节）；由契约推出「情绪间桥可用 standby 内部帧」；**契约本身仍需探针钉死**（见下节规划 P1） | **素材契约属于「本板特殊规律」，只靠一条代码注释口传＝没有确定性证据**；术语混用会直接推出错误的产品结论 |
| **s1ca-pick** | 观感还债项风报比不一，需用户裁定，避免 Agent 自作主张抬 cap / 降 PSRAM | 用户裁定：**E/C/D 可做**；A/B/F/G/H/I(P0) **冻结**；TTS 争用时**降动画不砍功能** | 出刀序 **E→P1→C→D**；冻结表见下节；TTS 原则写进允许/禁止 | **风报比由人拍板**；Agent 只执行裁定后的单刀，不把冻结项「顺手做了」 |
| **s1cb** | s1ca 续泡仍复位；末次 `prev_phase=FACE_PRESENT` 易误判砍脸；监控曾多实例抢 COM7 | ①`rst:0x7`/`prev_t=224s` 崩前 `FACE_BREATHE`/`FACE_ROWS` 正常；`PANIC_LAST` **`mepc=loop_end_fftr`/`ra=test_real_fft_hp`（AFE FFT）** `mtval=0x4ff6bb00` `cache_on=1` `mtval≉sp` ②另次 `tlsf_walk_pool` 野 `mtval=0x04c0998c` ③`stack_free=22700` 再证伪栈不够 | **音画冗余**：MID=`decode_at`（E）；speak 默认 cadence **step=1(260ms)**；曾试 idle breathe `rows=200` | **邻接 FACE_PRESENT ≠ 脸有罪**；降行带须看视觉门 |
| **s1cc** | s1cb breathe=200 → **头身割裂**；对话 OPEN 像「旧静止帧」 | dirty 常 0..479，每 tick 只刷 200 行顶/底交替 → 头身来自不同帧；OPEN 用 mid-clip **seed(fc/3)** 非契约 frame0，与 breathe 活姿不一致 | **撤回 breathe 特供 200**（仍 400）；曾改 bookend=`decode_at(standby,0)` | 行带不能砍到接缝级 |
| **s1cd** | s1cc frame0 书签 → **唤醒/对话开场双眼紧闭** | SD clip **首帧常是眨眼/闭眼**；`open_skip`+MID `start=0` 把闭眼姿钉在屏上；seed(fc/3) 才是开眼静帧（历史注释 frame0 近白/同姿） | bookend/rest 恢复 **seed**；`mid_arc_start=fc/3`；step 扫尾段；`FW_MARKER=…s1cd_open_eye_seed` | **「契约帧=frame0」在本板素材上不成立时不能硬套**；开眼静帧以 seed 为准，P1 探针后再定契约 |
| **s1ce** | P1 契约未钉死，C 桥不能开 | — | seed 后对 6 表情 `decode_at` f0+last，同算法 sparse hash；`FACE_ASSET emo=… same_ends= vs_standby_f0=`；汇总 `FACE_ASSET contract=0\|1 matched=n/6 … s1ce`。**不改策略**。`FW_MARKER=…s1ce_asset_contract` | contract=1 才允许开 C；=0 则 C 作废或改书签定义 |
| **s1ce-gate** | — | 冷启上板：`FW_MARKER …s1ce…`；`FACE_ASSET`×6 全 `same_ends=0`；`vs_standby_f0` 仅 standby=1；**`contract=0 matched=0/6`**（standby f0=`e8f66ae1` ≠ last；happy/sad/angry/loving/neutral 的 f0 均 ≠ standby.f0） | **契约假设证伪**：本板 SD **不是**「各表情首尾=standby 静止帧」。**C 情绪间桥按原设计作废**；书签继续用 seed(fc/3)。D 空档 standby 不依赖该契约，仍可另议 | **口传素材规律必须以板上 hash 为准**；frame0 闭眼/异姿与此一致 |
| **s1cf-obs** | **14 小时监控空窗被误当「板子稳」** | `serial_monitor.log` 停在 08:13、`pos` 不变；但 COM7 一 Open 即有 **2685 字节待读**、板上 `t=53s`（即一直在跑且仍在复位）。原因：早晨 08:08–08:13 反复重启采集，留下 **两套 python 环境 × 3 脚本 = 6 实例** 互抢 COM7 | 全杀 python → 单实例 `serial_monitor.py` → 验证 `Length` 增长后才分析。**采集侧健康必须先验证再读数** | **「日志没有新崩」有两种解释：真没崩，或没人在收**。判稳前先证明采集活着（文件在长、`pos` 在动）；多实例抢串口是本板惯犯（s1cb 已记一次，本次复犯） |
| **s1cf-attr** | 「今天的复位」系统性判决：**90 个 distinct PANIC 事件散在 40+ 族**，横跨 19 个 marker、两核、所有任务栈、十几个互不相关模块（tlsf / dl_nn_* / ROM memcpy / lv_draw / fft / IRAM vectors） | ①`addr2line`(本 ELF `xiaozhi_s1ce.elf`)：s1ce 的 **5 次**（5 个不同 mtval，非重播）全落 **`tlsf_walk_pool` tlsf.c:211/219** `block_size` 读 `block->size`；同族上溯 s1bh/s1bj/s1bn/s1bv/s1bz/s1ca/s1cb 共 **14** 帧，`sp` 恒 `0x4ff5bxx/0x4ff5fxx`(main_event_loop) ②**该族 14 帧中 13 帧 mtval 是畸形地址**：`0x844cc5d4`↔合法 `0x484cc5d4`（首两 nibble 互换）、`0x048e1a94`/`0x038ee1e0`/`0x050ead54`（整体右移一个 nibble）——**无一个合法 PSRAM 地址**；其余各族 mtval 反而**几乎全是合法 PSRAM 地址却访问失败** ③堆**未**被持久写坏：`ok=1` **7533** 次 vs `ok=0` **1** 次，且 IDF 坏块详情（`Bad head/tail`/`CORRUPT HEAP`）**0** 命中 ④归因序全层已排除：panic 路径(1)、cache suspend(2, `flashop_seq` 停 447–477)、栈溢出(3, `stack_free=22700/24576`)、堆损坏(4) ⑤`sdkconfig`：`SPIRAM_MODE_HEX` + **`SPEED_200M`**（IDF 对 P4 标 `IDF_EXPERIMENTAL_FEATURES`，默认 20M）+ **`SPIRAM_ECC_ENABLE` 未开** | **主矛盾方面从「present 路径 PSRAM 负载」上升到「PSRAM 工作点本身」**。两种 mtval 形态是同一病的两面：*读出的指针被整体移了一个 nibble* = 采样相位错；*合法地址访问失败* = 访问超时。HEX(x16) 模式下 4bit=1 nibble 恰是一个数据相位 → 与 200M 时序边缘吻合。**唯一未跑过的单变量对照 = `SPIRAM_SPEED` 降档**（P0，用户曾冻结「续攒样本」，今样本已足，须重新裁定） | **广谱 ≠ 无解**：崩点越分散、越该怀疑共享资源而非业务代码。这直接解释了「砍了十几刀脸都没换来量级变化」（每 marker 平均寿命恒在 75–170s）——脸只是 PSRAM 最大消费者，改脸只改暴露频率。**畸形指针的位模式是可读的证据**，不要只看「野指针」就归给 UAF |
| **s1cf-selfneg** | 本回合两次自证伪，均**同回合撤回**，未据以出刀 | ①猜「integrity walk 是长临界区 → 128 次 FACE_PRESENT WDT 元凶」：实测 walk 时长 `n=7556 p50=2ms max=14ms`、`≥100ms 0 次` → **证伪** ②猜「崩富集在 walk 的 2ms 窗口内（164/249=66%）」：该数字含 **s1bg 之前根本没有此探针的老固件段**，剔除后真实 `tail_dangling=2/88` → n 太小，**不足以定案，撤回**（且此判据灵敏度低：panic 时 UART 尾部整段丢失，抓不到 dangling，故也不能反证 addr2line） | 结论只建立在 **PANIC_LAST + addr2line** 上（panic 钩子存盘、不依赖崩前 UART） | **统计判据必须先剔除「构造性 dangling」**（老固件无该探针 → 段末必然悬空）。分母不干净的富集度＝幻觉；低灵敏度判据可证实不可证伪 |
| **s1cf-errata** | **根因落地到硅片**：`esptool chip_id` → **`ESP32-P4 (revision v1.0)`**；Espressif 官方 errata **MSPI-750 / MSPI-751** 均标 **「Fixed in chip revision v3.1」** | ①**MSPI-751**：DMA/CACHE 随机访问 PSRAM，「先写后读」同址在 **AXI/MSPI core 频率比越界**时返回旧数据 ②**MSPI-750**：非 4B 对齐 burst1/2 DMA 读返回旧数据 ③本板 v1.0 + HEX + 曾踩 20M/200M 两档 | **硅片病成立**。但当时把 workaround **读反**成「降 20M」（后被 s1cf-fail/s1cf-ratio 证伪）——正确约束是 `N×freq_core≥freq_axi`，要 **高** MSPI 钟。ECC 对本病无效 | **芯片 rev 必须早查**；读官方 workaround 表时分清 `freq_core`=MSPI 钟 ≠ CPU |
| **s1cf-cut** | 用户裁定执行：降 20M 单变量对照 | `sdkconfig`+`sdkconfig.defaults.esp32p4`：`SPIRAM_SPEED_20M`；曾试 `BOOTLOADER_LOG_LEVEL_INFO` → **bootloader 0x6480>0x6000 编不过** → 回 NONE，改 app 打 `SPIRAM_CFG`。`FW_MARKER=…s1cf_psram_20m`。ELF `xiaozhi_s1cf.elf` | **上板已见**：`FW_MARKER …s1cf…` + `SPIRAM_CFG speed_mhz=20 mode_hex=1`；`present_ms` ~15→**~200ms**、`decode_ms` ~3→**~30ms**（带宽门生效）；`free_psram` 仍 ~11.4MB（**容量未变**）。同场 `CONTEND_STALL afe_fetch ~200–280ms`。**稳门未过**：烧录后一次 boot 的 `PANIC_LAST` 仍为 `tlsf_walk_pool` + `mtval=0x84f5f8f4`（nibble-swap 形态），`prev_t≈125s`——须冷启长泡判决是否仍复现 | BL INFO 撑爆分区≠可忽略；验收改 app 侧标记 |
| **s1cf-lesson** | **本次归因战役复盘 → 原则固化**（见下表扩写） | 见材料：s1cf-obs / s1bz-audit / s1cf-attr / s1cf-selfneg / s1cf-errata / s1cf-debt | 写入 core C 序步骤 -1、战史教训表、preflight 默念 | 复盘同回合写回，否则下一会话又从业务刀起步 |
| **s1cf-debt** | **A8 违约**：`s1by`–`s1cd` 七个 marker **无 ELF 快照**，今日只能靠 `build/xiaozhi.elf`(07:57=s1ce) 解析 | `elf_snapshots` 最新为 `xiaozhi_s1bx.elf`(05:01) | 已补存 `xiaozhi_s1ce.elf`；**构建后立刻存快照**恢复为硬动作 | 无 ELF 的 marker 等于**不可复判**——地址在版本间会漂移，跨 marker 借 ELF 只能定组件、不能定行 |
| **s1cf** | 用户确认走 **20M 单变量**；同时开 bootloader INFO 以便见 DQS/Speed | 配置：`sdkconfig`+`defaults.esp32p4` → `SPIRAM_SPEED_20M`；`BOOTLOADER_LOG_LEVEL_INFO`；`FW_MARKER=…s1cf_psram_20m` | 容量仍 32MB（降频≠降容量）；带宽约降 10×；验收见下 | **降频不是投降砍目标**——是对硅片 errata 的规避，与「关表情保不死」不同类 |
| **s1cf-gate** | — | 冷启：`FW_MARKER …s1cf…` + **`SPIRAM_CFG speed_mhz=20`** + seed；长泡 mean ≫170s；M−=仍 nibble-swap@tlsf | **未过门** | 见 s1cf-fail |
| **s1cf-fail** | 20M 单变量**未消除**复位 | 上板后 **15** 次 `REBOOT_AFTER PANIC`，mean lifetime **≈80s**（min33/max164）；`mepc` 仍 `tlsf_walk_pool`；`mtval` 仍 nibble-swap/`0x04……` 移位；多发 `prev_phase=IDLE_HEAP`（正是 PrintHeapStats→walk）。带宽副作用：`present_ms~200`、`afe_fetch~250ms`。P4 CPU **仅 40/360** 两档，无法降 AXI。errata 约束要 **freq_core 足够高** → **降 MSPI 到 20M 可能反而恶化比值** | **不得宣称 20M 已修好**。下一刀 **s1cg**：关 integrity walk（诊断探针变凶手）；20M **暂留**作单变量对照，勿叠回 200M | **规避刀也要过门才算胜利**；方向与 errata 公式冲突时立刻重开假设，勿死磕 |
| **s1cg** | walk 是最稳触发器 | 上表 IDLE_HEAP + tlsf；堆无持久损坏 | `PrintHeapStats` **不再**调 `heap_caps_check_integrity_all`；打 `HEAP_INTEGRITY … skipped=walk s1cg`。`FW_MARKER=…s1cg_no_heap_walk` | 关探针≠修好硅片；只验「去掉 walk 后寿命是否量级上升」 |
| **s1cg-fail** | 关 integrity 后仍崩@tlsf | s1cg 上板后仍 `mepc=tlsf_walk_pool`、`prev_phase=IDLE_HEAP`、nibble-swap mtval；addr2line 同族 | 根因：`heap_caps_get_largest_free_block` **也会** `tlsf_walk_pool`。s1cg 只砍了一半 | 关「名叫 integrity 的 walk」≠关所有 walk |
| **s1ch** | 去掉全部热路径 tlsf walk | 上表 | PrintHeapStats 去掉 largest_*；BootTrace RecordPhase 不再取 largest。`HEAP … largest=skipped s1ch`；`FW_MARKER=…s1ch_no_tlsf_walk` | 单刀补全 s1cg |
| **s1ch-gate** | — | per-marker：s1cg mean≈83s → **s1ch mean≈871s**（min570/max1107，n=4）；崩点离 tlsf，落到 PresentFace / WakeNet LoadFault；带宽仍 20M 副作用 | **过门**（去掉 walk 拉寿命 ~10×）；硅片病未除 | 触发器≠根因；下一刀纠正比值方向 |
| **s1cf-ratio** | **s1cf 把 MSPI-751 读反了**：官方 workaround 是 `N×freq_core ≥ freq_axi`（freq_core=MSPI/PSRAM 时钟），要 **高** MSPI 钟，不是降到 20M | 官方 errata 表：poor+AXI concat → 3×freq_core≥freq_axi；20M 时 3×20=60 ≪ AXI，**更深掉进危险区**；200M 时 3×200=600 / 4×200=800 才可能满足。s1cf-fail 板上已证 20M 无稳门收益 | **撤回 20M**；见 s1ci | 读错 workaround 方向＝整场实验无效；比值约束里的「core」是 MSPI core 不是 CPU |
| **s1ci** | 撤回失败的 20M，回到满足比值约束的 200M；s1ch 的「禁热路径 tlsf walk」保留 | 上表 s1cf-ratio + s1ch-gate；闪烁主因是 20M 带宽门：present≈200ms + top/bot 400 行交替 + 重启 | `SPIRAM_SPEED_200M`；`FW_MARKER=…s1ci_psram_200m_ratio`；ELF `xiaozhi_s1ci.elf`；双槽+otadata 已写 | 验收：冷启 `SPIRAM_CFG speed_mhz=200` + present_ms 回 ~15；稳门另计 |
| **s1cj** | 第二轮唤醒听态「闭眼+正在聆听、说话无表情」 | 串口：`FACE_BREATHE stop present_enter` 后**无** `FACE_SLICE present_listen`；`ignore standby during live`；breathe 末帧（如 idx=50/66）冻在屏上。根因：`EnterConversationPresent` 仅在 `face_anim_*` 时 bookend，idle breathe 不置这些 flag | listen/connecting **总是** `PresentStandbyBookend`（开眼 seed）；speaking 无 pending 时亦清 blink 残影。`FW_MARKER=…s1cj_listen_seed_bookend` | 听态书签≠仅 MID 收尾；ignore_standby 时书签是唯一开眼路径 |
| **s1ck** | 200M 合比值后仍偶发复位；breathe 全幅 dirty 扫白做 | 串口 breathe 常 `dirty=0..479 capped=1`；全高 PSRAM 读再写=MSPI-751 触发窗口 | idle breathe **跳过** dirty 扫描，假定全脏走 capped top/bot；MID 仍扫。`FACE_ROWS … scan=skip s1ck`；`FW_MARKER=…s1ck_breathe_noscan` | 降触发≠降 20M；对话路径单变量不动 |
| **s1ck-boot** | — | 双槽烧录 EXIT=0；USB 启后见 `scan=skip`（breathe）与 `scan=full`（MID/angry）；bin 含 `s1ck_breathe_noscan` | 实现/烧录门过；**寿命稳门待冷启长泡**（对标 s1ci≈278s） | 须断电冷启见 `FW_MARKER …s1ck…` + `SPIRAM_CFG …200… mem_mhz=180` |
| **s1ck-gate1** | — | 用户已冷启并确认 s1cj 观感（闭眼听态已修）。板上功能 marker 确认 s1ck（`scan=skip s1ck` 在跑）；banner 未采到。已观测：一轮 ~350s 后复位（盲窗，无 PANIC），随后一轮 **连续 ≥469s 仍在涨**，对标 s1ci mean≈278s | **寿命门尚未判决**（n 太小）；**冻结**下一刀，先长泡 | 单轮超 mean ≠ 过门；n<4 不判 |
| **s1ck-obs-fail** | 6:21–6:26 盲窗，丢掉那次复位的 PANIC_LAST | WindowsApps 的 `python.exe` 是**存根**，每个逻辑进程在 `Win32_Process` 里成对出现（存根 + pythoncore 真实进程）。误判「重复 monitor」并 kill，杀掉的是真实进程 → 采集死、meta 仍 `running=1` | 判重复必须看 **CommandLine + 解释器路径成对性**；停采集前先确认 `serial_monitor.log` 是否仍在增长 | `running=1` ≠ 在采；成对进程 ≠ 双实例（勿再自伤观测根据地） |
| **s1ck-fail** | 寿命门判决 | 完成轮 n=3：~284 / **667** / **76** s，mean≈343（对标 s1ci≈278）；667s：`prev=FACE_PRESENT` + `mepc=dl_*atrous_conv`/`ra=dl_nn_pointwise_conv1d` + `mtval=0x494f7f00`（MSPI-751）；76s 再短命 | **未过稳门**；降 breathe 扫描不够；主矛盾方面→ **idle AFE∩脸** | 单轮超 mean≠过门；短命轮否决 |
| **s1cl** | idle AFE fetch ∩ breathe present 同窗撞 PSRAM | 上表 s1ck-fail；speak 已关 AFE，错峰须落 idle | `AfeFetchGate` 包 `fetch_with_delay`；`IdleBreatheTick` 忙则 `FACE_BREATHE skip_afe`（不砍 left）。`FW_MARKER=…s1cl_afe_busy_only`；ELF `xiaozhi_s1cl.elf` | 降动画节奏≠关 AFE/砍脸 |
| **s1cl-fail** | busy-only 实现门失败 | 冷启后仍 `skip_afe` 连跳、`tick=0`、`left` 冻；fetch 连续 → busy 间隙≈0 | **证伪**「仅看 busy 标志」；改互斥等锁 | 连续 fetch 下 busy skip=假关动画 |
| **s1cm** | AFE∩脸须真互斥，不是忙闲探测 | s1cl-fail | `AfeFetchGate`→**mutex**：fetch Enter/Leave；breathe `TryLock(60ms)` 包 decode+present。`FACE_BREATHE skip_afe … s1cm`；`FW_MARKER=…s1cm_afe_face_mutex` | 等锁=冗余容错；持锁 present 时 fetch 让路 ~十几 ms |
| **s1cl-flash** | 双槽烧录遇 CDC `Write timeout` | COM7 Open 后写超时；**已停**一切 esptool（E5） | 用户断电/BOOT 后单次抢窗 **EXIT=0**（双槽+otadata+assets Hash 全过） | 烧录后门仍须**冷启**验 `FW_MARKER …s1cl…` |
| **route-v2** | **两条被证伪的路线假设**：①「PC 自动裁统一 ROI → 固件播小 ROI」当通用解 ②400 行全宽行带当**对话期局部刷新终态** | ①新素材 `mjpeg_ai` 全画幅生成动画：背景/镜头/人物整体在动，`dirty` 恒 `0..479`，无统一静止 ROI 可裁 ②`480×400×2=384KB`=屏幕 83%，硬件只省 ~17%；s1bs-note（capped 切肩）、s1cc（顶/底交替头身割裂）、s1bv/s1bw（锚定与限高只是缓解）三次都指向同一结论：**大条带不是局部刷新，是打折全屏** ③`s1bz-audit`/`s1cf-attr`：行带时代 per-marker 寿命 81–439s，明显差于 s1bn 固定框 1441s；每 tick 384KB 全宽读写正落在 MSPI-751 触发窗口 | **路线修订**（见上「素材分类与渲染契约」）：对话期改**静态情绪底图 + 独立小图嘴部/眼部 ROI**；全画幅 clip 退回低频一次性动作 + 安全窗；400 行条带**降级为兼容/诊断路径**，不再是 L1b 终态目标。**按阶段拆刀**（core 一节）：防御期先做任务隔离/三态/全屏单例，`VisualBudget` 与 manifest v2 属后续阶段 | **「局部刷新」的局部必须由素材能力定义，不能由固定行数定义**。同一抽象（seed+增量+局部）在不同素材上落成不同渲染器——**通用的是状态机，不是像素算法**。另：观感刀与稳定刀在此处**合流**（降突发同时消接缝），但合流不等于免过门，仍须 per-marker 平均寿命判决（A5/s1bz-audit） |
| **s1cm-gate** | — | `reboot_audit.py` per-marker（单位 **ms**）：`s1cm n=14 boots=14 mean≈2054s min≈324s max≈6697s`；对标 s1ck≈371s / s1ci≈278s / s1ch≈871s。**min 已≈s1ck 的 mean**，n≥4 | **寿命门过**（暴露频率降约 6×）。硅片病**未除**（仍 14 次复位），阶段仍防御。**已建 known-good**：`elf_snapshots/xiaozhi_s1cm.elf` + `fw_archive/xiaozhi_s1cm.bin`（E8/G3 的 R2 落地） | **AFE↔face 真互斥有效**，验证 s1cl「忙闲探测」证伪是对的。注意：采集自 7/30 15:20 起停摆两天，样本全来自 7/30——**判的是该 marker 的寿命，不是「今天板子稳」**（s1cf-obs） |
| **s1cn-gate** | route-v2 防御期正交批次实现门 | 串口：`FW_MARKER …s1cn_route_v2_batch`；`face_worker`×227；`s1cn-b` pending/commit；`s1cn-c begin`；上板后 `REBOOT_AFTER=0`（样本仍不足 n≥4） | **实现门软过**（a/b/c 见日志）。寿命门继续泡，不对标宣称稳。known-good 可升为 s1cn（ELF+bin 已有） | 采集曾断档；以 marker 后 chunk 为准 |
| **s1co** | 对话期仍跑 400 行 MID＝防御期最大突发 | TTS/listen 每 tick ~384KB 行带 | **StaticHold**：对话上下文禁 MID 行带，只刷情绪 seed 静帧；idle breathe 不动。并入已有 BootTrace 定长环（face_worker 并发安全）。`FW_MARKER=…s1co_static_hold_bt_ring`；flag `e_static_dlg` / `FaceRouteV2_SetStaticDialogue` | M+=`s1co-e static_hold`；对话期无 `FACE_SLICE mid_arm … roi_follow=1`；回滚 R0 关 e 或重烧 s1cn |
| **s1cp** | s1co 后：上下头分割线；情绪「快闪完→僵停最后一帧」 | StaticHold/`PresentStandbyBookend`/breathe 仍走行带（`has_fb→roi_only` → `anchor=top|bot`）；单帧 seed 无 enter | **f** breathe/hold/bookend 强制 `seed_full`；**g** 匀速全帧 enter（6×180ms）→ hold seed。`FW_MARKER=…s1cp_full_still_enter` | **实现门软过**：串口 `enter_arm`→`enter_hold`（happy/angry/sad/laughing/relaxed）。寿命门未判（n&lt;4）。ELF+bin 已存。**下一刀堵点=无独立嘴/眼小图** → 禁越级开 VisualBudget/L1m 解码路径 |
| **s1cr** | idle 全帧 breathe WDT；字幕被「正在说话」盖；嘴无分层 | s1cq 早退已落地；补 timer/tick；caption keep；PC rgb565+eye；固件 `face_mouth_layer` | `FW_MARKER=…s1cr_caption_mouth`；M+=`!!FACE_CAPTION keep`/`flush_end`；`!!FACE_S1CR h=1 pack=`；有 pack 时 `mouth_arm`/`mouth_blit`；无 pack 降级 StaticHold | SD 部署 `/sdcard/dialogue_v2/`；PC：`emotion_tool_dev/dialogue_emotion_builder.py` |
| **s1ct-gate** | 砍 idle 动态过猛；开机 SD 加载黑屏 | Init 过早 Play→空 canvas；P2 先灌 5 段再画；s1cq 禁 breathe | **keep_main** + **seed_early/early_paint**；idle **BAND** breathe 恢复（禁再全帧 idle）；enter 8×160ms。`FW_MARKER=…s1ct_boot_face_keep` | **防御期停火点软过**：串口 `early_paint ESP_OK`、`FACE_BREATHE arm`、`enter_arm`；`mouth_skip`（包未上 SD）。ELF+bin 已存。下一刀=拷 `dialogue_v2`+调 ROI，非再改调度 |
| **s1ct** | s1cq 掐 idle 动态=过度砍功能；开机 SD 加载黑屏 | Init 过早 Play→空 canvas；P2 先灌 5 段 MJPEG 才画 | **恢复 idle BAND breathe**（禁再全帧 idle）；Init 保 main_image；**standby seed 尽早上屏**再灌其余；`FW_MARKER=…s1ct_boot_face_keep` | M+=`!!FACE_BOOT keep_main`/`early_paint`/`seed_early`；`FACE_BREATHE arm`；禁再以复位为由关动态（B3/F6） |
| **s1cu-gate** | flush fence 改变了故障形态但未根治 | 完成轮 4 次：9735695/856095/324981/116529ms；mean≈2758s。两次无 PANIC；一次 `InstrAccessFault mepc=0x0505f9fa ra=lv_draw_buf_reshape`；一次 `LoadAccessFault mepc=ROM memcpy ra=PresentFaceFrameToLvgl:1407 mtval=0x484a971c`。每次最后 `FACE_PRESENT ret`，已见 fence 长期 `ok=1` | **稳定门未过**；fence 非死锁，仍符合 MSPI-751 下高频 PSRAM 写后读/控制数据损坏。禁止据 `FACE_PRESENT` 砍动画；下一批只补 RTC 音画事务刀口 | s1ct mean≈2816s，s1cu≈2758s，无量级提升；单个 2.7h 长轮不能掩盖 116s 短轮 |
| **s1cv** | bare HP-WDT 无 PANIC 时，`prev_phase` 只能看到最后一次完成的 FACE，无法区分下一刀口 | s1cu 四轮含两次 `PANIC_LAST none`，现有 flush fence 只报告成功次数，不跨复位保存 face/AFE/flush 状态 | `afe_fetch_gate` 增加 allocation-free RTC breadcrumbs：`face_seq/stage`、`afe_seq/stage`、flush start/finish/active；启动打印 `WDT_LAST ... s1cv` 后清零。动画、间隔、PSRAM 频率均不变。`FW_MARKER=…s1cv_wdt_txn_diag` | 纯诊断 `S1/D0/R0/V1`；M+=重启后 `WDT_LAST` 可指出 CANVAS/INVALIDATE/FENCE/FETCH/DONE；M−=仍只有 `FACE_PRESENT` 无子阶段。策略刀等证据闭环 |
| **s1cv-gate** | s1cu 的 fence `ok=1` 被误读成整帧 DMA 已完成 | 549012ms 崩：`WDT_LAST face_seq=493 face_stage=DONE afe_stage=FETCH flush=5027/5027 active=0`，但 `face_t/afe_t=549081`、最后 `flush_t=549089`，AFE 比最后显示传输早 8ms 进入 fetch；`5027/493≈10.2` 恰与 400 行刷新被 50 行 buffer 拆成约 8～10 strip 同量级。addr2line：`mepc=dl_*atrous_conv1d`、`ra=dl_nn_pointwise_conv1d`、`mtval=0x49505300` | **根因闭环**：`LV_EVENT_FLUSH_FINISH` 只表示异步 `flush_cb` 返回，且每个 strip 都发一次；s1cu 等到首个 strip 即放 AFE，剩余 DMA 与 WakeNet 并发命中 MSPI-751 | 邻接 FACE 不是“动画有罪”；错误在 fence 的完成语义 |
| **s1cw** | fence 必须等 LCD 最后一 strip 的真实传输完成 | LVGL `call_flush_cb` 在 `esp_lcd_panel_draw_bitmap` 返回即发 `FLUSH_FINISH`；LCD port 的 `on_color_trans_done` 才调用 `lv_disp_flush_ready`，且 `lv_disp_flush_is_last` 可识别最后 strip | 在 esp_lvgl_port transfer-done callback 通过 weak hook 递增 `frame_ready`；fence 改等 `frame_ready`，100ms 仅异常上限；保留 RTC `ready/ready_t`。动画/帧率/400 行/AFE/PSRAM 200M 均不变。`FW_MARKER=…s1cw_final_dma_fence` | 单变量根因刀；M+=`FACE_FLUSH_FENCE ok=1 … final_dma=1` 且 `ready` 每 face txn 前进；M−=`ok=0` 或 WDT 时 `afe_stage=FETCH` 早于 `ready_t`。稳定门仍须 n≥4 |
| **s1do** | 代码真源尚是 `s1dn` 5 档 VisualBudget，`StaticDialogue=0` / `EnterArc=1`，越过 M0 存活门 | 静态核对：对话期可达 `FACE_BUDGET admit` + 400 行 MID；`s1dn` 寿命脚本 30s 预算内未完成，不作稳定裁决 | **M0 单变量收敛**：`StaticDialogue=1`、`EnterArc=0`；保留 idle BAND breathe、情绪 seed、mouth 能力、face worker、全屏单例与 AFE↔face mutex。`FW_MARKER=…s1do_m0_static_survival_gate` | M+=`e=1 g=0` + `s1co-e static_hold`；M−=对话期仍有 `FACE_BUDGET admit`/`enter_arm`/400 行 MID。待编译、冷启、n≥4 对标 s1cm mean≈2054s；失败 R0 切回开关或 R1 回 s1cm |
| **s1dp** | 用户确认 GitHub 存档版功能已真机验证；其真源组合为 `StaticDialogue=1` / `EnterArc=1` / `Mouth=1`，当前 `s1do` 仅因 `g=0` 表现为稳定但无动画 | 本地 `_ref_esp32-p4-i2cpolling` 精确对比：Presenter/嘴层与当前一致；存档 `g=1`；当前额外保留 Listening 边界接受 standby、close 失败重试、TTS 期间延后 close 三项修正 | **确定性合流**：恢复 `g=1`，保留 `e=1`使对话 400 行 MID/5 档 `FACE_BUDGET` 不可达；短 enter 后 hold seed，pack 可用则接 layered mouth。`FW_MARKER=…s1dp_reference_flow_gate` | M+=`e=1 g=1`、`enter_arm→enter_hold`、`FACE_CLOSE committed standby`；M−=`FACE_BUDGET admit`/400 行 MID、假 Listening、音频退化。失败 R0 关 g/嘴层，R1 回 s1cm；V3 继续 per-marker n≥4 |
| **s1dq** | `s1dp` 上板有 `llm emotion`、`static_hold`、`enter_arm`、解码帧，但 sad/angry/happy **每帧** `s1cn-c skip_afe` + `ESP_ERR_TIMEOUT`，仍递减 `left`后假 `enter_hold`；STT 原文“好，你休息吧。”被当普通问句，TTS stop 按 realtime 模式回 Listening | 真机日志：三情绪 8/8 present 均 timeout；`session=User state=listening voice=1`长时保持 | ① `AfeFetchGate` 仅在 face waiter 存在时，fetch 释放后让 1 tick，不绕过 mutex。② enter present 失败不消费同帧，120ms 重试，5s deadline。③ STT 窄匹配“你休息吧/退出对话/结束对话/再见了”，允许最后 TTS，stop 时 `EndSessionAndRestoreIdle`。`FW_MARKER=…s1dq_face_window_local_end` | M+=`AFE_FACE_WINDOW yield`后 present `ESP_OK`，`LOCAL_SESSION_END arm→apply=tts_stop→Idle`；M−=仍全帧 timeout、TTS 断续/STT 退化、仍停 Listening。动画失败 R0 关 g；会话意图失败移除窄匹配 |
| **s1dr** | `s1dq` 真机冷启后约 21s 起，即使 idle 也每约 960ms 出现 `AFE_FACE_WINDOW yield waiters=1`；约 63s 后 `rst:0x7`，`prev_phase=FACE_PRESENT`，`face_stage=2`，`flush active=0`，`PANIC_LAST none` | 反证 AFE 释放后让 1 tick 并未形成可靠显示安全窗，反而把长期 face waiter 与 AFE 调度耦合成重复争用；同时对话期仍无动画 | 撤销 `s_face_waiters`/yield 和 enter 同帧重试/deadline；保留情绪静帧、layered 嘴层与 `LOCAL_SESSION_END`。`FW_MARKER=…s1dr_rollback_face_window` | M+=冷启 2min 无 yield/无 63s WDT、对话音频稳定、结束语回 Idle；M−=仍 `FACE_PRESENT` WDT。后续全帧 enter 只走明确的非音频安全窗，M1 对话动画继续以 layered 小图为主 |
| **s1ds** | `s1dr` 对话结束后日志声称 `wake=1 voice=0`，却长期 `fetch_ms=0`、`afe_ago_ms`增长，随后 AFE FEED ringbuffer 每 30–40ms 报满；3 秒内及之后均不能唤醒。动画侧 pack `bind_ok`，但每次 `base_present=ESP_ERR_TIMEOUT` 后 `mouth_skip` | `EndSessionAndRestoreIdle(force_capture)` 在 reminder 分支丢弃参数；Idle 状态提前置 wake bit，`RestoreIdleReady` 误判 `already_armed`，没有在 voice deferred reset 后做真实 rearm。layered 启动仍错误依赖对话期 480×480 base present | 完整 `EnterIdleStandby` 返回后，在 Idle/None 边界执行一次 wake off→Capture(force)→on；保留 AFE 对象与既有 Stop/Start 所有权。动画本批不绕过 base 门 | M+=`force_rearm begin→end wake=1 voice=0`、恢复 `AFE_DIAG`、无 FEED full、3s 内再唤醒；M−=仍假 armed。动画下一批做安全窗 base 预驻留，不在 Speaking 期强刷全帧 |
| **s1dt** | 真机新 session 前 5 轮 STT/TTS 正常、UDP RX 至少到 `#300`；约 69s 后无新 STT，109s 起上行每包 `Ml307Udp: Not connected`，但 `session=User state=listening ch=1 voice=1`且 MQTT 仍收到迟到 `tts start/stop` | 控制面 MQTT 存活、媒体面 UDP 已被 modem URC 置 disconnected；`SendAudio` 失败只 break 当前队列，不关闭坏 session，`OpenAudioChannel` 也忽略 UDP Connect 返回值，最终永久聋哑 | 自有 `mqtt_protocol` 层检查 Connect；连续 8 次 send fail 后释放 UDP并在 mutex 外触发一次 `OnAudioChannelClosed`，复用应用现有 Idle/rearm；不修改 managed component、不在旧密钥/序号 session 内偷重连。动画冻结 | M+=掉线后单次 `MEDIA_CHANNEL trip`、退出假 Listening、可重新唤醒且新 session STT/TTS恢复；M−=仍无限 Not connected或误熔断健康 UDP。失败 R2 撤销协议熔断 |
| **s1du** | `s1dt` 退出日志在 `81897..81951ms` 执行 `s1ds force_rearm` 后，软件显示 `wake=1 voice=0`，但此后至 284s 只有每 30–40ms 的 `Ringbuffer of AFE(FEED) is full`，无 `AFE_DIAG`，实际不能唤醒 | `EndSessionAndRestoreIdle` 先由 Idle 状态完成一次 WakeNet Start，随后 `s1ds` 又立即 Stop→Start；Stop 的 deferred reset/fetch 所有权与二次 Start 竞态，Start 只恢复事件位，不能证明 fetch 消费者已前进 | 删除 `s1ds` 二次 off/on，保留 Idle 状态机唯一一次 voice→WakeNet 交接；WakeNet Start 加 generation，首次真实 fetch 打 `AFE_REARM_FETCH … s1du`。`FW_MARKER=…s1du_single_wake_rearm` | M+=退出后 3s 内出现新 generation 的 `AFE_REARM_FETCH`、无 FEED full、可再次唤醒；M−=仅 `wake=1`无 fetch。失败 R2 回滚本批；禁止用第三次盲重启掩盖生命周期问题 |
| **s1dv** | `s1du` 真机明确失败：59.830s `AFE_RESET apply`，59.870s `single_rearm wake=1`；其后 `AFE_REARM_FETCH=0`、`AFE_DIAG=0`、FEED full=8380，至 319s 仍不能唤醒 | 删除二次 Start 仍失败，说明主因进一步收敛为 WakeNet 唯一 owner 卡在 `fetch_with_delay(..., portMAX_DELAY)`；无限等待让它不能再次检查 deferred reset/运行代际。旧 s1du 探针位于有效性检查前，也可能把 timeout 误报为恢复 | 仅 WakeNet fetch 改 200ms 有界等待；无效返回每 generation 只报一次 `AFE_REARM_WAIT`，有效结果后才报 `AFE_REARM_FETCH`。voice processor 的实时 fetch、AFE 对象、managed component均不动。`FW_MARKER=…s1dv_bounded_wake_fetch` | M+=退出后≤3s `WAIT→FETCH`或直接 `FETCH`、FEED full停止、实际二次唤醒；M−=持续 WAIT/FEED full。失败 R2 回滚；若失败下一刀只能增加 owner 内部状态探针，禁止继续盲 Start/Stop |
| **s1dw** | 用户要求回到 GitHub 已验证基线。精确对照本地镜像：GitHub 的 `AfeWakeWord` 同样是 `fetch_with_delay(portMAX_DELAY)`，`audio_service` Wake/Voice Start/Stop也一致；因此 `s1dv` 把阻塞 fetch 当根因没有反例支撑。应用层唯一直接影响退出的新差异是 `s1dq` 本地 STT 结束意图＋TTS stop 主动 `EndSessionAndRestoreIdle`；同一时刻云端又发 `goodbye`，真机出现三次 `channel_closed` | 两个 teardown owner 并发操作 voice/WakeNet/channel，破坏 GitHub 原本“协议 goodbye/channel_closed 独占退出”的串行生命周期；相邻 `AFE_RESET apply`/FEED full是后果，不应继续在 AFE 打补丁 | 删除 `IsLocalConversationEndIntent`、`local_end_after_tts_`与 TTS stop 抢关；撤销 s1dv bounded fetch/代际探针，AFE 回到 GitHub 精确行为；保留 s1dt UDP坏会话熔断。`FW_MARKER=…s1dw_reference_single_exit` | M+=结束语后仅协议 goodbye→单一 channel_closed→Idle，3s内可再唤醒且无 FEED full；M−=仍多重 close或假 armed。失败只比较剩余 application/protocol 差异，禁止重新猜 AFE参数 |
| **s1dx** | `s1dw` 上板后仍不能 3s 唤醒；日志显示 TTS stop 后 11ms 收 goodbye，并出现两次服务端 goodbye、两次 `channel_closed`。协议 `CloseAudioChannel()` 即使 `udp_==nullptr` 仍回发 goodbye并回调应用 | MQTT close 缺少 open→closed 幂等门；重复 goodbye 把一次退出放大为多次 idle/audio teardown。先消除已实证的重复生命周期变量，再判断 11ms voice→WakeNet 瞬态是否仍有罪 | `CloseAudioChannel` 在锁内判 `udp_ != nullptr`；首次 `MEDIA_CHANNEL close commit s1dx`，已关闭仅 `close skip already_closed` 并返回，不回发、不回调。`FW_MARKER=…s1dx_mqtt_close_idempotent` | M+=同一退出只有一次 `channel_closed` 且 3s内再唤醒；M−=单次 close 后仍 FEED full/假 armed，则幂等刀证伪为充分修复，下一单变量延迟 TTS stop→Listening，避免 goodbye 前 11ms 启停 AFE |
| **s1dy** | `s1dx` 真机：幂等门过（一次 commit、重复 goodbye skip、一次 channel_closed），但仍无法 3s 唤醒。终止 TTS `73715ms STATE listening`，`73726ms goodbye`，Voice AFE 启动仅 11ms 即被拆；WakeNet `75130ms reset apply` 后 `76550ms` 起持续 FEED full | 终止 TTS 与 goodbye 缺少确认窗，导致同一 AFE feed 链路发生 Voice→WakeNet 高频反向交接；先阻止无意义 Voice start，再判断 owner 是否仍会卡死 | TTS stop 非 manual 时不立即 Listening，one-shot 120ms；仍为 User+Speaking+channel open 才 commit。TTS start/channel_closed 取消。保留 s1dx。`FW_MARKER=…s1dy_tts_listen_grace` | M+=普通轮 `arm→commit`；终止轮 arm 后先 close、无瞬态 Listening，WakeNet 3s内实际唤醒且无 FEED full。M−=交接已消失仍 FEED full，则停止调延迟，改加 WakeNet owner 内部状态探针。闭眼开场已定位为 `present_seed ESP_ERR_TIMEOUT` 留旧帧，另批修复 |
| **s1dz** | 用户裁定停止局部补丁，基于已验证 GitHub存档恢复并递进合入。最新一次构建同时出现字幕白块、末帧冻结和第二轮不可唤醒；此前还出现 marker=s1dx但运行含s1dy的混合对象，验证链已污染 | 先恢复完整功能行为根据地，再逐项吸收增强；不再让后加显示调度、TTS预缓冲、Wake编码常驻任务、MQTT熔断共同干扰基线归因 | 精确恢复镜像 `audio_service.{cc,h}`、`afe_wake_word.{cc,h}`、`mqtt_protocol.{cc,h}`、`eezui_display_adapter.cc`；application仅撤 s1dy grace、预缓冲调用、channel-lost自愈，保留当前提醒接口。保留稳定硬约束与本地 esp_lvgl_port最终DMA fence。`FW_MARKER=…s1dz_github_baseline_reanchor` | M+=冷启marker一致、中文字体正常、连续≥3轮且每轮3s内再唤醒、无FEED full/末帧冻结。M−=仍失败则只在已恢复基线与本地稳定硬约束交集查差异；禁止再同时合入体验增强 |
| 观感 | 向 SD 靠拢 | 帧弧/时长 | MID stride + idle breathe；不强求对齐满帧 | 稳定门与自然门分开，勿混为一谈 |
| 云端 | 「播不了 sad」怪固件 | 无 `type=llm` | 先查 MQTT llm 再改脸 | 无入口不改渲染 |
| **s1ed-audit** | 两周 356 次 REBOOT 系统审计：预防路线是否还有量级空间 | `reboot_audit.py`（`prev_t` ms）：s1dc≈16878s/s1dd≈39465s(min606)/s1de≈29088s 已把量级推到 **4–11 小时**（对标 s1cm 2054s）；但 **s1di≈118s / s1dq≈63s 明显回归**。崩点 **66%=`FACE_PRESENT`**（234/356，暴露面非罪）。mtval 直方图 **~90% MSPI-751 指纹**（65 合法 PSRAM 读失败 + ~40 nibble 互换/移位 + 18 null + 8 `0xF`）；少数 26 内部 SRAM 含 `mtval≈sp`（栈类可修） | **战略转向韧性/冗余存活**（core 原则 H + hard-constraints §8 + current-state 六/七）：控制态迁 SRAM、PSRAM 读前校验、暖启动恢复、崩因自动分流、s1di 回归二分 | **预防触硅片地板后，下一量级来自「廉价存活+冗余」不是再降暴露**；`mtval≈sp`(栈,可修) 与 nibble-swap(硅片,只降暴露) 必须分流，禁混判 |

**崩溃归因统一顺序（不可跳步；core 与本账本必须一致）**

```
-1. ★ 硅片与 errata（s1cf）：esptool chip_id 拿 revision；查 esp-chip-errata 是否已覆盖本现象
    → P4 v1.0 落在 MSPI-750/751（v3.1 才修）：DMA/CACHE 随机访问 PSRAM「先写后读」返回旧数据
    → 命中则先满足官方比值 `N×freq_core ≥ freq_axi`（要 **高** MSPI 钟，保 200M），
      **禁**再拿「SPIRAM_SPEED 降档」当规避（s1cf-ratio 已证伪，s1ci 已撤回）；
      禁再拿业务刀（脸/ROI/AFE）去修硅片；IDF 5.4.1 无官方 MSPI-751 软规避开关（已查证）
0. 抓 PANIC_LAST / Guru 完整寄存器（无则只记 rst，禁止改策略）
1. Saved PC / mepc 是否在 panic 路径内？
   → _panic_handler / panicHandler / panic_enable_cache / s_get_cache_state /
     panic_print_* / esp_restart_noos / reconfigure_wdts
   → 是：现场丢失；禁止改业务；看 PANIC_LAST（钩子须在 panic_enable_cache 之前，s1bb）
2. PANIC_LAST | CACHE：cache_on=0 或 flashop_depth>0？
   → 是：flash 挂起 EXT_MEM cache，不是 PSRAM 坏、不是脸负载（s1bf）
3. ★ mtval 与 sp 的距离（本会话最关键漏步）
   → mtval≈sp（几十字节内）且 mepc∈{_interrupt_handler, rtos_int_enter}
   → 栈溢出铁证；罪魁看 ra 所属任务；立刻查该任务 xTaskCreate 栈深
   → ESP-SR fetch()/WakeNet 在调用者栈上跑 FFT+卷积（s1bh）
4. HEAP_INTEGRITY 与 mtval 映射区
   → 全 stage ok=1 → 排除堆损坏假设（勿因偶发野 mtval 就定 heap）
   → ok=0 或 mepc∈{tlsf_*, multi_heap_*} 且步骤 3 已否 → 再查堆
5. addr2line mepc/ra → .a(obj)；落在三方库勿改脸/ROI
6. 多崩指纹同型但业务场景不同 → 排除该业务；多模块轮流中枪 → 先回步骤 3/4
7. 构建后必存 elf_snapshots/xiaozhi_<marker>.elf
```

解析：`riscv32-esp-elf-addr2line -pfiaC -e elf_snapshots/xiaozhi_<marker>.elf <addr...>`

**战史教训 ↔ 原则号**

| 教训 | 原则 |
|------|------|
| <90%只侦察 | A2 · 先胜后战 |
| 排除≠占领 | A4 |
| 反例定案 | A5 · 实验法 |
| 误判债必还 | A6 · F 演进 |
| mtval≈sp / 禁 HWM | C |
| SAFE_MODE / CDC | E · 先为不可胜 |
| 只打当前主矛盾方面 | B6/B7 · 避实击虚 · F1 相持；s1bi=face timer 栈 |
| 心跳/alive 相位≠崩点 | A1 · s1ax/s1ay · 邻接日志≠因果 |
| 跨任务销毁共享总线对象 | 软件演进 · s1aw · 生命周期归持有任务 |
| 只读检查可含 AT/总线副作用 | 知彼 · s1ay · `IsConnected`≠无代价 |
| 并行矛盾勿混刀 | B4 · idle modem 侦察 ≠ 脸/UAF 战役 |
| **硅片 rev 先于业务刀** | C−1 · s1cf-errata · `chip_id` + esp-chip-errata |
| **MSPI-751 比值方向** | C−1 · s1cf-ratio · `N×freq_core≥freq_axi`；freq_core=MSPI 钟；**降 20M 恶化比值** |
| **广谱崩=共享资源** | B1/B4 · 40+ 族跨模块 → 查 PSRAM/总线，勿砍脸 |
| **畸形 mtval 可读** | C · nibble 互换/移位 = 总线读旧值旁证，≠UAF |
| **experimental 当嫌疑人** | 0.7 · `IDF_EXPERIMENTAL`+200M ≠ 性能选项 |
| **ECC≠总线时序病** | B3 · 勿为 MSPI-751 开 ECC 吃 1/8 |
| **采集活着才判稳** | E · s1cf-obs · 日志停长 ≠ 板子稳 |
| **统计分母须干净** | A1 · 老固件无探针 → 构造性 dangling 禁当富集 |
| **降频≠降容量** | 第一性 · 20M 仍 32MB；瓶颈转带宽 |
| **BL INFO 撑爆 0x6000** | E/A8 · s1cf-cut · 验收改 app `SPIRAM_CFG`，禁盲开 BL INFO |
| **每 marker 平均寿命** | A5/s1bz-audit · 勿用「最近几次崩」判回归 |
| **小步微调收益递减** | G5 · 同一假设族连打十几刀，per-marker 寿命恒 75–170s → 换假设或打包正交批次 |
| **贵的是验证不是编码** | G · 纪律约束不可回滚性与不可分辨性，不是改动数量 |
| **无 known-good 不出刀** | E8/G3 · 失败损失必须能封顶到「一次重启」 |

**诊断关键字（复现后必搜）**：`FW_MARKER` / `SAFE_MODE` / `REBOOT_AFTER` / `PANIC_LAST` / `cache_on=` / `flashop_depth=` / `HEAP_INTEGRITY` / `!!HEAP_CORRUPT` / `FLASH_WINDOW` / `SPI SRAM memory test` / `Saved PC` / `AFE_MEM` / `CONTEND_AFE_HB` / `prev_phase=` / `FACE_PIN` / `FACE_FLUSH` / `FACE_COST` / `FACE_STRIDE` / `FACE_BREATHE` / `IDLE_WK_SKIP` / `RestoreIdleReady cost_ms` / `close_roi|close_seed|present_roi` / `CONTEND_ARM face_speak` / `HTTP_POLL_` / `MQTT_STATE_` / `MQTT_CONN_` / `rst:0x7` / `Stack canary` / `watchpoint`

### 演进阶梯（实现手法；过门判据＝存活门）

> **门已统一为存活门**：下表「验收」是**实现门**（功能上板可见）；**升阶还须过寿命门**——per-marker 平均寿命不回退（对标 current-state 的 known-good）。存活优先于视觉。战略级映射见 `p4-current-state.mdc` 的 **M0–M3**：
> - **M0**（防御·现在）= L0 + StaticHold + 低频 idle 微动；对话期不跑 L1b/L1m 行带。
> - **M1** = 有 layered 包后，仅 speak 的 L1m 嘴部（每 tick 硬上限 1 行带、2 态预算）。
> - **M2** = L2 三态 + L3 字幕 + L3b 5 档 VisualBudget。
> - **M3** = manifest v2 + 绞杀 L1b 兼容路径 + 可选 L4。
> 每个视觉特性挂 R0 flag；寿命回退即关回退，不升阶。

| 级 | 内容 | 实现门（尚须叠寿命门） |
|----|------|------|
| L0 | 崩溃可观测性 + seed + `FACE_STILL` | 可拿到 `PANIC_LAST`/`Saved PC` 且静帧可见 |
| L1 | 固定中心 ROI 增量；`prime_arm_only` | `lvgl_face incr`；无新 WDT |
| L1s | **时间片**：OPEN/MID/CLOSE；MID 有限预算；ROI **局部 sync**（禁每帧全幅 memcpy） | `FACE_SLICE` / `mid_done→close`；无新 WDT |
| **L1t** | **时间维度**：MID 节奏≈人眼融合（目标~160ms）+ 实测 `FACE_BACKOFF` | `FACE_RATE interval_ms≈160`；`FACE_COST` 相邻差≈160；present 多数≤35 |
| **L1b** | **空间维度**：全宽行带 ROI（对齐 50 行刷带；禁居中方块当终态）。**route-v2 起降级为兼容/诊断路径**：仅用于 `full_frame_clip` 素材的非对话场景，**不再是对话期终态** | `FACE_ROI_GEO` 为全宽行带；面积有行数上限（≤400，s1bw） |
| **L1m** | **对话实时层**（route-v2 主线）：静态情绪底图 + **独立小图**嘴部 3～4 档 / 眼睛 3 档；多 dirty rect，不把嘴与字幕合成大矩形 | 对话期 `decode` 为小图尺寸（非 480²）；present 行带数明显下降；嘴部延迟 <120ms；静音 ~150ms 内闭嘴；无上下拼接 |
| L2 | Presenter 统一 Commit（pending 亦走 NotifyEmotion）；**情绪三态 requested/pending/committed** | `flush_pending … via=presenter`；覆盖式 pending，不排队播过期情绪 |
| L3 | 脸+字幕调度；CLOSE ROI；`caption_live`（s1an） | `close_roi` + `L3 caption_live`；无新 WDT |
| **L3b** | `VisualBudget` 准入 + 音频包络驱动嘴部（`face_worker` 队列长度 1、generation、先查预算再解码） | 预算日志可见档位切换；I2S 无欠载；上行 Opus 不积压 |
| L4 | 可选硬件 ROI | 默认关 |

### 待还债与出刀排序（用户裁定 s1ca-pick，**顺序即纪律**）

前提：s1ca 已证伪栈溢出；s1bz-audit 已证伪「s1bx 拖崩」。用户勾选 **E / C / D 可做**；风报比不合项**冻结不做**。

> **批次化（core G2）**：本表「可做」项若各自满足「运行期可关断 + 独立 `s1xx-x` 标记 + 独立 M+/M−」，**允许同一次烧录打包**，崩了用开关二分，不必一项一轮长泡。**冻结项禁止夹带**；`sdkconfig`/栈深/分区类改动仍独占批次。

#### 可做（按序出刀）

| 序 | 刀 | 内容 | 资源消耗（量级） | 收益 | TTS 冲突时 |
|----|----|------|------------------|------|------------|
| **E** | **MID `decode_at`** | MID 从 `decode_next_n(step)` 改为索引 seek + 单次 JPEG（与 breathe s1by 同构） | 每 tick JPEG：~step 次→**1 次**；decode ~数十 ms→~3–4ms | 降本、稳；观感不变 | **s1cb 已做** |
| **P1** | **契约探针**（C 前置，纯日志） | seed 时解 `frame0`/`frame(fc-1)` 打 hash，验 `frame0[emo]==last[emo]==frame0[standby]` | 仅启动期多解几帧 | **s1ce 已做** → 看 `FACE_ASSET contract=` | 无 |
| **C** | **情绪间桥** | A→B 切换插入 standby clip **少量**内部帧（契约保证连续） | 每次切换 +2～4 JPEG + 行带 present | 切换不跳 | **s1ce-gate：契约证伪 → 按原设计冻结/作废**；另开假设再议 |
| **D** | **对话空档生命感** | 「一律 ignore live standby」→「**仅活跃时间片期间** ignore」。**route-v2 修订**：空档动效改**小 ROI 眨眼/微动**（独立小图），**不再**用全宽行带播 standby clip 内部帧 | 听态/空档 ~1 小图/600ms 级 | 对话间隙有生命感 | **降动画不砍功能**：空档间隔 600→900→1200ms；帧预算 6→4→2；TTS speaking 边沿强制停空档动画 |

**TTS 共存原则（E/C/D 共用）**：语音优先于观感满配。见 `FACE_BACKOFF` / present_ms / 听态序告警 → **先缩阶数/拉间隔**，禁止直接关 C/D 或回退到「对话期假死」。禁止用砍上行 Opus / 砍 AFE 给脸让路。

**音画冗余（s1cb→s1cc）**：TTS/AFE 争用时脸不得贴满预算——要给音频留余量：
1. speaking MID：**默认节奏勿从最密档起步**（留一档余量）；`present_ms` 提早触发 BACKOFF  
2. ~~idle breathe 行带低于对话~~ **已撤回（s1cc）**：压到 200 → 顶/底交替造成**头身割裂**；行带仍 400；余量靠 decode_at + speak cadence  
3. OPEN/CLOSE bookend = standby **seed 开眼静帧 (fc/3)**（s1cd）；**禁**再默认 frame0（本板素材首帧常闭眼）  
4. C/D：桥帧/空档间隔可再缩；speaking 边沿停空档动画  
5. 禁止：为满配动画占满 PSRAM 总线 / CPU 时间片；禁止用砍行带到接缝级「保命」

#### 冻结（风报比不合，**禁止出刀**，除非用户再裁定）

| 冻结项 | 原因 |
|--------|------|
| **A** `kBandMaxRows` 400→450/480 | 视觉收益大，但直接加压 present/PSRAM（当前主矛盾）；行带时代寿命已比 s1bn 差 ~10×。**route-v2 起升级为长期冻结**：条带路径已非终态，给兼容路径加压没有战略收益 |
| **B** 回退 s1bn 固定 240×300 | 稳但肩/左右残影回来；假稳定≠胜利。route-v2 下接缝正解是**分层小图**，不是换一个固定矩形 |
| **F** MID 18→24/30 | 锦上添花；二次对话加压负载真 |
| **G** MID 160→120ms | TTS 边沿更易叠峰 |
| **H** breathe 更密 | idle 是复位主战场之一 |
| **I / P0** `SPIRAM 200M→20M` | **已证方向错（s1cf-ratio）**；禁再做；保持 200M |
| **P2 cap 还债**（同 A） | 并入冻结 A |

**故意保留、永不「还债」**：禁 EXTEND/loop（s1ah）、禁 compose 全屏（s1bk）、speak 700ms settle + BACKOFF、禁 speaking inject / 全屏 burst。

## React→LVGL 允许

- **canonical 分层合成契约**：嘴部/眼部/低频 life 小图由 PC 相对同一 `hold_base` 预合成；空间边缘使用 A8/羽化，时间轨迹在固定帧数内按 `0→峰值→0` 缓入缓出，首尾 ROI 必须逐像素回到底图。固件只做确定性 ROI 覆盖与状态调度，禁止把每帧继续 alpha 叠加到上一画布造成漂移；不得以“更平滑”为由增加每 tick 行带数。

- 开机 seed 静帧 + 状态增量 Commit
- 固定中心 ROI / **全宽行带 ROI（L1b，兼容路径）** / **分层小图 ROI（L1m，对话主线）** / 后续硬件 ROI（默认关）局部 invalidate
- 按素材能力选渲染器：`full_frame_clip` / `fixed_roi_clip` / `layered` / `static`；无 manifest → legacy 六表情
- 全画幅 clip：**低频、一次性、安全窗、单例**（开机/闹钟/enter·exit/idle 偶发）
- 时间片 OPEN/MID/CLOSE；**MID 节奏 L1t（目标~160ms + 背压）**；STT hint / llm → 对应情绪时间片
- standby：**契约帧** OPEN/CLOSE + **clip 内部帧**做 A→B 桥（C）与对话空档生命感（D）；MID 走 `decode_at`（E）
- TTS 冲突时允许缩桥帧/拉空档间隔，不允许关 C/D 功能本身
- `SAD_DIAG` / `FACE_*` / `FACE_SLICE` / `FACE_RATE` / `FACE_BACKOFF` / Opus 上行诊断日志

## 禁止

- 对话期全屏 short_anim burst、speaking 强制 inject
- 说话期无上限 ROI EXTEND / loop_continue（已证实 → `HP_SYS_HP_WDT`）
- ROI `prime` 再打一次 `lvgl_face seed full=480x480`（二次全刷 WDT）
- 旁路 blit 与 LVGL 抢面板当主路径
- 已挂载 SD deinit+remount；InitEmotionSystem 破坏性 remount
- 听态先开 voice 再关 AFE
- 关上行 Opus / `SEND_WAKE_WORD_DATA` 却期望云端 stt
- 无串口对照跨 L 级乱改
- **USB 烧录后未断电冷启就验表情 / 因 init_fail 乱改 SD 读取**（见核心规则「烧录后强制门」）
- **冻结项 A/B/F/G/H/I（P0/P2）未获用户再裁定不得出刀**
- **因 TTS 争用直接关掉 C/D**（应降动画要求，见上表）
- **route-v2 新增禁止**：
  - 对话期把 **≥400 行条带**当局部刷新主路径（打折全屏；见 route-v2 账本行）
  - 从 480×480 全帧里**实时抠嘴**冒充分层（`decode_ms` 不降，只是把成本挪位）
  - 音频任务（`audio_input`/`audio_output`/`audio_detection`/`audio_communication`）内调 LVGL、解 JPEG 或等显示完成
  - **解码完才判视觉预算**；嘴部事件排队补播过期口型
  - 同时进行多个全屏视觉事务；在 AFE fetch 或 TTS 起始边沿铺全屏底图
  - 以「省 PSRAM 容量」为由拆 seed/底图缓存（容量非瓶颈，见 core D10）
  - 给 `face_worker` 沿用小栈（`esp_timer` 3584 / 4096 档）或用返回后 HWM 判其安全

## 验收门

- L0：`seed_stills` + `FACE_STILL` / seed 可见；并且崩溃时可拿到 `PANIC_LAST` 或 `Saved PC`
- L1：`lvgl_face incr` ROI；`prime_arm_only`；若有 `rst:0x7` 必有 `PANIC_LAST`/`Saved PC`/`addr2line`
- L1s：`FACE_SLICE open` → mid ROI → `slice_close`（standby）；无 EXTEND/loop WDT
- L1t：`FACE_RATE`≈160ms + `FACE_COST` 时间戳差≈160；`FACE_BACKOFF` 可出现但不每段退到 450
- L1b（s1bs→s1bv）：全宽脏行带；`FACE_ROWS` + `anchor=`；**兼容路径，仅非对话场景验**
- **L1m**：对话期 `decode` 尺寸为小图（非 480²）；present 行带数较 s1bv 明显下降；嘴部延迟 <120ms；静音后 ~150ms 闭嘴；无补播；无上下拼接
- **L3b**：`VisualBudget` 档位可见且随队列深度切换；I2S 无欠载；上行 Opus 队列不积压；Listening 期 STT 不退化
- **稳定门（route-v2 通用）**：不看「动画能动」，看 **per-marker 平均寿命**（对标当前 mean）+ 冷启 + 多轮长对话 + MQTT 提醒 + idle WakeNet 混合数小时；无 WDT / panic / heap 损坏
- 语音：`Session ID` + wake opus + 上行；再看 `type=stt` / `type=llm`
- **SD 前置门**：烧录后必须断电冷启；先见 `SD_MOUNT_OK` + `seed_stills done ok=6/6`，再验脸
- 失败形态：`SD_MOUNT detail=fail` / `SD_POWER_CYCLE_REQUIRED` / `early_exit=init_fail` → 只催断电，不改码
# 2026-08-16 s1eb — WakeNet 生命周期单一拥有者

- H：常规会话 Stop 的 `reset_buffer` 破坏 ESP-SR 长期 fetch 等待关系；s1ea 证据为 reset 后再无 fetch 返回并持续 `AFE(FEED) is full`。
- 唯一变量：常规 Stop 改为只撤防，不 reset；Start 递增 generation，丢弃约 300ms 暖机帧后才发布 `WAKE_READY` 并接受唤醒。
- M+：每轮 `WAKE_ARM_REQUEST gen=N` 后出现同代 `WAKE_READY gen=N`；三轮退出后 3 秒内可再次唤醒；无持续 FEED full。
- M-：任一新代无 `WAKE_READY`，或 FEED full 持续。
- 交付：`boot_trace_v10_s1eb_wakenet_lifecycle_owner` 已编译并双 OTA 烧录；ELF/BIN 已归档。USB 重启后观测到 AFE fetch 持续正常，但未捕获早期 marker，且尚未完成物理冷启、多轮人工唤醒，故不得宣称真机闭环。
# 2026-08-16 s1ec — GitHub 功能基线优先恢复

- 方法纠偏：GitHub 已验证版本是功能真源，不再把它仅当参考后在当前分支连续打补丁；以后先做 GitHub→当前→稳定性硬约束三方差异。
- 回归证据：`application.h` 残留 s1dq 精确退出注释，但字段/实现已被半回退删除；用户确认“你休息吧”不退出。
- 改动：AFE WakeNet 恢复 GitHub 默认分支生命周期；精确 STT“你休息吧”进入统一 `EndSessionAndRestoreIdle`；服务器 channel close 无论先前 listening/speaking 均走完整 `EnterIdleStandby`，主动 close 仍用 suppression 防递归。
- M+：精确短语退出；自然结束退出；两种路径 3 秒内均可第二轮唤醒。M-：任一路径停留 listening/末帧或第二轮无响应。
- 交付：`boot_trace_v10_s1ec_github_session_restore` 已编译、双 OTA 烧录、哈希通过；匹配 ELF/BIN 已归档。未用串口验收，因为现有 pyserial 打开 COM7 会触发 `CHIP_USB_UART_RESET`，不得以被监控重启后的状态冒充结果。

# 2026-08-16 s1db GitHub exact — 放弃局部补丁，整树恢复

- 用户证据：`s1ec` 烧录后仍无法第二轮唤醒，继续在当前分支缝补已失去投入产出比。
- 唯一变量：将本地 GitHub 存档 `_ref_esp32-p4-i2cpolling/main/**` 原样覆盖当前 `main/**`；源文件逐项 hash 校验 `mismatch=0`，不叠加会话、唤醒或动画业务补丁。
- 构建：执行完整 clean 后全量构建成功；固件 marker 回到存档真源 `boot_trace_v10_s1db_clean_extract`。
- 烧录：同一镜像写入 OTA_0/OTA_1，bootloader、partition、ota_data、assets 均写入，esptool 全部 hash verified。
- 归档：`elf_snapshots/xiaozhi_s1db_github_exact.elf`；`fw_archive/xiaozhi_s1db_github_exact.bin`。
- 验收边界：未开启会触发 USB reset 的串口监控；第二轮唤醒与动画表现等待用户真机直接验收，不提前宣称通过。

# 2026-08-16 s1ed — 嘴形缓动与末帧收口（未烧录）

- 基线证据：GitHub exact `s1db_clean_extract` 烧录后，用户确认多轮会话恢复正常；该会话/AFE/协议路径冻结为本轮根据地。
- H：layered 嘴层直接显示 PCM 目标等级，且 PCM 停止后保留最后等级，导致 0↔3 硬跳和末帧张嘴冻结。
- 唯一变量：`FaceMouth_Level()` 在 140ms 无 PCM 后返回闭嘴目标；Face Worker 显示等级上升每 80ms 最多一级、下降最多两级。复用 4 档素材与 latest-value，不增加 JPEG 解码、队列或全屏事务；眼眉块仍关闭。
- M+：嘴形从 closed→small→medium/large 渐入；TTS 尾音后经中间级回 closed，不停在最后开口帧；第二轮唤醒维持基线行为。
- M-：第二轮唤醒回归、字幕异常、嘴形延迟明显超过 120ms、TTS 停止后仍开口或新增音频欠载。
- 交付：`boot_trace_v10_s1ed_mouth_ease_hold` 编译成功；`elf_snapshots/xiaozhi_s1ed.elf`、`fw_archive/xiaozhi_s1ed.bin` 已归档；双 OTA、bootloader、otadata、assets 烧录完成且 hash verified，待冷启与真机裁决。

# 2026-08-16 s1ee — 长 TTS 嘴层降级启动 + 水平接缝羽化（未烧录）

- 用户现场：天气长播报开始时闪过情绪动画，随后停在 hold/末帧直至播报完成；嘴部与上层脸存在水平切割线。
- 日志证据：已记录的天气请求云端均发 `emotion=happy`，不是本地映射成 angry；反复出现 `bind_ok emo=happy` 后 `mouth_skip ... base_present=ESP_ERR_TIMEOUT`，说明 Speaking 安全门拒绝第二次 480×480 canonical base 后，旧代码错误地放弃整个嘴层。
- 素材复核：PC 包 `happy/hold_base.jpg` 与 `angry/hold_base.jpg` 未放反；设备 SD 是否同包仍须冷启/日志确认。
- 唯一变量：canonical base 全屏 present 被拒时保留现有情绪 hold 并继续 arm 小嘴层；嘴 patch 上下边缘仅 4 行 RGB565 羽化，中央仍 memcpy。未放宽全屏 gate、未增加 JPEG decode/队列、未启用眼眉块。
- M+：长 TTS 中持续出现 `s1ee mouth_blit`，不再因 `base_present` 超时冻结；水平线减弱；播报结束逐级闭嘴；第二轮唤醒不回退。M-：嘴层仍无 blit、接缝扩大、音频欠载或会话回归。
- 交付：`boot_trace_v10_s1ee_mouth_feather_defer` 编译成功；`elf_snapshots/xiaozhi_s1ee.elf`、`fw_archive/xiaozhi_s1ee.bin` 已归档；bootloader、otadata、双 OTA、assets 烧录完成且 hash verified，待冷启与真机裁决。

# 2026-08-16 s1ej — PC life 双轨完整周期交替（待烧录）

- 基线证据：用户已确认 s1eh 单轨低频生命动画“还可以”，且撤销 s1ei 本地“你休息吧”退出判断后基础会话正常；因此本刀冻结会话、AFE、音频、嘴部缓动和刷新预算。
- H：只重复 track_0 容易形成机械循环；PC 包已有同一 base hash 下的 track_1，可用完整生命周期交替增加自然度，无需提高单位时间刷新量。
- 唯一变量：neutral/happy 最多装载两条 life track；每条仍最多 9 帧、单帧最高 48 行。只有一条轨迹完整结束后才切到下一条，帧间 160ms、轮间保持原约 4.2s；第二轨失败则保留第一轨。
- M+：冷启出现 `s1ej life_track_bind ... track=0` 与 `track=1`；Speaking 长播报中 `life_blit track=0/1` 按轮交替，无同 tick 并发；第二轮唤醒、字幕和音频连续性不回退。
- M-：任一会话/唤醒回归、音频欠载、接缝明显扩大、WDT/panic，或 per-marker mean 低于生存门；R0 直接回烧已归档 s1eh。
- 交付：`boot_trace_v10_s1ej_life_tracks_alternate` 已编译成功；`elf_snapshots/xiaozhi_s1ej.elf`、`fw_archive/xiaozhi_s1ej.bin` 已归档并与构建产物 SHA256 一致；bootloader、partition、otadata、双 OTA 与 assets 已单次烧录且全部 hash verified，待物理断电冷启与真机裁决。

# 2026-08-16 s1ek — canonical 分层确定性合成（待烧录）

- 用户裁决：s1ej 冷启后基础功能“还可以”，允许继续推进，并要求底层/低频/高频层之间用羽化与缓慢变化稀释不协调。
- 根因：PC life A8 帧均相对 canonical base 生成，但 s1ej 固件把它累计混到上一画布；末帧 mask=0 无法擦除历史残留。嘴部素材已做 `canonical_base_feather_v1`，固件再对历史嘴形做4行羽化也会累积旧边缘。
- 唯一变量：PC 将每个 life ROI 预合成到 canonical base，并声明 `canonical_base_precomposited_v1`；固件见契约后直接覆盖 ROI。嘴层见既有 canonical 契约也直接覆盖；旧包无声明时仍走 legacy alpha/feather。
- 预算：仍为每 tick 最多一个行带，life 9帧×≤48行、轨迹间约4.2s；不增加解码、刷新次数或全屏事务，且移除运行时逐像素混合热路径。
- M+：新包6/6通过，12条轨迹首尾ROI逐像素等于底图；真机日志 `s1ek ... compose=canonical`，嘴部水平线/低频残影减弱，第二轮唤醒、字幕、音频不回退。M-：出现残影/矩形边界、会话回归或复位；R1回烧s1ej并恢复v3包。
- 交付：`boot_trace_v10_s1ek_canonical_layer_composite` 已编译；`elf_snapshots/xiaozhi_s1ek.elf`、`fw_archive/xiaozhi_s1ek.bin` 已归档且与构建产物 SHA256 一致；用户已换SD v4素材，bootloader、partition、otadata、双OTA与assets已烧录且全部hash verified，待物理断电冷启验收。

# 2026-08-16 s1el — ROI 感知低频层仲裁（待烧录）

- 用户现场：s1ek 基础功能可用；天气播报期间表情不动；愤怒整体表达成功但主观感觉资源紧张。当前日志采集已在14:17停止，不能用旧日志冒充本次现场。
- 代码根因：life 的全局 `mouth level>1` 门会在连续长TTS中每480ms延期，天气 happy 因而可能永久饥饿。v4实测 happy两条life ROI均与mouth ROI不相交；neutral track_0相交、track_1不相交；angry life默认关闭。
- 唯一变量：记录本tick是否实际mouth blit；若是则life延后一tick，保证每tick一个行带。否则不相交life允许在张嘴期间推进；相交life仍在level>1时延期。angry不启用life、不增加视觉负载。
- M+：天气长TTS出现 `s1el life_blit ... overlap_mouth=0`，嘴与life交错而非并发；音频、字幕、第二轮唤醒不回退。M-：出现同tick多带压力、音频欠载、接缝或复位；R1回烧s1ek。
- 交付：`boot_trace_v10_s1el_roi_aware_life_arbiter` 编译成功；`elf_snapshots/xiaozhi_s1el.elf`、`fw_archive/xiaozhi_s1el.bin` 已归档且SHA256与构建产物一致；bootloader、partition、otadata、双OTA与assets已烧录且全部hash verified，待物理断电冷启验收。

# 2026-08-16 s1em — M1 两态音频视觉预算（烧录中断，待补烧）

- 用户裁决：s1el 真机主观体感整体可接受，允许继续推进并现场验收；因此冻结 s1el 的会话、AFE、ROI仲裁、life与素材行为，本刀只增加 M1 `MouthOK/Frozen` 两态预算。
- H：嘴/life 即使已经单tick单条带，仍缺少音频欠载边沿的主动让路；低频读取播放队列与输出年龄，可在真实短欠载时冻结视觉240ms而不长期饿死动画。
- 唯一变量：每320ms采样一次音频诊断；仅在播放刚推进后，decode/playback队列同时为空且 `output_age=120..1199ms` 时进入Frozen。Frozen丢旧开嘴姿态、允许闭嘴，life同步让路；到期或队列恢复回MouthOK。运行期开关 `S1EM_J_VISUAL_BUDGET2` 可R0关闭。
- M+：长TTS正常以MouthOK为主，只在真实短欠载出现有限 `Frozen→MouthOK`；音频连续、嘴不冻结在开口、life不过期补播、字幕和第二轮唤醒不回退。M-：Frozen频繁振荡/长期冻结、持续FEED full、音频断续、退出或第二轮唤醒回归。
- 构建：Ninja成功；BIN SHA256 `8730b51ed9aad63a6693c9dd090c69bcd1e954e12d75a8a0615c63e92425aa4b`，ELF SHA256 `7a6f407a0e905eeda7add4ee1db3fc552e29a0c7fef878404bc34c1bf0a0f8d6`；归档为 `fw_archive/xiaozhi_s1em.bin` 与 `elf_snapshots/xiaozhi_s1em.elf`。
- 烧录：首次写 bootloader/partition/otadata 后继续双OTA/assets，最终在assets约85%处串口写入失败并返回非零；依硬约束立即停止，未重试。设备当前可能为部分写入态，待物理断电或BOOT上电后单次完整补烧；未见新marker前不得称已上板。
- 补烧：用户完成物理处理后执行一次完整补烧；bootloader、partition、otadata、OTA_0、OTA_1、assets 均写入成功并 `Hash of data verified`，esptool正常退出并RTS复位。仍须物理断电冷启后见 exact marker 与SD 6/6，才能进入视觉/会话验收；RTS启动不冒充冷启。
- 真机裁决：用户连续三轮体验确认 M−：第一轮嘴部变化主观过密，最大开合处与脸线条割裂明显；第二轮表情/life不明显或无动画；第三轮近正常；整体不如s1el。两态Frozen方案不再进入寿命门。高概率机制是冻结期间覆盖目标、解冻后按80ms阶梯连续追赶形成“突发补跳”，且预算跨轮状态未证明完整重置；空间割裂属于最大嘴形与canonical底图/软边界不一致，不能归咎同一时间调度变量。
- 回滚：按R1将归档 `xiaozhi_s1el.bin` 写回OTA_0/OTA_1并复位otadata，三项均hash verified；bootloader/partition/assets不变。下一设计拆成两刀：①时间侧使用硬刷新节拍、session generation重置、过期目标直接丢且禁止追赶；②PC素材侧为最大开合扩大同generation软过渡区并校验边界差，不在固件实时羽化历史画布。
- 回滚验收：用户确认恢复s1el后“当前版本整体体感还可以”，回滚闭环成立；s1el升级为当前体验基线。该证据仅覆盖短时主观体验，不覆盖冷启日志、三轮功能细项或per-marker n≥4寿命门。

# 2026-08-16 s1en — s1el 源码根据地重锚（不烧录）

- 目标：当前板上s1el已获用户体验验收，但源码停在已否决s1em，存在下一次误烧失败版本的根据地风险；本批只恢复可复现性，不增加视觉功能。
- 唯一变量：删除s1em的两态预算常量、四个跨tick状态字段、route-v2开关/API、音频诊断采样和Frozen热路径；嘴层80ms跟随、life ROI仲裁、canonical素材、会话/AFE/协议均保持s1el行为。marker改为 `boot_trace_v10_s1en_s1el_source_reanchor` 以区分构建证据。
- 验证：增量构建成功；BIN内存在exact s1en marker且无 `s1em/S1EM/visual_budget state` 残留。BIN SHA256=`abeb8f9754f38081dade1c1f3f4c4529d9f30d13fdc1bbadbd7222b6ef87c802`，ELF SHA256=`7e69c78a07dc8815340006ca4863e7dd238bbbcc6223a39b8ef2edb6fd3527fc`；已归档 `fw_archive/xiaozhi_s1en.bin` 与 `elf_snapshots/xiaozhi_s1en.elf`。
- 裁决：用户要求当前版本验收通过后把综合进度推进到60%并于今天停改；因此s1en不烧录，板上保持s1el。60%仅代表体验集成与工程可复现门，不代表per-marker寿命门。

# 2026-08-16 存档 — s1eo → GitHub esp32-p4-i2cpolling

- 当前源码/上板 marker=`boot_trace_v10_s1eo_reminder_defer_spk_noop`。PC 工具以 `emotion_tool_dev` 为准；推荐素材 `dialogue_v4_temporal_feather`。
- 分支可吸收项与明确不吸项写入 `p4-current-state.mdc` 第八节（禁整包合入 `com4-proactive-reminder-stability`）。
- 本批不改固件热路径（行为守恒存档）；代码味道债（上帝类 adapter / builder 单文件）记在第八节，另批抽取。

# 2026-08-17 s1eo 三轮验收 + 首个自然失败样本

- 功能现场：happy 嘴层仍有明显 `112x56` 方形边界；后续空间刀必须覆盖 `standby/neutral/happy/loving` 四个非强情绪逐包边界差，不再只看 angry/sad。时间节拍与空间接缝继续分批。
- 退出现场：`你休息吧。` 均被 STT 正确识别；当前本地严格退出已按用户此前裁决回退，行为依赖云端 `goodbye`。日志既有约 6s 后 goodbye，也有连续三次 STT 后仍拖到云端超时，坐实云端依赖的不确定性；若恢复本地退出须作为独立产品语义批次。
- 情绪现场：`怎么和女孩子聊天？` 云端下发 `emotion=embarrassed`；`emotion_video_player.c` 旧别名把 `embarrassed -> angry`，而其他路径又对 unsupported emotion 回 neutral，形成短暂 angry/neutral 错帧。不是云端发 angry；须独立统一六表情 canonical 映射，建议 embarrassed→neutral 或 loving，不与崩溃/素材刀同批。
- 自然失败：s1eo `prev_t=422272ms`（422.272s，n=1），`reason=WDT`、`prev_phase=FACE_PRESENT`；`PANIC_LAST core=1 mcause=7 StoreAccessFault mepc=0x4fc19f8c ra=0x4002e1ca sp=0x4ff43560 mtval=0x482edb00 cache_on=1 flashop_seq=687`。匹配 ELF 将 RA 定位到 `CopyRgb565BandRows()` / `eezui_display_adapter.cc:153,1467`。
- 复位前：idle standby breathe，`FACE_ROWS band=80..479 rows=400`、`FACE_BUS bytes=384000`；随后 `FACE_UNLOCK end`、`FACE_PRESENT ret`。AfeTxnDiag `flush=4657/4657 active=0`，AFE 仍有健康心跳。该 mepc/PSRAM mtval 与历史 s1di/s1dm 同家族，优先判 MSPI-751 写后读腐败暴露；邻接 FACE=最大消费者，不等于脸业务有罪。
- 裁决：n=1 不判 s1eo 稳定性；当前样本 422.3s 低于 s1cm mean≈2054s，记预警。禁因此关闭表情系统；下一稳定刀仍按 `R0-triage/R1-guard`，idle 400行 legacy breathe 另作为暴露量对照，不与退出/情绪/素材三刀混判。

# 2026-08-17 s1ep — 空间接缝/canonical/idle小轨道

- 用户裁决：执行问题清单1、2、4；明确不做第3项本地退出语义。
- 素材根因：旧工具只验羽化后最外沿1px，无法发现ROI内部颈饰/衣领属于另一动画帧。v5改为活动差软扩张×中央上部空间先验×矩形羽化，并增加outer/near/inner多圈层评分和热图。六包校验6/6；四个非强情绪分数均≤3，原MJPEG只复制不重编码。
- 语义根因：MJPEG私有alias、mouth私有alias和presenter原字符串不一致；统一到`emotion_video_player_canonicalize_emotion()`，`embarrassed/sleepy→neutral`，保留R0关闭恢复旧输入直通。
- 稳定刀：s1eo唯一自然失败邻接idle `rows=400 bytes=384000`；s1ep不关动态，而是优先复用standby包的双life轨道，每帧≤48行、首尾回canonical base、轮间4.2s。无包/契约失败才走legacy；R0可独立关。
- 验收：冷启先见exact s1ep marker、pack/standby两轨bind与`idle_life=1`；idle日志不得再出现400行breathe。三轮对话复验happy嘴形、`embarrassed→neutral`、第二轮唤醒和音频；稳定性仍须per-marker n≥4对标s1cm。
- 构建状态：PC工具语法与六包验证通过；清理本轮残留Ninja/ccache后，完整重建320/320成功。BIN=`17df3dbebdce8e2cce83964c77eaa78e4fbd979ce4752a23cd39deb53f4da706`，ELF=`b53342b3ce0cbd2cdd5c724e6d51435cd8b06b17fdf62ea80249d0654b8e8ba0`；已归档`fw_archive/xiaozhi_s1ep.bin`与`elf_snapshots/xiaozhi_s1ep.elf`。尚未烧录/冷启，不能声称真机通过。

# 2026-08-17 s1eq — 冻结对话全帧 enter arc

- 现场：s1ep 首次唤醒在云端下发`emotion=happy`后进入`FACE_ARC→FACE_PRIME`，首帧产生10条全宽flush（约480x480）并WDT；`prev_phase=FACE_PRIME`，DMA已完成。Saved PC仅为`_panic_handler/CaptureFrame`，损坏的`PANIC_LAST`禁止addr2line。
- 素材否证：当前准备包六个`/mjpeg/*.mjpeg`与格式化前备份SHA256逐一一致；换卡只可能改变时序，不改变机制或素材字节，禁止把复位归因成“新MJPEG坏”。
- 唯一行为变量：`S1CP_G_ENTER_ARC`默认1→0；保留StaticHold单次情绪seed、layered嘴部、Speaking/idle life与canonical。R0仍为`FaceRouteV2_SetEnterArc()`；不改AFE/Opus/会话/SD路径。
- 构建：无缓存完整重建成功。BIN SHA256=`0d67c913bd8b95c90928876b703651ef1c1d8ed53cada6a6d91b0f34aee92c12`，ELF SHA256=`5ef702e681b1199f3708657e2a385e1761762c03ba063e47612c38f9cf59bd2e`；归档`fw_archive/xiaozhi_s1eq.bin`、`elf_snapshots/xiaozhi_s1eq.elf`。
- 上板：bootloader/partition/otadata/OTA_0/OTA_1/assets单次写入，全部hash verified。物理冷启见exact marker、`PANIC_LAST none`、`SD_MOUNT_OK`、seed 6/6、pack与standby双life轨hash正常，启动旗标`g_enter=0`。
- 短验收：首次happy会话出现`static_hold_done→mouth_arm→mouth_blit`，未出现`FACE_ARC/FACE_PRIME`且未复位。该证据只通过V2短门；per-marker n<4不判长期稳定，仍需多轮/长TTS/第二轮唤醒和长泡。

# 2026-08-17 s1er — 网络掉线统一会话收尾

- 复位核查：s1eq 本轮只有 `POWERON boot_count=1`，没有第二次 ROM reset、marker 或 panic；不得把 UI 回 idle/字幕停住误记为复位样本。
- 现场链：第二次天气 TTS 尚未收到正常 stop 时 4G 掉线；旧回调直接改 idle，留下 `ch=1`，后续每 10s 报 `Channel timeout`，达到 400s+。
- 软件根因：网络异常路径绕过 `EndSessionAndRestoreIdle()`，同时 idle debounce 未把“通道仍开”视为必须清理的异常态。
- 唯一行为变量：掉线回调改走主事件循环的统一 teardown，关闭通道、清 decoder/speaker、恢复 Capture/WakeNet/唤醒提示；表情和总线预算保持 s1eq。
- 验收：M+=`NET_DOWN_RECOVERY begin ... ch=1` 后出现 `network_down_recovery ... ch=0 wake=1`，无延迟 `Channel timeout`；恢复网络后第二轮可唤醒。M-=掉线后仍有 idle+ch=1、字幕残留或唤醒不恢复。
- 交付：完整镜像双 OTA 写入并逐段 hash verified；采集恢复后见 seed 6/6、dialogue pack 与双 life 轨 hash 正常。因采集在 uptime≈25s 才打开，启动 marker 未捕获；不得把“烧录成功”冒充网络掉线恢复已验证。

# 2026-08-17 s1es — 同情绪跨 TTS 代际重新提交

- 现场证据：同一云 session 内连续 `天气怎么样` 都收到 `emotion=happy`。首次提交后有 StaticHold/嘴/life；后续请求因 `committed=happy` 被字符串去重，未再次 commit。新 session 首次恢复，坐实生命周期而非素材或音量问题。
- 根因：Presenter 的 requested/pending/committed 缺少 TTS turn generation；监听边沿已把画布恢复 standby，但 committed 字符串仍保持旧 happy，形成逻辑态与渲染态分裂。
- 唯一行为变量：Listening→Speaking 新边沿递增 generation，pending/committed 按「emotion+generation」去重。同代消息仍覆盖/去重，跨代同 emotion 重新执行一次 StaticHold 并武装 layered 嘴/life；不启用 enter arc。
- 验收：连续三次同问题须各有新 `speech_gen`、对应 `commit ... gen` 与 `mouth_arm/blit`；无 `FACE_ARC/FACE_PRIME`、无过期补播、音频和第二轮唤醒不回退。寿命仍按 marker n≥4 判断。
- 交付：`boot_trace_v10_s1es_emotion_turn_generation` 已编译、归档并完整烧录，全部分区 hash verified；启动日志已见 exact marker、`SD_MOUNT_OK`、`seed_stills 6/6`、dialogue pack 与双 life 轨 hash 正常，`PANIC_LAST none`。尚未把烧录成功冒充连续三问 M+，等待真机交互裁决。

# 2026-08-17 s1et — angry/sad 公共 standby 退场中枢

- 证据：当前 v5 hold→standby 像素差 angry=26.07、sad=23.32；原生 exit 末段降至14.20/13.77。六表情均有跨度，但 loving收益约4%，禁止做30套两两过渡。
- 素材刀：工具新增 `release_layer`，仅 angry/sad；序列为闭嘴→眉眼/嘴交替释放，共5帧、90ms、每帧≤48行，全部预合成到对应 canonical hold。v6 对当前 v5 六个MJPEG/hold_base逐一hash守恒，验证6/6。
- 固件刀：Listening/Idle 边沿将共享timer从mouth转交release；每tick只提交一带，结束再走原standby bookend。缺包、hash不符、锁失败或状态抢占均回原s1es路径；不解JPEG、不启全屏enter/exit、不改AFE/Opus/会话。
- R0=`l_release`；M+=`release_bind`、`release_arm`、5次有序`release_blit`后正常聆听/退出，肉眼跳变减弱；M-=字幕/聆听明显迟滞、任何音频/唤醒回归、WDT/panic或寿命均值回退。
- 交付：v6最终包已复制到`C:\bake\mjpeg_ai_dialogue_v6_release_hub`；BIN/ELF已归档，完整镜像逐分区烧录且hash verified。日志采集在uptime约90s恢复，旧v5包兼容路径下AFE与idle life健康；未捕获启动marker，也未部署v6，故release真机门仍待物理冷启。
# 2026-08-17 s1et 视觉裁决 M- → s1eu standby common hub

- 真机证据：angry 入口仍是 `static_hold_done full=1`，约 70ms 由 standby 直接整帧跳到 angry；退出侧 `release_arm` 后 5 个 `release_blit` 全部 `ESP_OK`，但结束仍执行 `close_seed standby`。用户观察到入口跨度大、出口花糊，与日志一致，故 s1et release v1 判视觉 M-，不得继续仅靠延长旧 release 帧数。
- 根因：v6 小带预合成在 angry/sad 原生 hold 上，而原生 hold 与 standby 存在全局头身位移；局部释放后的混合画布不是 standby，最后必然还要全屏替换。问题是**基底/几何不一致**，不是帧数不足或锁失败。
- s1eu 唯一变量：PC 将 angry/sad 的眉眼、嘴部语义以活动岛羽化到 standby canonical 底图；生成 8 帧 enter + 8 帧 exit，75ms/帧，每 tick 一条≤48行带。enter 最终逐像素等于 emotion hub，exit 最终逐像素等于 standby，因此固件跳过原生强情绪全帧 seed，并在 exit 后不再补全屏 standby。
- PC 包：`mjpeg_ai_dialogue_v7_common_hub`，原始六个 MJPEG 不重编码；验证6/6。离线重放 angry/sad 的 enter/exit 终点均为 `mismatch=0`。
- 固件 marker：`boot_trace_v10_s1eu_standby_common_hub`。R0 继续复用 `S1ET_L_RELEASE_HUB`；旧包无 `enter_layer` 自动走 s1es/s1et兼容路径。未改 AFE/Opus/会话/提醒/vendor。
- M+：`enter_bind→enter_arm→enter_blit×8→enter_done exact_hub=1→mouth_arm`；退出 `release_blit×8→release_done exact_standby=1`，其间不得出现 `close_seed`，肉眼无首帧猛跳/末帧花糊。M-：强情绪语义过弱、ROI仍见矩形、任一音频退化/复位或退出仍有 full bookend。稳定性仍按 per-marker n≥4，不因一次观感通过宣称过门。
# 2026-08-17 s1ev — s1et/s1eu 双实验回滚

- 用户真机裁决：v7 上天气播报嘴部不动；angry 起始与终止均花糊。s1eu 由观感实验升级为基础功能 D2 回归；结合 s1et 已证“小带全部ESP_OK但末尾仍花糊”，低成本退场轨与共底双向轨均停止推进。
- 决策：回退到 s1et 之前的 s1es 行为，不再调 release 帧数/间隔/羽化。固件 R0 `S1ET_L_RELEASE_HUB=0`，配套 SD 强制恢复 `mjpeg_ai_dialogue_v5_activity_feather`；v6/v7 仅保留为失败实验备案，禁止部署。
- 保留 s1es 的 TTS turn generation 修复，因此同 session 重复天气应每轮重新 `commit→mouth_arm→mouth_blit`；不回退会话、音频、提醒、canonical、idle small-life。
- marker=`boot_trace_v10_s1ev_s1es_visual_reanchor`；构建通过。BIN SHA256=`1AAE78EAC67C4CE3D4905501F2C73DAA94BFC08F6C8FD95DB5C5F59BEA088F6F`，ELF SHA256=`EC6FE907E07114E7C14EC490AC1C7075899175A61F3475564BEB50CC478F2568`。未烧录/冷启，不得写已恢复。

# 2026-08-17 寿命审计口径修正与 s1ev 上板门

- 诊断工具缺陷：`reboot_audit.py` 的自然失败集合遗漏 `INT_WDT`，导致 s1es 的 9975.9s 中断看门狗未计入失败样本；根目录与 `tools/stability/` 副本均已补入，固件行为未改变。
- 全量重扫校正：s1cm=`n14/mean2053.6s`；s1es=`n2/mean5933.2s/min1890.5s/max9975.9s`。s1es 均值暂高于基线，但 `n<4`，只记趋势、不判稳定通过。
- 活跃真源分裂：板上仍为 s1et，源码/构建产物为 s1ev；板上日志另见 `release_reject ... count=8`，说明固件与SD契约未对齐。禁止继续叠加动画实验或把 s1et 现场归因给 s1ev。
- 下一唯一集成门：s1ev + v5 冷启 exact marker 后，按顺序验连续两次天气均有嘴部变化、angry 不花屏、正常退出、3秒内第二轮唤醒；失败即停在该单变量归因，不进入新退场轨。

# 2026-08-17 FACE_SD 0x80070570 修复

- 否证源包：`mjpeg_ai_dialogue_v5_activity_feather` 460 文件、46,512,681 bytes 全量实际读取，`BadFiles=0`。
- 介质现场：FAT32 `FACE_SD (D:)` 的目标目录不可恢复，CHKDSK 发现坏目录项、坏簇链与约 93.7MB lost chains；`/F` 后续传写入再次在多个目录复发 `ERROR 1392/0x570`，坐实目标文件系统/介质路径而非某个素材文件。
- 修复：完整格式化 2GB FACE_SD 为 FAT32，再从 `mjpeg_ai_sd_ready_v5` 单次恢复 `/mjpeg` 与 `/dialogue_v2`。结果 466/466，源/卡 SHA-256 mismatch=0；写后 CHKDSK 无错误。
- 剩余门：安全弹出、装回设备、物理冷启；须见 `SD_MOUNT_OK`、`seed_stills done ok=6/6` 与 v5 pack bind。若短期再次出现 0x80070570/簇链损坏，直接更换 SD 卡或读卡器，禁止继续循环修补。

# 2026-08-17 v5 冷启短门与联合验收器

- 修复卡装回后的冷启日志见 `SD_MOUNT_OK`、`seed_stills done ok=6/6`。同一会话三段 TTS 分别出现 `speech_generation=1/2/3`，每代均重新 `mouth_arm`，共见6次 `mouth_blit`；随后 `channel_closed` 并回 idle，短窗无 `REBOOT_AFTER`/panic。
- 该证据通过“重复同情绪不衰减”和“正常退出”短门；采集错过本次 `FW_MARKER`，且退出后尚无第二次 listening/TTS，所以不能确认 s1ev 上板或第二轮唤醒通过。
- 新增只读 `tools/stability/s1ev_acceptance.py`：marker 不匹配判 FAIL、漏采判 PENDING；任一 panic/reboot、SD/seed/重复嘴部/退出缺失判 FAIL。当前裁决 `PENDING(marker_exact, second_wake)`。

# 2026-08-17 s1ev 完整烧录

- 写入前 `build/xiaozhi.bin` 与归档 s1ev BIN SHA-256 均为 `1AAE78EAC67C4CE3D4905501F2C73DAA94BFC08F6C8FD95DB5C5F59BEA088F6F`；匹配 ELF 已归档。
- 单次 esptool 写入 `0x2000 bootloader`、`0x8000 partition`、`0x10d000 otadata`、`0x200000 ota_0`、`0x700000 ota_1`、`0xc00000 assets`，过程无 timeout，六段写入 hash verified。
- 独立 verify：bootloader/partition/ota_0/ota_1/assets digest matched；otadata 因烧录后启动由 bootloader 写入 OTA 选择记录，与 `ota_data_initial.bin` 不再相同，属预期运行态变化。COM7 monitor 已恢复。
- 仍未通过 V2：USB hard reset 后必须物理断电冷启，捕获 exact s1ev marker、SD mount 与 seed 6/6；不得把烧录成功写成上板验收通过。

# 2026-08-17 s1ev 第二轮唤醒门校正

- 旧验收器用“退出 idle→下一 listening”作为3秒门，会把用户何时再次说唤醒词计入设备延迟，属于错误指标；日志中的7279ms不得作为唤醒慢证据。
- 校正为：必须先有一次 idle channel close，再以第二轮 `wake_word_detected` 为起点，到随后 `STATE: listening` 为终点。本轮为 `178419→178628=209ms`，通过3秒门。
- 当前同段证据：generation 1..11、mouth_arm=13、mouth_blit=49、channel_closed=6、panic/reboot=0；联合门仅剩 exact s1ev marker，长稳仍需 n≥4。

# 2026-08-17 s1ev 烧录后功能/集成短门

- 烧录后 monitor 从设备启动数秒后才恢复，捕获 `seed 6/6` 但漏掉 `FW_MARKER` 与 `SD_MOUNT_OK`。该日志段内见 generation 1..7、5次 mouth_arm、29次 mouth_blit、正常退出；idle `200329ms`→下一 listening `203235ms`，二次唤醒延迟2906ms通过3秒门，且无 reboot/panic。
- 修正 `s1ev_acceptance.py`：若最新 `MONITOR START` 晚于旧 SD mount，禁止跨段借用旧启动证据；marker/SD 日志漏采均报 PENDING，错误 marker 仍报 FAIL。
- 当前联合裁决 `PENDING(marker_exact, sd_mount)`；功能/第二轮短门通过不等于 V2 冷启通过。下一步仅需采集开启时物理断电冷启，不改固件与素材。

# 2026-08-17 P-seam — 四表情峰值嘴层离线软接缝

- 用户本轮明确跳过长泡；该决定只允许继续离线/灰度工作，不得把 s1ev 的 exact marker 或 per-marker n≥4 寿命门记为通过。
- 假设：v5 对所有嘴形使用同一 7px 活动区扩张与羽化，medium/large 峰值嘴形更容易暴露方形边界。唯一变量为 standby/neutral/happy/loving 的 medium/large 嘴层扩大软过渡区；不改 ROI、帧选择、原始 MJPEG、固件协议、眼/life、sad/angry。
- 工具新增 `--max-mouth-soften`、`--skip-strong-hub`、`--skip-release-layer`。后两项明确阻止已判 M− 的 s1et/s1eu release/common-hub 产物偷偷进入新候选；默认路径保持兼容。
- 候选 `mjpeg_ai_dialogue_v5p1_p_seam_candidate` 校验6/6；与当前 v5 均为460文件，missing=0、extra=0、六个 `frames.mjpeg` hash差异=0。媒体差异严格为四个目标表情两套pose下 medium/large 的 JPG+RGB565，共32文件；sad/angry及其他非目标媒体差异=0。
- 接缝分数 standby/neutral/happy/loving：`0.197/0.916/1.862/0.489 → 0.147/0.916/1.578/0.306`。离线预览未见新增水平线或错位，但这不能代替480×480真机观感。
- 裁决：候选仅完成 PC 侧体验工程与集成边界，不换 SD、不改固件、不编译烧录。v5仍是当前回退基线；后续若上板须单独验收四表情方块感、嘴延迟/闭嘴、重复TTS、字幕/音频与第二轮唤醒，长稳另行补门。

# 2026-08-18 s1ew — 无 cloud emotion 的 Speaking 首轮嘴部兜底

- 现场证据：三轮连续天气语境中，唯一缺 `mouth_arm` 的 `speech_generation=4` 从 Speaking 到 TTS stop 全段没有 `type=llm emotion`、pending、commit、StaticHold、arm 或 blit；Presenter 保持上一代 `committed=happy,cgen=3`。下一条 happy 明确绑定 generation 5。其他 generation 均在 commit 后约103–141ms完成 arm，故根因不是 SD、音量、晚到emotion串代或同情绪去重，而是云端该段未发 emotion，固件却把嘴部武装硬依赖 emotion commit。
- 唯一功能变量：`NotifySpeechState()` 在 Listening→Speaking 边沿返回新 generation；若当前没有 pending emotion，先提交 standby bookend，再以当前v5 standby layered包 provisional武装嘴部。后到emotion仍按同代 pending/commit重绑；Presenter在新代开始时显式迁移旧 pending，避免跨代悬空。
- 同批稳定性护栏：`FaceMouth_BindEmotion()` 会释放/替换patch，而face worker异步读取原始patch指针；provisional路径提高重绑概率，因此给bind/clear、mouth worker与可能尚在途的idle-life读取加同一递归互斥。`ArmMouthFollow()`改为返回真实成功值，只有实际bind/timer成功才打印arm，失败明确skip，禁止假阳性。
- R0=`S1EW_M_PROVISIONAL_MOUTH=0`；M+=无emotion的TTS仍有 `!!FACE_S1EW provisional_mouth=arm gen=N` 与 mouth blit，后到emotion同代重绑成功；M-=打印arm却无blit、patch mutex持续drop、音频/字幕/唤醒退化、接缝增加或任何panic/WDT。
- 构建：1311步完整构建成功；补齐idle-life在途读取锁后，9步增量重编译成功。最终应用大小`0x45db30`，分区余量13%。BIN SHA256=`2715059C16C23393B4B827BDF48774804810C4D6D710754D8E3BAFA593980C13`，ELF SHA256=`EC69166370EDF264879CE7B386AA273C5406D00F0954DA59F115CC0357FC72B8`；已归档`fw_archive/xiaozhi_s1ew.bin`与`elf_snapshots/xiaozhi_s1ew.elf`，二次hash一致。尚未烧录/冷启，不能声称真机解决；SD继续使用v5，无需更换。angry首尾跨度与M2素材几何问题不在本批。
- 烧录：2026-08-18 单次连接COM7成功，以460800 baud一次写入`0x2000 bootloader`、`0x8000 partition`、`0x10d000 otadata`、`0x200000 ota_0`、`0x700000 ota_1`、`0xc00000 assets`；六段均显示`Hash of data verified`，无timeout、无重试，最终RTS hard reset成功。USB复位不替代物理冷启；尚须捕获exact s1ew marker、`SD_MOUNT_OK`与`seed_stills done ok=6/6`。

# 2026-08-18 P-life-semantic — happy 头发误动语义门

- 真机观感：天气/happy 嘴部动画已可接受，幅度仅略小；主要低频生命感来自右侧脸边头发持续重复运动，注意力主体错误。嘴幅与life语义是两条独立问题，本批不调嘴幅。
- 素材根因：happy auto track0 ROI=`128,40 288x48`、motion_score=4.501，覆盖上方头发/饰品；track1 ROI=`24,384 448x48`、motion_score=3.555，提供下方身体/衣饰微动。旧工具把运动分数直接当发布许可，缺少“这个部位是否适合持续动”的产品语义门。
- 工具修复：`life_track_policy.approved_auto_tracks`与`--life-approved-tracks EMOTION:IDS`将自动轨降级为候选；manifest写入`semantic/approval`。不同素材必须重新看预览并批准，固件禁止硬编码固定track id。长期契约已增加“头发/饰品/背景单一区域持续重复运动默认拒绝”。
- 候选包：`mjpeg_ai_dialogue_v5p2_semantic_life`校验6/6，只批准happy track1。与v5相比六个原MJPEG一致、共有的RGB565/A8/MJPEG媒体hash差异0，仅缺少happy track0的9组RGB565+A8共18文件；其他表情、嘴层、刷新节拍和固件均不变。
- 裁决：S1/D0/R0/V1离线门通过；当前SD未更换，设备行为未改变。若用户选择V2，整包替换`/sdcard/dialogue_v2/`后物理冷启；M+=天气长TTS头发不再循环而仍有轻微身体生命感，M-=生命感消失、嘴/字幕/音频/第二轮回退。

# 2026-08-18 s1ex — 嘴部 medium 轻微增益

- 用户验收 s1ew 天气嘴部“能接受，只是幅度小一点点”；本刀不扩大嘴部ROI、不提高80ms刷新频率、不改small/large门槛，也不部署P-life-semantic头发候选。
- 唯一变量：音频mean-abs映射的`medium`门槛从1100降到1000；`small=400`、`large=2200`及release迟滞保持。故只增加中等嘴形命中率，不直接放大到large，理论上不增加单tick行带数或定时刷新次数。
- R0=`S1EX_N_MOUTH_GAIN_SOFT=0`；M+=天气长播报中medium略多但节奏与接缝不恶化；M-=嘴部重新过密、最大开合方框更醒目、音频/字幕/二次唤醒退化或复位。稳定门仍须per-marker，短观感不能代替寿命。
- 构建通过，marker与`!!FACE_S1EX`均存在于BIN/ELF；应用大小`0x45db80`、余量13%。BIN SHA256=`44EBD08F5EA817FE720A74F2F30857445DBB9D12F68A2914700D58E0FF5246E4`，ELF SHA256=`9364F02D563E8E31F73740D7A4126B27C4D0CA0040D900B28B5F7FFB9CDF92FB`；归档二次hash一致。
- 烧录：2026-08-18 单次连接COM7，以460800 baud完整写入bootloader、partition、otadata、双OTA与assets；六段均`Hash of data verified`，无timeout、无重试，最终RTS hard reset成功。SD无需更换；仍需物理断电冷启确认exact marker、`SD_MOUNT_OK`、`seed_stills 6/6`并做V2嘴幅裁决，当前不得声称真机观感已通过。

# 2026-08-18 s1ey — 嘴部/头发生命轨视觉权重重平衡

- s1ex真机M−：用户确认嘴部仍不够，头发动画感强于嘴部。该反馈否证“只降medium门槛足以改善幅度”；档位命中率与素材几何、life语义必须分开处理。
- 固件O刀：large mean-abs门槛`2200→2000`，medium维持1000；不改ROI、80ms节拍、pose文件或单tick行带数。独立R0=`S1EY_O_MOUTH_LARGE_GAIN_SOFT=0`。
- 素材语义刀：standby track0=`120,76 312x48`、happy track0=`128,40 288x48`均属上半部头发；二者只保留下半部track1=`24,384 448x48`。候选`mjpeg_ai_dialogue_v5p3_mouth_focus`验证6/6；对v5的282个共有媒体hash差异0，仅删除两条track0共36个RGB565/A8媒体，禁止把效果归因成嘴素材变化。
- 构建通过并归档：marker=`boot_trace_v10_s1ey_mouth_large_semantic_life`，应用`0x45dbe0`、余量13%；BIN SHA256=`8A6549595F0B627EFCD7D760072D268DE4FBE3BB2C95C356361C4736F7E9E0E7`，ELF SHA256=`AF824FA8D36780C78DDC5844E2F69718480421E9FFC5A6474845E9EE71631EDA`，归档hash一致。
- 烧录：2026-08-18 单次COM7完整写入bootloader、partition、otadata、双OTA、assets，六段均`Hash of data verified`，无timeout、无重试，RTS hard reset成功。素材是否替换尚未确认；物理冷启前不得把SD或观感判为通过。
- M+=天气播报中large略更常出现且头发不循环、下方仍有轻微life；M-=嘴过密或接缝扩大、生命感全失、音频/字幕/二次唤醒退化、panic/WDT。需配对换包+烧录+物理冷启V2；稳定仍按per-marker n≥4。

# 2026-08-18 08-16/08-17 CRASH marker校正与 s1ez life-DMA 稳定性刀

- 按带日期CRASH块去重后确认11起自然失败。08-16五起依次归属：00:01/01:43/02:29/03:42=`s1dm_standby_close_retry`，05:19=`s1dq_face_window_local_end`。08-17六起：02:09=`s1eo`，05:12/05:39=`s1ep`，07:54=`s1er`，10:58/11:30=`s1es`；DS把最后两起写成s1er是归属错误。故“MQTT_END 3起”横跨s1er/s1es，不能作为单marker回归。
- 三起 `prev_phase=MQTT_END` 均在MQTT失败后继续正常运行，复位前最后活动为idle life小图；`MQTT_END`只是旧breadcrumb。10:58 INT_WDT用匹配s1es ELF解析：CPU0 `esp_cpu_compare_and_set→spinlock_acquire→xQueueSemaphoreTake→AfeFetchGateEnter→AudioDetectionTask`；Core1在SMP tick/spinlock。RTC显示AFE从life frame2后进入WAIT_GATE约0.87s，而face仍连续提交frame3..5，flush active=0。
- 适用范围分流：08-16的s1dm/s1dq仍是旧400行/整帧暴露族；08-17的s1eo与s1ep首两起分别早于small-life收口和enter关闭，当前s1ey已包含其后继结构修复。s1er/s1es三起使用的small-life同步缺口仍存在于s1ey，故本批只认领这一个仍存根因，不宣称一刀消灭全部11起或根治MSPI-751。
- 根因窗口：small-life旧路径持AFE门写canvas但在LVGL invalidate后立即解锁，未像全屏路径等待LCD final-DMA；audio/face同为prio3，AFE等待期间视觉tick仍可连续获门。它不是关闭动画的理由，而是“音频优先+DMA所有权”契约未落实。
- s1ez唯一稳定性变量由R0 `S1EZ_P_LIFE_AFE_FENCE`统辖：visual TryLock改0等待；每个life帧 `prepare fence→invalidate→unlock LVGL→wait final-DMA` 后才释放AFE门；face worker prio 3→2，audio保持3；fence timeout帧丢弃不补播；逐帧日志移至AFE门释放后。嘴幅、80ms说话节拍、emotion、会话和SD媒体均不改。
- M+=启动见 `FW_MARKER ...s1ez...`、`!!FACE_S1EZ ...p=1`、`face_worker ...prio=2 audio_prio=3`，idle life持续且日志 `gate=released`，AFE fetch/唤醒/TTS无回退；M-=连续`ESP_ERR_NOT_FINISHED`、life停止、音频欠载/第二轮失败或任何panic/WDT。稳定性仍以s1ez per-marker n≥4/mean不低于s1cm裁决。
- 集成工具同批修正：两份`reboot_audit.py`遇到新`MONITOR START`会清空旧marker/uptime，禁止跨采集段借marker；`tools/stability`副本修正日志根路径；`s1ev_acceptance.py`在monitor中途恢复且无mount边界时返回PENDING而非伪FAIL。三脚本已通过`py_compile`，当前无冷启边界的验收结果为PENDING。
- 构建门：独立`build_s1ez`完整构建通过，应用`0x45dbf0`、分区余量13%；二进制已确认包含exact marker、`p_life_fence`与`!!FACE_S1EZ`。归档`fw_archive/xiaozhi_s1ez.bin`/`elf_snapshots/xiaozhi_s1ez.elf`；BIN SHA256=`77C76D57CF4613987602C98924C35250A390CC4CA8CD0122098803F2F7723C2A`，ELF SHA256=`DBA5AC1499D64514D6739BB447BF0539B9B54F35B2BF38E3D05B1D3285BB0770`。尚未烧录/冷启，不得宣称稳定性已解决。
- 烧录门：2026-08-18对COM7单次完整写入bootloader、partition、otadata、双OTA与assets，六段均`Hash of data verified`，无timeout/重试。恢复采集后见`s1ez idle_life_blit ... err=ESP_OK gate=released`、`wait_ms=0`及AFE_DIAG连续；因监控晚于启动恢复，exact `FW_MARKER`/SD seed门仍待物理冷启补证。当前只判“新路径已运行”，不判稳定性通过。

# 2026-08-18 v5p4 mouth-focus soft-seam 离线合流

- 边界：夜间长泡继续使用板上`s1ez + v5p3`；本批不改`main/**`、不换SD、不编译烧录，不污染s1ez寿命变量。
- 候选=`mjpeg_ai_dialogue_v5p4_mouth_focus_soft_seam`，由v5精确真源一次重建，同时显式启用`--max-mouth-soften 1.5`、`standby:1`、`happy:1`，并继续禁止strong-hub/release失败实验。validation=6/6，424文件/45,727,234 bytes。
- 对当前v5p3逐媒体SHA-256：两边各378文件，missing=0、extra=0、diff=32；差异只在standby/neutral/happy/loving两套pose的medium/large JPG+RGB565。sad/angry diff=0，六个MJPEG不变，standby/happy life各18文件且hash diff=0。故后续V2可把“方框感变化”归因给软接缝，不与头发生命轨混判。
- manifest SHA256=`C86C69911859A4150FB60186628691D32E2FE467EE8608F99E5FFD27DFE78E06`；当前只完成离线门，禁止复制到SD冒充体验通过。s1ez夜间寿命无回退后再部署，重点验四表情最大嘴形边界、重复TTS、字幕/音频、退出和第二轮唤醒。
- 集成验收器不再写死s1ev marker：默认读取`main/reminder/boot_trace.cc`真源；s1ez附加检查`p=1`、face prio2/audio3、life解锁后提交、AFE诊断连续及fence timeout上限。现有日志：generation=1/2/3、mouth_arm=6、mouth_blit=17、normal_exit通过、life gate=38、AFE_DIAG=78、fence timeout=0、panic/reboot=0；仅exact marker与实际second wake待补，裁决PENDING。

# 2026-08-18 P-contract-v2 — manifest整包契约硬化

- 边界：夜间长泡仍是板上`s1ez + 当前SD包`；本批只改PC工具、生成新候选并做离线测试，不改`main/**`、不换SD、不编译烧录，稳定性样本变量不变。
- 假设：旧builder只逐clip校验，不能在复制SD前识别pack入口/路径逃逸/ROI越界/固件行带上限/未审核life语义等跨文件不一致。新增`dialogue_pack_contract.py`，把六表情入口、相对路径、480×480 RGB565、MJPEG连续索引与SHA、嘴/眼/life尺寸和hash、canonical base、pose/transition、每tick≤48行且单层统一为可执行门；builder固定输出`runtime_contract`、显式legacy根`/sdcard/mjpeg`与`contract_report.json`。
- 自动反例4/4：合法最小包通过；`../`路径逃逸、ROI越界、RGB565尺寸错误均准确拒绝。现有v5p4硬契约`errors=0`，同时显式报告neutral/sad/angry/loving共8条`auto_unreviewed`语义警告，证明探针能发现此前隐性风险。
- 候选`mjpeg_ai_dialogue_v5p5_contract_hardened`：standby/happy继续只批准track1；neutral/loving/sad/angry使用显式空批准，不把运动分数冒充产品语义。输出validation 6/6、contract errors=0/warnings=0。对v5p4共有234个媒体SHA完全一致、六个MJPEG一致；仅删除四表情各36个未批准life RGB565/A8文件，无新增媒体。
- 裁决：`S1/D0/R0/V0`离线门通过，工程综合进度71→72%，板上体验仍按69%。该包在s1ez寿命门通过前禁止部署；未来V2只裁决“非happy表情不再出现无语义局部乱动”，嘴部/字幕/音频/第二轮不得回退。

# 2026-08-19 s1ez 夜泡归因与 s1fa MQTT-idle 预算门

- 夜泡事实（22:00后）：s1ez自然失败2起。22:31:45为Core1 LoadAccessFault，上一轮寿命2557s，`prev_phase=MQTT_CONN_BEG`；23:34:05为纯HP WDT，上一轮寿命3733s，`prev_phase=MQTT_END`。第二次复位后右删失存活约3.95h。s1ez当前仅`n=2`且已有失败，禁止用最后一段长存活宣布稳定。
- PANIC按归因序：匹配`s1ez` ELF SHA256=`DBA5AC1499D64514D6739BB447BF0539B9B54F35B2BF38E3D05B1D3285BB0770`解析，`mepc=0x4ff0f40a`与`ra=0x4ff0f3e8`均落在`xTaskIncrementTickOtherCores`（`freertos_tasks_c_additions.h:108`）；故障指令以当前TCB的`uxPriority`乘20索引`pxReadyTasksLists`，`mtval=0x45a1b08c`表明任务控制数据/优先级索引已坏。`mtval`不近`sp=0x4ff1e9d0`，cache_on=1、flashop_depth=0、heap余量稳定，不能把MQTT breadcrumb直接解释成MQTT业务空指针。
- 同窗证据：崩前`FACE_BREATHE skip_afe`虽然限频打印，但真实重试仍为120ms，计数已到约1.88万；折算约2.8万次Core1低价值唤醒/小时。panic前约1.1s存在`s1ez idle_life_blit ... gate=released`。第二起在MQTT connect失败/`MQTT_END`后触发纯WDT。证据支持“idle-life准入重试与MQTT AT连接窗口重叠是调度/总线暴露放大器”，但不声称它是硅片病或所有内存踩踏的唯一根因。
- s1fa唯一变量：新增独立MSPI预算互斥。提醒MQTT的Connect/Subscribe/IsConnected/Disconnect阻塞持有；idle-life只try-lock，遇忙丢当前帧并以900ms重试。门覆盖现有AFE锁、patch读取、present与final-DMA fence的完整低频视觉事务；Speaking mouth故意不使用此门。互斥量初始化失败时fail-open，网络RAII只有真实持有才释放，避免未持有释放。
- 四问：`S1/D1/R0/V3`。R0=`S1FA_Q_MQTT_IDLE_QUIET=0`。M+=exact marker=`boot_trace_v10_s1fa_mqtt_idle_quiet_gate`、启动`!!FACE_S1FA ... q=1 retry_ms=900`；`MQTT_CONN_BEG..OK/END`窗口若有life请求，只见`!!FACE_S1FA q=mqtt_skip`而无`idle_life_blit`；窗口结束后life恢复；Speaking仍有`mouth_arm/mouth_blit`，AFE/TTS/STT/第二轮唤醒不退化。M-=窗口内仍出现life blit、窗口外life永久冻结、音频欠载/字幕/二次唤醒回退或任何panic/WDT。
- 构建：fresh `build_s1fa`完整构建及增量复核通过，应用`0x45df60`、分区余量13%；BIN包含exact marker与两条`!!FACE_S1FA`探针。归档`fw_archive/xiaozhi_s1fa.bin`与`elf_snapshots/xiaozhi_s1fa.elf`，二次hash一致：BIN SHA256=`CD22D87B035A93D7489E483EAA4D2A4B192B30F7B9FC898637781923A458109C`，ELF SHA256=`4EEDD0FB85BE3204D7B481B1FEE55FDF94D0780CCC94152F64D4D8C8EE19D333`。尚未烧录/冷启，不得声称真机修复；寿命门为s1fa per-marker `n≥4`且mean不低于s1cm≈2054s。
- 烧录：2026-08-19占用COM7前按规则停止DS的serial monitor，随后以460800 baud单次写入`0x2000 bootloader`、`0x8000 partition`、`0x10d000 otadata`、`0x200000 ota_0`、`0x700000 ota_1`、`0xc00000 assets`；六段全部`Hash of data verified`，无timeout、无重试，最终RTS hard reset成功。USB复位不替代物理冷启；需DS恢复监控后断电冷启，补exact marker、`q=1/retry_ms=900`、`SD_MOUNT_OK`与`seed_stills 6/6`。

# 2026-08-19 s1fa 待机饥饿复判与 s1fb AFE phase-dither

- 冷启采集晚约63s接入，虽未捕获exact marker/SD seed边界，但`t=66.258s`出现`s1ez idle_life_blit track=0 frame=2/9 err=ESP_OK`，证明standby life素材、patch层和显示路径已运行；后续`s1fa idle_life_drop mqtt_window n=12..21`严格落在`MQTT_CONN_BEG..MQTT_END`，END后消失，证明q门正确释放、无网络锁泄漏。
- 反例：MQTT结束后约47s只有`FACE_BREATHE skip_afe n=33→74`且frame不再推进。900ms固定退避约等于AFE fetch约30ms周期的30倍，每次timer都回到同一busy相位；因此s1fa视觉M−为确定性相位饥饿，不是SD素材太弱、life未武装或MQTT互斥死锁。该轮是静态偏置负载，不计完整动画工作量寿命通过。
- s1fb唯一变量：保留MQTT miss=900ms；AFE miss改为确定性`731/887/1019ms`循环。三值跨常见20/30/40ms音频周期走相位，平均879ms，与s1fa约4k次/小时的唤醒预算基本相同，禁止恢复120ms约28k次/小时竞抢。成功后仍按260ms提交下一life帧，整轨结束仍休息4.2s；不改AFE任务、优先级、嘴部、情绪、素材或DMA fence。
- 四问：`S1/D1/R0/V2+V3`。R0=`S1FB_R_IDLE_AFE_DITHER=0`恢复s1fa固定900ms。M+=exact marker=`boot_trace_v10_s1fb_idle_afe_phase_dither`、启动`r=1 afe_retry_ms=731/887/1019`；AFE miss日志轮换retry，非MQTT窗口8–15s内life frame持续前进；MQTT窗口仍只有q skip；AFE/TTS/STT/第二轮唤醒正常。M-=frame再次长期冻结、平均尝试率明显高于s1fa、q窗口出现blit、音频回退或panic/WDT。
- 构建：`build_s1fa`增量14步及二次无源重编复核通过（目录名沿用，但内容marker为s1fb），应用`0x45dfe0`、分区余量13%。BIN含exact marker与两条S1FB探针；归档`fw_archive/xiaozhi_s1fb.bin`/`elf_snapshots/xiaozhi_s1fb.elf`，二次hash一致。BIN SHA256=`490B953132DE62525F0F0068E37E8980ECC5E2D2AA48F800260B21C29B7C4A6C`，ELF SHA256=`FDC1F478741854748210C3ED2DE5665179F9A6722457854DC7C1553048652D17`。尚未烧录/冷启，不得声称体验或稳定性已修复。
- 烧录：2026-08-19按monitor启动时间只停止COM7对应进程，未动后启动的其他DS任务；460800 baud单次写入bootloader、partition、otadata、双OTA与assets，六段均`Hash of data verified`，无timeout/重试，RTS hard reset成功。需DS恢复monitor后物理断电冷启；V2先验exact marker、`r=1`、SD seed 6/6、8–15s内life帧前进，再进入V3长泡。
- 冷启V2：monitor在uptime约31s接入，未捕获启动marker/seed边界，但`s1fb retry_ms=731/887/1019`、MQTT窗口q drop与5次`idle_life_blit`证明固件/素材/两层门均运行。约5分钟只推进5帧，92条限频skip对应AFE长期占门；肉眼静态与日志一致。裁决M−：错相修复了固定周期共振，却未达到8–15s持续推进门。禁止再调retry常数或恢复120ms；下一候选改为AFE返回安全点的低频、单帧、超时可丢令牌，且必须以音频健康和非MQTT为前置。
# 2026-08-19 s1fc — idle-life AFE释放边沿令牌

- s1fb冷启短验：约5分钟仅5次`idle_life_blit`，大多数tick仍在`FACE_BREATHE skip_afe`；错相轮询只偶尔撞中空隙，肉眼仍近静止。MQTT窗口内q-drop、窗口后回AFE skip，说明网络门有效，主因是WakeNet fetch几乎连续占门。
- 唯一变量：idle tick撞AFE后不再启动731/887/1019ms定时重试，而登记一个latest-value pending；`AfeFetchGateLeave()`先释放互斥，再以900ms最小间隔调用非阻塞回调，仅向len=1的Face Worker队列投令牌。worker仍prio2、零等待抢AFE/MQTT门，失败即丢；回调不解码、不绘制、不打印、不等待。
- R0=`S1FC_S_AFE_EDGE_TOKEN=0`恢复s1fb错相轮询。M+=exact marker、`s=1`、`edge_wait`后idle life能按秒级持续推进且MQTT窗口无blit；M-=仍数分钟才一帧、音频/STT退化、窗口内视觉泄漏或任一panic/WDT。寿命仍须per-marker n≥4，不因idle变动宣称稳定。
- V0：增量构建成功，应用`0x45e1a0`、分区余量13%。BIN SHA256=`E207CAE426815B4BA0D3520F69920C6F87806229B7DD15DF6983DC9517492C14`；ELF SHA256=`D078B396B8E42ED56176B85E452F8BB02F512147C7625D81222989C5D624A2A8`；已归档。烧录与物理冷启V2尚待本批后续记录。
- 烧录：按`serial_monitor.meta`启动时间只停止COM7对应PID，未动另外三个DS/稳定性Python进程；460800 baud单次写入bootloader、partition、otadata、OTA_0、OTA_1与assets，六段均`Hash of data verified`，无timeout/重试，RTS hard reset成功。USB复位不替代物理冷启；须由DS恢复采集后断电上电，先验exact marker、SD/seed 6/6，再看约10–20s内life是否持续推进。

# 2026-08-19 s1fd — PSRAM-free 待机背光呼吸

- 用户V2观感判定s1fc待机仍无动画感；同时采集器自05:09停写，现有日志只含旧s1fb，故不伪造s1fc运行链。但静态调度缺陷成立：prio3 AFE释放mutex后仅唤醒prio2 Face Worker，不等于转交所有权，AFE可立即重获门；禁止继续调retry、抬视觉优先级或人为暂停AFE。
- 唯一变量：`S1FD_T_IDLE_BACKLIGHT_BREATHE=1`时，`ArmIdleBreathe`改走背光PWM生命轨，不再启动idle patch/AFE令牌。24级包络、300ms一步、7.2s一轮；默认亮度最多下沉4个百分点，低亮度自动缩幅。只改LEDC duty，不触碰framebuffer、PSRAM、MIPI/LCD DMA、AFE或SD。
- 离开idle立即停止并恢复进入前亮度；若idle中外部修改亮度，下一tick把新值收作基准，避免与用户设置争夺。`SetBrightnessQuiet`不写NVS、不产生300ms日志风暴。R0关闭t恢复s1fc旧路径。
- M+=exact marker、`t=1`、`t=arm base=N`，肉眼有低幅整体呼吸；进入Listening/Speaking见`t=stop restore=N`且字幕/嘴部/唤醒无退化。M-=无呼吸、亮度跳变/闪烁、对话后亮度不恢复或任何音频/复位回归。长期仍按per-marker n≥4。
- V0：55步增量构建成功，应用`0x45e6c0`、分区余量13%。BIN SHA256=`F040606519566C5E8F3F7A8D96B0AFA42B4A2BDD1636F6131CEB1587FED16192`；ELF SHA256=`C5517660B53170A2BD14F632A8ACF9FD8CA3AE22AFDB65AEB699D2235B8B6A39`；已归档。烧录与物理冷启V2待本批后续记录。

# 2026-08-19 s1fe — MQTT/UDP边界防御（未完成链接）

- 从远端`agent/com4-proactive-reminder-stability`只吸收合法输入行为不变的防御思想，不复制UDP prime/重排/预缓冲或vendor/UI混合代码。当前真实缺陷为接收端用`sizeof(aes_nonce_)`衡量`std::string`对象而非16字节协议头，server hello多个字段直接解引用，发送端未校验空packet/nonce/payload上限，且UDP创建/连接失败仍可能继续报通道已开。
- s1fe固定音频头为16字节；server hello严格校验transport/server/port/32位hex key与nonce；上行拒绝null与>65535 payload；收发字段用`memcpy`替代未对齐强转；下行核对header payload_len与实际CTR密文长度；CreateUdp/Connect失败清理并返回false。
- 四问与门：`S1/D0/R2/V0→V2`。M+=正常STT/TTS/二次唤醒不变，畸形输入只产生guard日志；M-=合法包被拒、音频通道打不开或上下行退化。当前仅两改动对象经P4工具链编译通过；完整链接/归档/烧录均未完成，不能计入per-marker寿命或声称修复上板。
- s1fd烧录补记：COM7无serial monitor进程占用；460800 baud单次写入bootloader、partition、otadata、OTA_0、OTA_1及assets，六段全部`Hash of data verified`，无timeout/重试，RTS hard reset成功。DS须恢复采集后物理断电冷启；先验exact marker、`t=1`、SD/seed，再观察完整7.2s周期和对话入口亮度恢复。

# 2026-08-19 s1ff — 分支防御能力整合（V0）

- 用户授权吸收 `agent/com4-proactive-reminder-stability` 中当前工程可用的优化；仍遵守“禁止整包合入”与音频优先。整合项为：assets分区表/offset边界校验和按实际长度mmap；BQ27220缓存快照+LVGL电量UI；MJPEG 16KB internal staging分段搬入PSRAM、decode栈12KB；主动提醒4帧/420ms有界预缓冲并在播放队列排空后反馈/ACK；UDP transport-only prime与3包小窗重排；保留s1fe输入guard。
- 分流未合：vendor ML307 UART fork、12秒冷启与强制重启（无本机串口根因证据）；managed EEZ整包（会覆盖当前表情主线）；FATFS LFN heap（独占sdkconfig且无栈证据）；远程重复响铃（新产品语义）；硬编码endpoint。它们不是“遗漏”，而是当前不可安全归因/不属于同一防御目标。
- 四问：`S2/D2/R0~R2/V0→V2/V3`。关键M+为exact s1ff、SD/seed 6/6、普通三轮STT/TTS与3秒二次唤醒不退、主动提醒ACK晚于真实音频排空、无UDP reorder stall；M-为启动误关机、电量层遮挡、SD失败、首音/尾音退化、无STT/TTS、会话卡死或任何panic/WDT。
- 完整构建通过（应用+bootloader）：marker=`boot_trace_v10_s1ff_branch_hardening_bundle`，应用`0x461220`，分区余量12%。BIN SHA256=`BFEE4CAD65349EA68ABFEABC96562C1F177A444870EE0885D9197254DA2421C0`；ELF SHA256=`BEA5D7CF4DD0A927D0A1255EA96C581E0876CE56113DD37D7A36F2F1180BF4CF`。未归档、未烧录、未冷启，当前只能记V0；稳定性必须per-marker n≥4且mean不低于s1cm。
- 烧录：2026-08-19确认旧COM7采集在05:09已停写且无匹配占用进程后，以460800 baud单次写入`0x2000 bootloader`、`0x8000 partition`、`0x10d000 otadata`、`0x200000 OTA_0`、`0x700000 OTA_1`、`0xc00000 assets`；六段全部`Hash of data verified`，无timeout、无重试，最终RTS hard reset成功。USB复位不替代冷启；需DS恢复采集后物理断电冷启，先验exact marker、SD/seed 6/6、电量快照，再做普通对话/主动提醒门。

# 2026-08-19 s1fg — 视觉底图契约与强情绪停留（V0）

- s1ff真机日志把三个现象拆开：天气轮次连续收到约30次云端`tts sentence_start`正文`% get_weather...`，随后`tts stop`且零UDP音频；同一session后续普通回答可正常播音，UDP汇总`reordered/skipped/duplicate=0`，故不能归为本地扬声器或UDP回归。angry则已`static_hold_done/mouth_arm`，但云端仅发emoji情绪且立即零音频stop，Listening入口随即画standby，造成肉眼闪回。嘴/脖子矩形接缝来自MJPEG standby seed与layered pack canonical底图未证明像素同源，却以`base_already_present=true`直接叠patch。
- 唯一视觉契约批次：有效BQ电量取消开机30秒可见窗并常显；provisional Speaking改为`ArmMouthFollow("standby", false)`，由layered包先呈现canonical base一次，失败才回standby；angry/sad成功静态提交后保留至少1.2s，Listening期间只延迟视觉回standby，不阻塞采音。未改MQTT/UDP/AFE/SD素材和天气业务。
- 四问：`S2/D1/R0~R1/V0→V2`。M+=exact s1fg、电量计有效时常显、`provisional_mouth arm canonical=1`、多轮天气嘴/颈无矩形底图差、angry/sad即使零音频也至少可见约1.2s且麦克风继续Listening；M-=双全屏闪烁、字幕/音频/第二轮唤醒回退、强情绪卡住不退或任何panic/WDT。天气工具循环若复现仍归云端工具结果，不得用本批视觉刀认领。
- V0：`build_s1fa`完整应用重建并链接成功，生成应用`0x4614a0`、分区余量12%；BIN SHA256=`8E4148F4394CA990308C12E4A02A59FE5BB677A41E8A729662AAEC2F0D0B3B30`，ELF含exact marker与三条s1fg探针。默认目标在应用BIN生成后因旧bootloader子构建读取sdkconfig的GBK环境失败；不影响已成功生成的本轮ELF/BIN，但本轮尚未烧录/冷启，不得冒充V2。

# 2026-08-19 当前上板冷泡右删失证据

- `serial_monitor`于07:43:17接入COM7时设备uptime≈57s，推算本轮约07:42:20启动；至17:38尾部uptime=35765s≈9.94h。接入后按行号过滤`FW_MARKER/BOOT/REBOOT_AFTER/PANIC_LAST/rst/Guru Meditation`事件为0；meta实时为`running=1 state=connected`，日志持续写入，watch state=`crashes=4`且最后triage仍停在08-18 23:34旧WDT。
- 该段超过此前记录的6.8h无复位段，是正向稳定性证据；但启动marker和seed边界在接入前被错过，不能严格归属per-marker，且单条右删失样本不满足`n≥4`。综合工程口径仅75%（+1），当前上板口径71%（+2），不宣布寿命门通过；继续泡并补下一次完整冷启exact marker。

# 2026-08-19 s1fg 集成门与双槽烧录

- `tools/stability/s1ev_acceptance.py`扩展为当前marker验收器：s1fg额外检查BQ初始化、`provisional_mouth canonical=1`、strong hold arm/done及可见停留≥900ms；exact marker缺失时相关项只报PENDING，避免旧固件假阴性。连续`% get_weather...`≥3次独立分类为`CLOUD_TOOL_LOOP`且不归罪本地UDP。脚本`py_compile`通过；在旧s1ff尾部正确输出PENDING、91次weather placeholder、panic/reboot=0。
- 匹配归档：`fw_archive/xiaozhi_s1fg.bin` SHA256=`8E4148F4394CA990308C12E4A02A59FE5BB677A41E8A729662AAEC2F0D0B3B30`；`elf_snapshots/xiaozhi_s1fg.elf` SHA256=`5FE88589307BB8CE7856C1806B61B6DD5D3BA06DFFCEE84A2379CA46F626DD3C`。
- 烧录：精确停止07:43:17启动的COM7采集PID 2480；460800 baud单次写`0x10d000 otadata`、`0x200000 OTA_0`、`0x700000 OTA_1`，三段均`Hash of data verified`，无timeout/重试，RTS hard reset成功。bootloader/partition/assets无变化未重复写。工程准备度记78%，上板口径仍71%；DS恢复采集并物理冷启后，验收器全PASS才记80%，寿命仍独立要求per-marker n≥4。

# 2026-08-19 s1fh — BQ27220 冷启动有限重试（V0）

- s1fg冷启后用户询问电量，日志两次出现`self.battery.get_level`真实MCP调用，云端均回答设备查不到电量；因此MCP注册、意图与显示隐藏策略不是根因，`BatteryMonitor::is_ready()`为false。monitor在uptime约20.7s才接入，漏掉约9s初始化现场，不能虚构具体I2C错误码。
- 唯一变量：板构造早期、音频codec等其他I2C消费者启动前，BQ27220最多尝试4次；首次立即，失败后依次等500/1200/2500ms。驱动失败路径已核对会删除临时device handle并递减bus ref，重复尝试不累计设备；成功立即停止重试，失败保持unavailable。禁止运行期重试抢音频共享I2C。
- 四问与门：`S1/D0/R2/V2`。M+=exact `boot_trace_v10_s1fh_battery_cold_retry`，随后`BATTERY_INIT_OK attempt=N/4 soc=0..100`，UI常显且再次询问返回数字；M-=四次均fail并`GIVE_UP`、启动/音频延迟异常或任何I2C/AFE回归。失败只证明延迟不足或硬件无响应，禁止改UI伪造电量。
- V0完整重建通过：应用`0x461610`，5MB应用分区余`0x9e9f0`（12%）；归档`fw_archive/xiaozhi_s1fh.bin` SHA256=`8982D8FCD2DF7201CAAAF3F98881D3A76511B859ACC8D5C93F6E9ABAA2C0BB5A`，`elf_snapshots/xiaozhi_s1fh.elf` SHA256=`EAA36116EEC135169A895DBA9877B149C89DEB8AB5FCEC358E29DC17D93B14CC`。尚未烧录/冷启，不得声称电量已恢复。
- 烧录：确认并精确停止COM7采集PID 524（17:57:31启动），未动其他DS任务；460800 baud单次写入`0x10d000 otadata`、`0x200000 OTA_0`、`0x700000 OTA_1`，三段全部`Hash of data verified`，无timeout、无重试，最终RTS hard reset成功。bootloader/partition/assets未变化故未重写。USB复位不替代V2；DS恢复采集后必须物理断电冷启，捕获exact s1fh及`BATTERY_INIT_OK/GIVE_UP`，再询问一次电量。

# 2026-08-19 s1fi — 提醒服务离线失败退避（V0 + 双槽烧录）

- 复判事实：17:57后采集连续约2.1h没有新的`BOOT/PANIC/WDT/rst`，所以用户看到的“网络卡住复位”未被该监控窗证明为自然复位；确定异常是提醒服务不可达。日志约189次MQTT失败、25次HTTP失败，MQTT约40s固定循环；PC到`114.245.176.144:8443/8883`同样超时，支持外部端点离线。不能把网络breadcrumb直接当崩溃根因，但固定重试与HTTP重叠会放大ML307 UART/调度暴露。
- 唯一变量：MQTT任务启动先错峰16s；连续连接失败采用30/60/120/300s封顶退避并加0–15s抖动，成功后清零；长等待按1s切片并检查`running_`，保证Stop可及时退出。未改HTTP周期、MQTT/UDP会话通道、AFE、表情、SD、分区、第三方；电量链冻结，不再扩I2C/UI。
- 四问与门：`S2/D1/R2/V0→V2→V3`。H=固定失败重试和HTTP重叠放大网络窗口。M+=exact `boot_trace_v10_s1fi_reminder_fail_backoff`，首次见`MQTT_START_STAGGER`，失败后`MQTT_FAIL_BACKOFF`按阶梯增长且AFE/普通对话/二次唤醒连续；M-=仍约40s恒定风暴、提醒停止不可退出、合法恢复后不重连或同族panic/WDT继续。
- 构建：ESP-IDF 5.4.1完整构建通过，应用`0x461900`，5MB分区余`0x9e700`（12%）。归档`fw_archive/xiaozhi_s1fi.bin` SHA256=`2485408152B282FE6BD1F23AA707C1820535E1BAEA5B7E70BB0C5A3ABDB53D97`；`elf_snapshots/xiaozhi_s1fi.elf` SHA256=`91BDDFD9B4BD6B1F86AB4FAAE4220CB1B7188437BB76F7868D4BA6B5DBA61AB6`，与build产物逐一一致。
- 烧录：旧`serial_monitor.meta`显示connected但系统无实际采集进程，故未停止任何无关进程；460800 baud单次写`0x10d000 otadata`、`0x200000 OTA_0`、`0x700000 OTA_1`，三段均`Hash of data verified`，无timeout/重试，RTS hard reset成功。新monitor PID 18176已连接，接入时uptime约36s，AFE/heap健康但漏exact marker。USB复位不替代冷启；物理断电冷启后补marker、退避阶梯及长期per-marker寿命。
- 热启短门：已捕获`MQTT_FAIL_BACKOFF streak=2 base_ms=60000 jitter_ms=9765 delay_ms=69765`，下一轮为`streak=3 base_ms=120000 jitter_ms=11278 delay_ms=131278`，M+“失败间隔阶梯增长”成立；同期AFE每约5s连续、heap/PSRAM稳定，未见panic/WDT。exact marker、首次16s stagger、正常对话/二次唤醒与长期寿命仍待物理冷启后验，当前不得宣布稳定性修复完成。

# 2026-08-19 s1fj — 主动提醒首次连接即时恢复（V0 + 双槽烧录）

- 用户裁决：s1fi的16s首次连接错峰会延迟冷启后主动提醒上线，产品收益不足，必须撤销。s1fj删除`kInitialConnectStaggerMs`、启动等待及`MQTT_STAGGER`探针；MQTT任务启动后立即首次连接。服务离线后的30/60/120/300s封顶退避、抖动和可中断等待完整保留，不回到约40s固定风暴。
- 四问：`S1/D0/R1~R2/V0→V2`。M+=exact `boot_trace_v10_s1fj_immediate_reminder_backoff`后`MQTT_TASK start`可直接进入`MQTT_BEG/CONN_BEG`，不存在16s `MQTT_START_STAGGER`；首次失败后仍见`s1fj MQTT_FAIL_BACKOFF`。M-=仍延迟首次连接、退避丢失、普通对话/二次唤醒回退或panic/WDT。
- 构建/归档：完整增量构建已在中断后后台成功收口，应用BIN包含exact marker。`fw_archive/xiaozhi_s1fj.bin` SHA256=`69C6AA295BAB848C3AECFA21F6931C214732D1D884C18ABAD418B68E3B198677`；`elf_snapshots/xiaozhi_s1fj.elf` SHA256=`B38D5ABA63D1EA1B8FC104C8071768E18BC20587AC11496E5F3D918FBBCFDFB6`，均与build产物一致。
- 烧录：精确停止COM7采集父子PID 3904/18176；460800 baud单次写`0x10d000 otadata`、`0x200000 OTA_0`、`0x700000 OTA_1`，三段均hash verified、无timeout/重试，RTS hard reset成功。新采集PID 5684接入时uptime约31s，AFE/heap健康、无panic/WDT，但错过marker/SD边界。`s1ev_acceptance.py`正确输出PENDING；物理断电冷启后再验exact marker、SD/seed、重复TTS嘴层、正常退出及3秒二次唤醒。
- 冷启功能/集成裁决：SD mount、seed 6/6、9个Speaking generation、mouth_arm=18、mouth_blit=49、正常退出及零panic/WDT均通过；4个完整关闭周期的`channel_closed→event=wake_enable`分别为204/203/206/205ms，二次唤醒的`wake_word_detected→Listening`为218ms。`s1ev_acceptance.py`此前把关闭行错误要求为`state=idle`，导致延迟永远PENDING；工具已改为分别检查`wake_rearm_1s`与`wake_detect_to_listening_1s`，py_compile通过，当前两项均PASS。未改固件、无需烧录。
- 进度裁决：s1fj专属`MQTT_FAIL_BACKOFF ... s1fj`、双槽hash verified及冷启SD边界共同证明运行身份；开机早期exact marker文本仍因USB枚举漏采，保留可观测性PENDING。功能/体验/集成短门据实记80%，稳定性不跟涨：08-19早晨9.94h无复位已是账本中的未严格归属marker右删失证据，不转记到s1fj；s1fj寿命仍等per-marker n≥4且mean≥s1cm。

# 2026-08-19 s1fk — 延迟一次性 marker 回显（V0）

- 根因不是固件身份未知，而是ESP32-P4冷启后USB串口常在约20–31s才重新枚举，早期唯一`FW_MARKER`已过去；因此长泡虽可见专属业务日志，仍无法满足严格per-marker寿命统计。下一层M2不能建立在不可归因样本上。
- 唯一变量：复用既有每秒主事件时钟，uptime达到60s时一次性输出`FW_MARKER boot_trace_v10_s1fk_delayed_marker_echo echo=delayed_once`；不新建任务/定时器，不分配PSRAM，不改音频、AFE、显示、SD、MQTT或提醒时序。
- 四问：`S1/D0/R2/V1→V3`。M+=60s后exact marker恰好一次且无日志洪泛；M-=缺失、重复输出或任何功能/时序回退。完整构建通过，应用`0x461900`、5MB分区余12%；BIN SHA256=`F2FC76D72A669271C002580E036E6E5E9677D00B5EC3030B22CE2168363D3446`，ELF SHA256=`6C915B1E958F32FA3BB9FDE9F46902947B6AD8C4C91758FA6C4F03930EBFF9E6`。已归档，待双槽烧录与V1 marker裁决；稳定性仍必须per-marker n≥4且mean≥s1cm。
- 烧录/V1：精确停止COM7采集父子PID 5684/18460；仅写`0x10d000 otadata`、`0x200000 OTA_0`、`0x700000 OTA_1`，三段全部`Hash of data verified`且RTS复位成功，未写bootloader/partition/assets。新采集PID 10412连接后在uptime 61.499s捕获exact延迟marker，到73s未重复，AFE/heap/idle健康、零panic/WDT；marker门PASS。因RTS热启未获得本段SD mount/seed与交互样本，验收器其余项目诚实PENDING；物理冷启与多轮对话后补齐，长期仍按per-marker n≥4裁决。

# 2026-08-20 s1fl — M2a 五档 VisualBudget 影子灰度（V0 + V1）

- 前置裁决：s1fk夜泡从08-19 23:0x POWERON至审计时形成`20329.8s=5.65h`右删失样本，零自然复位；不是`n≥4`正式寿命门。用户明确授权保留s1fk根据地、先进入可回滚M2灰度，有回归即退；因此s1fl寿命独立重新计时，不继承s1fk时长。
- 唯一变量：新增`Frozen/StaticOnly/MouthReduced/MouthOnly/TransitionCandidate`纯分类器；应用主循环每秒读取一次现有线程安全音频诊断快照，只在档位变化或60样本心跳记录`VIS_BUDGET_SHADOW ... apply=0`。`TransitionCandidate`仍须未来通过AFE/MQTT/全屏单例门，本版Face Worker完全不读取预算，不改变任何帧、嘴频、情绪、SD、网络或音频策略。R0=`S1FL_U_VISUAL_BUDGET_SHADOW=0`。
- 四问与门：`S2/D0/R0/V1→V2/V3`。M+=idle candidate、Listening static、Speaking随真实队列落mouth两档/饥饿static，且旧画面行为、音频、退出与二次唤醒不变；M-=日志洪泛、分类永久错误、队列快照锁造成音频回退或任意panic/WDT。完整构建通过，应用`0x461cb0`、分区余12%；BIN SHA256=`480C5E2851CC2A7742F17B06C6868F321C64AE25DBA9503F0A61387708BD3972`，ELF SHA256=`8C9E3D9CA3ADD5DC1F3810646298A99F89BA02DA8CB0C4CD20B1464FAA2503F9`。
- 烧录/V1：精确停止COM7采集父子PID 10412/3520，仅写otadata/OTA_0/OTA_1，三段hash verified且RTS复位成功；新采集PID 6404连接。uptime 61.501s捕获exact `boot_trace_v10_s1fl_m2a_visual_budget_shadow`，64.501s捕获idle `TransitionCandidate ... apply=0`心跳，到108s AFE/heap稳定、零panic/WDT。启动早期初次档位变化被USB枚举漏采不算失败；V2需用户完成一轮正常对话，检查Listening/Speaking档位、音频连续、退出和二次唤醒。
- V2裁决：用户体感“还可以”，脚本`face_integration_gate=PASS/s1ev_gate=PASS`。同一exact marker段覆盖generation 1..13、mouth_arm=24、mouth_blit=69、四次退出重武装205/206/206/207ms、第二轮wake→Listening 224ms，零panic/reboot。影子轨迹完整：Connecting=`Frozen`、Listening=`StaticOnly`、Speaking按pq 1–2/3在`MouthReduced/MouthOnly`切换、idle settle后=`TransitionCandidate`，全部`apply=0`。M2a功能/集成V2通过；不等于s1fl V3寿命通过，下一批只允许M2b嘴部节拍消费，enter/exit与眨眼继续冻结。

# 2026-08-20 s1fm — M2b 嘴部预算消费（V0）

- 唯一变量：只让`MouthReduced/MouthOnly`消费嘴部上升tick；MouthReduced首步立即、后续隔tick，latest-value覆盖且不补旧口型。所有档位的闭嘴回落不受限；预算推迟嘴部时life让出该tick。无新任务/队列/锁/PSRAM/全屏事务，嘴幅、情绪、素材、enter/exit和眨眼不变。
- 为守住首嘴，Speaking队列尚未建立的分类从StaticOnly改为`MouthReduced/provisional`。R0=`S1FM_V_VISUAL_BUDGET_MOUTH_APPLY=0`；失败退s1fl，稳定性根据地仍s1fk。
- 四问与门：`S1/D1/R0/V0→V2→V3`。M+=健康队列行为不变、偏薄时上升降频且不补播、收口/退出/二次唤醒不回退；M-=首嘴冻结、闭嘴慢、音频断续、会话回归或panic/WDT。
- 完整构建通过，应用`0x461e40`、分区余12%；归档BIN SHA256=`E7045ABFFF91448EAA14532E390ADEB76D802773F331D4CCE0699365C1A753E4`，ELF SHA256=`006382AE57268535DE7607767D03D57EA2E70F893B469830FDB6EF7961117A36`。
- 烧录/V1：精确停止COM7采集父子PID 6404/16784，仅写otadata/双OTA，三段hash verified，RTS复位；新采集PID 18284。61.498s捕获exact marker，64.498s预算心跳为`TransitionCandidate apply=1`，AFE/heap健康且零panic/WDT。验收器当前marker/no-reboot PASS，其余交互门PENDING；不计体验或稳定性完成。
- V2裁决：用户体感“还可以”，联合脚本全PASS。generation 1..9、mouth_arm=16、mouth_blit=47，日志证实`MouthReduced/MouthOnly`均进入Face Worker；4次退出重武装209/207/207/203ms，第二轮wake→Listening 225ms，正常退出且panic/reboot=0。M2b功能/体验/集成短门通过；寿命仍须s1fm per-marker V3，不能继承s1fk右删失时长。

# 2026-08-20 存活门成本校正

- 用户明确选择“限时灰度推进，有问题回退”。`n≥4`不再是R0/D0~D1视觉刀的硬阻塞：有失败样本时仍按per-marker mean对标s1cm；零失败时使用右删失累计暴露，6–12h记阶段正证据、48–72h可强通过。V2、匹配ELF、known-good和回滚开关缺一不可。
- 同时最多一项未完成V3的视觉热路径变量；可重复panic/WDT、音频失效、正常退出或二次唤醒回归立即退上一marker。历史寿命记录不改写，后续账本同时列完成寿命与右删失暴露。

# 2026-08-20 s1fn — M2c life 余量门（V0）

- s1fm V2全PASS且用户授权按滚动门继续。唯一变量：Speaking `MouthOnly`允许现有低频life小带；`MouthReduced`丢未完成life轨、只保嘴，恢复时从轨起点继续，不补旧帧。idle背光呼吸、素材、嘴幅、情绪、enter/exit和眨眼均不变。
- R0=`S1FN_W_VISUAL_BUDGET_LIFE_APPLY=0`；M+=薄队列`defer drop_partial=1`、恢复`resume latest_only=1`且音频/嘴/退出连续；M-=健康队列life永久消失、嘴冻、二次唤醒回归或panic/WDT。失败退s1fm，不在s1fn叠刀。
- 完整构建通过，应用`0x461fb0`、余12%；BIN SHA256=`3C1410DE9217AC49497CE08EC6D5987AD84EF382383F66CF44E597BF3E3371E8`，ELF SHA256=`41E57DFFEB3EC99F9F26269D7A48480B2919794D2641EA7EAE2ED3B527C44AA4`。当前仅V0，尚未烧录/真机。
- 烧录/V1：精确停止COM7采集父子PID 18284/13704，只写otadata/双OTA，三段hash verified；新采集PID 19212。61.646s exact marker、70.646s `TransitionCandidate apply=1`，AFE/heap健康且零panic/WDT。RTS热启未捕获SD/seed，PSRAM约32.5MB，验收器仅marker/no-reboot PASS；物理冷启前不验表情、不计V2。
- V2裁决：用户体感“还可以”，联合脚本全PASS。冷启SD/seed 6/6，generation 1..9、mouth_arm=16、mouth_blit=43；`MouthReduced`多次defer且`MouthOnly`恢复latest-only，4次退出重武装205/203/205/204ms，第二轮wake→Listening 225ms，panic/reboot=0。日志中`resume level=MouthReduced`仅发生在life source转不可用后清deferred，未提交life，属探针措辞假阳性；后续裁决只认`resume level=MouthOnly`，不为此重烧。M2c V2通过，进入滚动V3。
